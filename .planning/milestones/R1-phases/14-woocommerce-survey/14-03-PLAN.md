---
phase: 14-woocommerce-survey
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - .planning/compat/SURV-01-woocommerce.md
  - .planning/compat/SCHEMA.md
autonomous: true
requirements: [SURV-01]
must_haves:
  truths:
    - "Every surfaced issue (every degraded/broken cell and interaction-scenario finding) has exactly one classified fix from the four R1 categories"
    - "A success-criterion traceability note maps survey sections to the 4 Phase 14 success criteria and to SURV-01"
    - "Schema gaps/ambiguities surfaced by WooCommerce are resolved in SCHEMA.md under a Phase 14 changelog, and the SURV-01 copy is reconciled to the final schema shape"
    - "The Survey Completion Check is fully satisfied"
  artifacts:
    - path: ".planning/compat/SURV-01-woocommerce.md"
      provides: "Completed Part 3 classified-fix list + traceability note + satisfied completion check; reconciled to final schema"
      contains: "## Part 3"
    - path: ".planning/compat/SCHEMA.md"
      provides: "Final-form schema with a Phase 14 changelog documenting every batched change"
      contains: "Schema changes (Phase 14)"
  key_links:
    - from: "Part 3 classified-fix rows"
      to: "Part 2 surfaced issues (degraded/broken cells + interaction findings)"
      via: "every issue mapped to exactly one of the four R1 fix categories"
      pattern: "slug-resolution tweak|admin_menu re-hook|special-casing|documented limitation"
    - from: ".planning/compat/SURV-01-woocommerce.md"
      to: ".planning/compat/SCHEMA.md"
      via: "SURV-01 reconciled to the final schema shape after batched refinement"
      pattern: "Schema changes \\(Phase 14\\)|reconcil"
---

<objective>
Close out the WooCommerce survey: turn every surfaced issue into a classified fix (Part 3), add a
success-criterion traceability note, then BATCH-APPLY the schema refinements WooCommerce surfaced
into `SCHEMA.md` under a "Schema changes (Phase 14)" changelog and reconcile the SURV-01 copy to the
final schema shape. This is the only point in the phase where SCHEMA.md is edited.

Purpose: Phase success criteria 3 (every issue has a classified fix) and 4 (schema gaps resolved and
committed in final form before Phase 15). WooCommerce is the locked schema stress-test, so this is
the moment the template is hardened against the hardest case before the five lighter Phase 15 surveys
inherit it.

R1 boundary: research only. Fixes are CLASSIFIED into one of four categories — NEVER implemented. No
production menu-handling code is written.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-woocommerce-survey/14-CONTEXT.md
@.planning/compat/SURV-01-woocommerce.md
@.planning/compat/SCHEMA.md
@.planning/phases/14-woocommerce-survey/14-02-SUMMARY.md

<fix_categories_reference>
<!-- The exactly-four allowed R1 fix categories, from SCHEMA.md Part 3. Pick exactly one per issue. -->
1. slug-resolution tweak — issue is Maestro matching the wrong/missing slug (e.g. Woo re-registers under a different slug, or a count-badge title makes the displayed label diverge from the slug). Fix is in how Maestro resolves the slug.
2. later `admin_menu` re-hook (later admin_menu re-hook) — issue is timing: Woo injects/re-registers AFTER Maestro's PHP_INT_MAX replay, so a later re-hook would catch it.
3. special-casing — issue needs WooCommerce-specific handling Maestro can't solve generically (e.g. preserve a count-badge span on rename).
4. documented limitation — accepted as-is; no fix planned (e.g. cosmetic badge loss judged acceptable, or behavior is correct and safe).
</fix_categories_reference>

<phase_success_criteria>
<!-- Map survey sections to these for the traceability note. -->
1. Survey covers HOW WooCommerce registers/manipulates the menu (all six dimensions) → Part 1 + Method/baseline (Plan 01).
2. Every Maestro op classified safe/degraded/broken with evidence → Part 2 matrix + interaction scenarios (Plan 02).
3. Every issue has a classified fix → Part 3 (this plan).
4. Schema gaps resolved, schema committed final before Phase 15 → SCHEMA.md changelog + SURV-01 reconcile (this plan).
</phase_success_criteria>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classified-fix list (Part 3) + success-criterion traceability note</name>
  <files>.planning/compat/SURV-01-woocommerce.md</files>
  <action>
    1. Enumerate every surfaced issue: each degraded/broken cell in the Part 2 matrix and each
       degraded/broken interaction-scenario finding (use the 14-02-SUMMARY issue list as the index).
    2. For each issue, add a Part 3 row: issue summary, affected operation(s), exactly ONE chosen fix
       category (see fix_categories_reference), and a one-line rationale grounded in the observed
       evidence and the late-replay timing cause. Every surfaced issue MUST appear — no orphans (this
       feeds Phase 16's DELV-02 backlog, which forbids orphaned issues). Issues that are genuinely
       fine get classified as "documented limitation" with a rationale (e.g. cosmetic badge loss).
    3. Add a short "## Success-Criterion Traceability" section mapping survey sections to the 4 Phase 14
       success criteria + the SURV-01 requirement (use phase_success_criteria), so the gsd-verifier and
       Phase 16 confirm coverage without inference.
    4. Walk the "## Survey Completion Check" checklist at the bottom of the template and tick every box,
       confirming each is genuinely satisfied (all dimensions noted, every top-level + submenu item has a
       row, all four op columns classified with evidence, every issue has exactly one classified fix,
       SURV file under the right path).
  </action>
  <verify>
    <automated>grep -q "## Part 3" .planning/compat/SURV-01-woocommerce.md && grep -Eqi "slug-resolution tweak|admin_menu re-hook|special-casing|documented limitation" .planning/compat/SURV-01-woocommerce.md && grep -qi "Traceability" .planning/compat/SURV-01-woocommerce.md && grep -qi "SURV-01" .planning/compat/SURV-01-woocommerce.md</automated>
  </verify>
  <done>Every surfaced issue (matrix + interaction) has a Part 3 row with exactly one of the four R1 fix categories and a one-line rationale; no issue is orphaned. A Success-Criterion Traceability section maps sections to the 4 criteria + SURV-01. The Survey Completion Check is fully ticked and genuinely satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Batched SCHEMA.md refinement + changelog, then reconcile SURV-01</name>
  <files>.planning/compat/SCHEMA.md, .planning/compat/SURV-01-woocommerce.md</files>
  <action>
    1. Review the "Schema-change candidates (Phase 14)" scratch list accumulated across Plans 01-02
       plus anything Part 3 surfaced. Decide which are real schema improvements vs. WooCommerce-specific
       noise. Apply the accepted changes to SCHEMA.md NOW (this is the only point SCHEMA.md is edited).
       Permitted scope per CONTEXT: add/clarify columns or cells (e.g. an explicit persistence/timing
       sub-cell convention, an N/A convention for top-level-only re-icon), tighten dimension wording, or
       restructure a dimension/column if WooCommerce proved the shape wrong (low cost — SURV-01 is the
       only survey to reconcile).
    2. If the interaction-scenarios sub-section proved broadly useful (Plan 02 flagged it), promote it
       into SCHEMA.md as an optional "Interaction notes" section so Phase 15 plugins inherit it; otherwise
       leave it WooCommerce-specific and note that decision.
    3. Add a "## Schema changes (Phase 14)" changelog section to SCHEMA.md listing every change made,
       each with a one-line reason tied to what WooCommerce surfaced. If NO changes were needed, still add
       the changelog section stating "No changes — schema validated unchanged against the WooCommerce
       stress-test" so success criterion 4 is provably addressed.
    4. RECONCILE SURV-01-woocommerce.md to the final schema shape: if columns/sections changed, update the
       SURV-01 copy to match so it stays a faithful instance of the final template (Phase 16 synthesis is
       mechanical only if structures match). Remove the now-consumed "Schema-change candidates" scratch
       list from SURV-01 (its content is captured in the SCHEMA.md changelog).
    5. Sanity-check both files render as valid markdown (tables intact, headings well-formed).
  </action>
  <verify>
    <automated>grep -qi "Schema changes (Phase 14)" .planning/compat/SCHEMA.md && grep -q "## Part 1" .planning/compat/SURV-01-woocommerce.md && grep -q "## Part 2" .planning/compat/SURV-01-woocommerce.md && grep -q "## Part 3" .planning/compat/SURV-01-woocommerce.md && ! grep -qi "Schema-change candidates" .planning/compat/SURV-01-woocommerce.md</automated>
  </verify>
  <done>SCHEMA.md carries a "Schema changes (Phase 14)" changelog documenting every batched change (or stating none were needed); accepted refinements are applied; the interaction-scenarios promote/keep decision is recorded; SURV-01 is reconciled to the final schema shape with its scratch list consumed. Both files are valid markdown.</done>
</task>

</tasks>

<verification>
- Part 3: every surfaced issue (matrix degraded/broken + interaction findings) has exactly one of the four R1 fix categories with a rationale; no orphans.
- Success-Criterion Traceability section maps survey sections to the 4 Phase 14 success criteria + SURV-01.
- Survey Completion Check fully ticked and genuinely satisfied.
- SCHEMA.md has a "Schema changes (Phase 14)" changelog (changes applied, or explicitly none); it is in final form for Phase 15.
- SURV-01 reconciled to the final schema shape; scratch list consumed; both files valid markdown.
- R1 boundary held: fixes classified, never implemented; no production menu-handling code touched.
</verification>

<success_criteria>
Maps to phase success criteria 3 (every issue carries a classified fix) and 4 (schema gaps resolved and committed in final form before Phase 15). Completes SURV-01 and finalizes SCHEMA.md so Phase 15 inherits a battle-tested template.
</success_criteria>

<output>
After completion, create `.planning/phases/14-woocommerce-survey/14-03-SUMMARY.md` summarizing the classified-fix counts by category and the schema changes made (or that none were needed). Stop the compat harness (`npm run compat:stop`) if no further work is queued.
</output>
