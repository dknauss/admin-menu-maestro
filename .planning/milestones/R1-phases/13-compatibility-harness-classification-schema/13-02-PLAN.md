---
phase: 13-compatibility-harness-classification-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/compat/SCHEMA.md
autonomous: true
requirements: [SCHM-01]
user_setup: []

must_haves:
  truths:
    - "A committed schema template exists at the stable milestone-level path .planning/compat/SCHEMA.md"
    - "The template names all six manipulation dimensions: custom positions, conditional/late injection, re-registered menus, count badges in titles, custom separators, direct $menu/$submenu surgery"
    - "The template has a matrix with a column for each Maestro operation (rename / reorder / hide / re-icon) and cells classified safe / degraded / broken"
    - "The template defines a three-part per-survey structure surveys copy and fill in"
    - "The schema is committed before any SURV-xx file is authored (no SURV-xx files exist in this phase)"
  artifacts:
    - path: ".planning/compat/SCHEMA.md"
      provides: "Canonical, pristine classification-schema template: 6-dimension checklist + rename/reorder/hide/re-icon x safe/degraded/broken matrix + classified-fix list"
      contains: "safe / degraded / broken"
      min_lines: 60
  key_links:
    - from: ".planning/compat/SCHEMA.md"
      to: "Phases 14-16 surveys"
      via: "copy-the-template-per-plugin instruction (e.g. SURV-01-woocommerce.md)"
      pattern: "SURV-"
---

<objective>
Author the committed classification-schema template at the shared milestone path `.planning/compat/SCHEMA.md`. It is the single canonical format every R1 survey (Phases 14-15) copies and fills in, and the structure Phase 16 synthesizes mechanically.

Purpose: SCHM-01 — a consistent schema committed BEFORE any survey, so cross-plugin synthesis (DELV-01) is mechanical.

Output: `.planning/compat/SCHEMA.md`. Pure documentation — no code, no SURV-xx files authored in this phase (the template must precede them).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-compatibility-harness-classification-schema/13-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the canonical schema template at .planning/compat/SCHEMA.md</name>
  <files>.planning/compat/SCHEMA.md</files>
  <action>
Create `.planning/compat/SCHEMA.md` (new `.planning/compat/` sibling dir of `.planning/phases/`). This is the PRISTINE template — written so each survey copies it verbatim into its own file (e.g. `.planning/compat/SURV-01-woocommerce.md`) and fills it in. Do NOT author any SURV-xx file here; only the template.

The document must contain, in this order:

1. Header / usage note — state that this is the canonical R1 classification schema; that every survey (SURV-01..SURV-06) copies this file to `.planning/compat/SURV-NN-<plugin>.md` and fills it in; that the template itself stays pristine; and that it was committed before any survey (SCHM-01 / success criterion 4). Include placeholder front fields a survey fills: Plugin, Slug, Pinned version (from tests/compat/VERSIONS.md), Date surveyed, Surveyor.

2. Part 1 — Manipulation-dimensions checklist. A checkbox list of exactly the six locked dimensions, each with a "Notes:" field for the survey to describe how that plugin exhibits it:
   - Custom menu positions (explicit $position in add_menu_page / high or fractional positions)
   - Conditional / late injection (menus added on later hooks, conditionally, or after the default admin_menu priority)
   - Re-registered menus (menu removed then re-added, or slug re-registered)
   - Count badges baked into titles (e.g. an awaiting-mod / update bubble span inside the menu title string)
   - Custom separators (custom add_menu_page separators or direct $menu separator rows)
   - Direct $menu / $submenu global surgery (writes to the $menu/$submenu globals rather than the API)
   Add a one-line definition next to each so surveyors classify consistently.

3. Part 2 — Classification matrix. A Markdown table: rows = affected menu items (top-level and submenu; one example placeholder row plus instructions to add one row per affected item), columns = the four Maestro operations Rename | Reorder | Hide | Re-icon. Each cell holds a classification — `safe` / `degraded` / `broken` — plus a short observable-evidence note (what was observed, e.g. "rename persists across reload" / "reorder reverts on next admin_menu pass"). Define the three classifications explicitly above the table:
   - safe — operation works as expected, persists, no side effects.
   - degraded — operation partially works or works with caveats / cosmetic loss (e.g. count badge lost on rename).
   - broken — operation fails, reverts, or breaks the plugin's menu/access.
   Provide one fully worked example row (clearly marked as illustrative) so surveyors see the expected cell format.

4. Part 3 — Classified-fix list. A list/table where each surfaced issue gets a classified fix using exactly these four R1 categories (verbatim from REQUIREMENTS):
   - slug-resolution tweak
   - later admin_menu re-hook
   - special-casing
   - documented limitation
   Each entry: issue summary, affected operation(s) from the matrix, chosen classification, and a one-line rationale. Note that these feed DELV-02's prioritized backlog in Phase 16.

Keep wording aligned verbatim with REQUIREMENTS.md (the six dimensions, the four operations, the three classifications, the four fix categories) so synthesis is mechanical. Exact headings/filenames inside the doc are at your discretion as long as all of the above is present.
  </action>
  <verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/compat/SCHEMA.md','utf8'); const low=t.toLowerCase(); ['custom','conditional','re-registered','count badge','separator','\$menu'].forEach(d=>{ if(!low.includes(d.toLowerCase())) throw new Error('missing dimension: '+d); }); ['rename','reorder','hide','re-icon'].forEach(o=>{ if(!low.includes(o)) throw new Error('missing operation: '+o); }); ['safe','degraded','broken'].forEach(c=>{ if(!low.includes(c)) throw new Error('missing classification: '+c); }); ['slug-resolution','admin_menu re-hook','special-cas','documented limitation'].forEach(f=>{ if(!low.includes(f.toLowerCase())) throw new Error('missing fix category: '+f); }); if(!/SURV-/.test(t)) throw new Error('missing copy-to-SURV instruction'); if(t.split('\n').length<60) throw new Error('template too short'); console.log('SCHEMA.md template OK');"</automated>
  </verify>
  <done>.planning/compat/SCHEMA.md exists with: usage header + survey-front fields; Part 1 six-dimension checklist; Part 2 rename/reorder/hide/re-icon matrix with safe/degraded/broken cells + one worked example row; Part 3 classified-fix list using the four R1 fix categories. No SURV-xx file authored.</done>
</task>

</tasks>

<verification>
- `.planning/compat/SCHEMA.md` exists at the milestone-level path (not inside the phase dir).
- All six manipulation dimensions, all four Maestro operations, all three classifications, and all four fix categories are present and worded to match REQUIREMENTS.md.
- A copy-to-SURV-NN instruction makes the template reusable; the template stays pristine.
- No SURV-xx survey file is authored in this phase (template precedes surveys — SCHM-01 success criterion 4).
</verification>

<success_criteria>
SCHM-01: a committed schema document defines the six manipulation dimensions and a matrix template (rename/reorder/hide/re-icon columns; safe/degraded/broken cells) plus the classified-fix structure — confirmed by the committed `.planning/compat/SCHEMA.md`, committed before any survey.
</success_criteria>

<output>
After completion, create `.planning/phases/13-compatibility-harness-classification-schema/13-02-SUMMARY.md`
</output>
