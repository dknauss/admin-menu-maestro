---
phase: 15-remaining-survey-set
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/compat/SURV-06-lifterlms.md
  - .planning/compat/SURV-06-assets/
autonomous: true
requirements: [SURV-06]
must_haves:
  truths:
    - "A committed SURV-06-lifterlms.md exists that fills the final SCHEMA.md template structurally identically to SURV-01"
    - "Every affected LifterLMS top-level and submenu item has a Part 2 matrix row with rename/reorder/hide/re-icon each classified safe/degraded/broken plus observable evidence"
    - "Every surfaced issue carries exactly one R1 classified fix"
    - "Setup-wizard-gated items are tagged [state]; if LifterLMS's own role (lms_manager/instructor) materially affects per-role Hide, it is provisioned via the survey procedure (NOT by editing committed .wp-env.json) and observed"
    - "The SCHEMA.md Survey Completion Check boxes are all satisfiable and a success-criterion traceability note maps sections to the 4 Phase-15 criteria + SURV-06"
  artifacts:
    - path: ".planning/compat/SURV-06-lifterlms.md"
      provides: "Filled LifterLMS compatibility survey (Parts 1-3 + Method header + Interaction Scenarios + traceability + completion check)"
      contains: "Part 1 — Manipulation-Dimensions Checklist"
    - path: ".planning/compat/SURV-06-assets/"
      provides: "Per-role natural-state baseline dumps + reused dump/probe scripts (planner-discretion: inline if volume is small)"
  key_links:
    - from: ".planning/compat/SURV-06-lifterlms.md"
      to: ".planning/compat/SCHEMA.md"
      via: "structural copy of the final template (all 7 Phase-14 conventions applied)"
      pattern: "Survey Completion Check"
    - from: ".planning/compat/SURV-06-lifterlms.md"
      to: "includes/class-replay.php"
      via: "classifies Maestro replay ops against LifterLMS's top-level + submenus"
      pattern: "PHP_INT_MAX"
---

<objective>
Survey **LifterLMS** (free LMS; `lifterlms` 10.0.8) for Admin Menu Maestro R1 compatibility (requirement SURV-06), producing `.planning/compat/SURV-06-lifterlms.md` as a filled copy of the finalized `SCHEMA.md`, structured identically to the SURV-01 exemplar so Phase 16 synthesis is mechanical.

Purpose: Document HOW LifterLMS registers/manipulates the admin menu (its own top-level **LifterLMS** menu plus submenus, and a custom `llms-separator`), classify every Maestro operation (rename / reorder / hide / re-icon) against each affected item as safe / degraded / broken with observable runtime evidence, and assign every surfaced issue exactly one classified R1 fix.

Output: `.planning/compat/SURV-06-lifterlms.md` (+ optional `.planning/compat/SURV-06-assets/`).

**R1 boundary — research only.** Fixes are CLASSIFIED, never implemented. No production menu-handling code is committed.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# THE methodology source (locked, inherited unchanged):
@.planning/phases/14-woocommerce-survey/14-CONTEXT.md

# THE final template to copy and fill (apply all 7 Phase-14 conventions):
@.planning/compat/SCHEMA.md

# THE worked exemplar — mirror its structure EXACTLY. Do NOT re-survey WooCommerce:
@.planning/compat/SURV-01-woocommerce.md

# Reusable, plugin-agnostic survey assets:
@.planning/compat/SURV-01-assets/dump-menu.php
@.planning/compat/SURV-01-assets/reorder-probe.php

# Pinned-version ledger (cite LifterLMS's pinned version + slug):
@tests/compat/VERSIONS.md

# The survey's SUBJECT — the replay engine being classified:
@includes/class-replay.php
@includes/class-ordering.php
@includes/class-config.php

<harness>
Committed Phase 13 harness ALREADY loads `lifterlms` 10.0.8 + the provisioned roles (admin,
compat_editor, compat_shop_manager). NO harness changes needed. `npm run compat:start` →
http://localhost:8890 ; `npm run compat:stop`. Docker required; cold boot ~15 min. Confirm `lifterlms`
active: `cd tests/compat && npx wp-env run cli wp plugin list --status=active`.

NOTE: SURV-01 already observed LifterLMS adds a custom `llms-separator` row (visible in the WooCommerce
baseline dumps) — confirm and characterize it here as part of this plugin's own survey.
</harness>

<method_inheritance>
Method is LOCKED by 14-CONTEXT.md and demonstrated in SURV-01's Method header. Reuse VERBATIM, adapting
only plugin name/slug:
- **Hybrid source + runtime dump.** Read LifterLMS's `admin_menu` registration to explain HOW, confirm
  WHAT via WP-CLI `$menu`/`$submenu` dumps.
- **`dump-menu.php`** (copy to SURV-06-assets, adapt header): hooks `admin_menu @ PHP_INT_MAX`, prints the
  per-row fields, exits before WP cap filtering. Run per role with `--user=` and
  `--exec="define('WP_ADMIN', true);"`; confirm/document whether LifterLMS's menu needs WP_ADMIN.
- **Dumps are REPLAY STATE, not the rendered sidebar** (convention 7): per-role Hide evidence MUST come
  from a separate rendered/post-cap-filter check.
- **Top-level Reorder from EFFECTIVE rendered order** via `reorder-probe.php`, not the raw global —
  especially relevant given LifterLMS's `llms-separator` (cf. WooCommerce's separator re-clustering, F4).
- **Config-driven op application:** write `maestro_config`, re-dump, compare, `wp option delete
  maestro_config` between cases. Spot-check notable items in the editor UI at discretion.

  ```bash
  cd tests/compat
  npx wp-env run cli -- php -d memory_limit=512M /usr/local/bin/wp \
    --exec="define('WP_ADMIN', true);" \
    eval-file wp-content/plugins/maestro-menu-editor/.planning/compat/SURV-06-assets/dump-menu.php \
    --user=admin            # repeat for compat_editor / compat_shop_manager (+ lms_manager if provisioned)
  npx wp-env run cli wp option delete maestro_config
  npx wp-env run cli -- wp option update maestro_config '<json>' --format=json
  ```
</method_inheritance>

<lifterlms_specifics>
- LifterLMS registers its own top-level **LifterLMS** menu plus submenus (e.g. Courses, Memberships,
  Students/Reporting, Engagements, Orders, Add-ons & more, Settings, Status, Help). It ALSO adds a custom
  **`llms-separator`** row (already seen in SURV-01's dumps). Determine the exact affected set at runtime
  from the natural-state dump — do not assume from memory. The separator makes the F4-style render-time
  reorder interaction worth probing here too.
- **Setup wizard.** LifterLMS has a setup wizard. Tag wizard-gated menu items `[state]` (record
  wizard-incomplete behavior; describe wizard-complete from source without performing external setup).
- **Plugin's own roles.** LifterLMS ships `lms_manager` and `instructor` roles; the harness only
  provisions `compat_editor` + `compat_shop_manager`. The baseline three roles (admin + editor +
  shop_manager) are the default observation set. IF — and only if — runtime observation shows a
  LifterLMS-owned role MATERIALLY changes per-role Hide behavior (i.e. LifterLMS rows render for
  `lms_manager` that none of the three baseline roles see, so Hide is otherwise untestable for those
  rows), provision `lms_manager` via the SURVEY PROCEDURE (a one-off WP-CLI `wp user create` /
  `wp user set-role` run, documented in the Method header) — do NOT edit the committed
  `tests/compat/.wp-env.json` afterStart. This is planner/executor discretion grounded in what actually
  appears at runtime; if the three baseline roles already exercise the LifterLMS Hide surface adequately,
  note that and skip the extra role.
- Watch for: any notice/count badge baked into a title (convention 3); any entity-encoded (`&amp;`) slug,
  especially the Courses/Memberships custom-post-type taxonomy submenus (flag for a slug-resolution
  tweak); custom menu position; the `llms-separator` (skipped by Maestro like WooCommerce's separator —
  classify its Reorder cell from effective render order).
</lifterlms_specifics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot harness, copy template, write Part 1 + Method header + per-role natural-state baselines</name>
  <files>.planning/compat/SURV-06-lifterlms.md, .planning/compat/SURV-06-assets/dump-menu.php, .planning/compat/SURV-06-assets/reorder-probe.php, .planning/compat/SURV-06-assets/baseline-*.txt</files>
  <action>
1. Boot the compat harness (`npm run compat:start`; Docker required — surface and pause if blocked).
   Confirm `lifterlms` 10.0.8 active and the three users provisioned.
2. Copy `SCHEMA.md` to `.planning/compat/SURV-06-lifterlms.md`; fill FRONT FIELDS (Plugin: LifterLMS;
   Slug: `lifterlms`; Pinned version: `10.0.8`; Date; Surveyor).
3. Create `.planning/compat/SURV-06-assets/`; copy `dump-menu.php` + `reorder-probe.php`, adapting only
   header comments to LifterLMS. Confirm/document whether LifterLMS's menu requires the `WP_ADMIN` define.
4. Write the **Method / how evidence was gathered** header mirroring SURV-01's (harness boot, per-role
   dump command, replay-state-not-rendered-sidebar warning, top-level-reorder effective-order exception,
   per-role two-gate observation, classification rubric, per-plan traceability table). If runtime shows a
   LifterLMS-owned role is needed for adequate Hide coverage, document the one-off `lms_manager`
   provisioning command here and add it to the per-role set; otherwise state the three baseline roles
   suffice and why.
5. Capture per-role natural-state (`maestro_config` deleted) baselines → `SURV-06-assets/baseline-<role>.txt`.
   Tag wizard-gated items `[state]`. Confirm and characterize the `llms-separator` row. Inline small dumps
   at discretion if volume is low.
6. Fill **Part 1 — Manipulation-Dimensions Checklist**: check each of the six locked dimensions LifterLMS
   exhibits with `Notes:` (source citation under `wp-content/plugins/lifterlms/` + runtime confirmation),
   explicitly covering the `llms-separator` (custom separators dimension) and any direct `$menu`/`$submenu`
   surgery. Apply convention 3 + the entity-encoded-slug note where applicable. Add a baseline "revealing
   slices" sub-section and an affected-items inventory (top-level + all submenus + separator) that seeds
   the Task 2 matrix.
  </action>
  <verify>
    <automated>test -f .planning/compat/SURV-06-lifterlms.md && grep -q "Part 1 — Manipulation-Dimensions Checklist" .planning/compat/SURV-06-lifterlms.md && grep -q "Method" .planning/compat/SURV-06-lifterlms.md && grep -qi "lifterlms" .planning/compat/SURV-06-lifterlms.md && grep -q "10.0.8" .planning/compat/SURV-06-lifterlms.md && echo OK</automated>
  </verify>
  <done>SURV-06-lifterlms.md exists with filled front fields, a Method header reproducing the locked procedure (with the per-role set decision documented re: lms_manager), per-role natural-state baselines (wizard-gated items tagged [state], llms-separator characterized), and Part 1's six-dimension checklist filled with source + runtime evidence and an affected-items inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Write Part 2 matrix + Interaction Scenarios + Part 3 classified fixes + traceability + completion check</name>
  <files>.planning/compat/SURV-06-lifterlms.md</files>
  <action>
1. Fill **Part 2 — Classification Matrix**: one row per affected LifterLMS item (top-level + every submenu
   + the `llms-separator` from the Task 1 inventory). Classify Rename / Reorder / Hide (per role) / Re-icon
   safe/degraded/broken with observable evidence. Apply ALL conventions: per-cell persistence + timing
   cause; top-level Reorder from effective rendered order (reorder-probe), checking whether `llms-separator`
   re-clusters the way WooCommerce's did (F4-style); Hide per-role two-gate model ({Maestro cosmetic
   `unset()`} + {WP cap gate}) with loads-vs-403 and "moot no-op" where WP already gates a role away
   (include `lms_manager` sub-cells if that role was provisioned), evidence from the separate rendered
   check; separator rows = N/A (skipped by Maestro) with reason; submenu Re-icon = N/A; `[state]` on
   wizard-gated cells; recurring evidence stated once as F1…Fn and referenced per cell.
2. Add the **Interaction Scenarios** section — apply S1 (hide-parent-with-visible-children), S2
   (rename+reorder), S3 (re-icon+reorder-across-separator, especially meaningful given `llms-separator`).
   Reset config after each; classify with evidence + persistence + timing cause; mark any N/A probe.
3. Fill **Part 3 — Classified-Fix List**: every surfaced issue → exactly ONE R1 fix category with a
   one-line rationale; no orphans; cross-reference shared findings as SURV-01 does (the separator
   re-cluster, if present, parallels WooCommerce's F4 → likely documented limitation).
4. Add the **Success-Criterion Traceability** table (sections → 4 Phase-15 criteria + SURV-06) and tick the
   **Survey Completion Check** boxes. Set Status to Complete. Do NOT edit SCHEMA.md.
  </action>
  <verify>
    <automated>grep -q "Part 2 — Classification Matrix" .planning/compat/SURV-06-lifterlms.md && grep -q "Part 3 — Classified-Fix List" .planning/compat/SURV-06-lifterlms.md && grep -q "Success-Criterion Traceability\|Success-criterion traceability" .planning/compat/SURV-06-lifterlms.md && grep -q "Survey Completion Check" .planning/compat/SURV-06-lifterlms.md && grep -Eqi "safe|degraded|broken" .planning/compat/SURV-06-lifterlms.md && grep -q "SURV-06" .planning/compat/SURV-06-lifterlms.md && echo OK</automated>
  </verify>
  <done>SURV-06-lifterlms.md is a complete, schema-faithful survey: every affected item (incl. separator) has a matrix row with all four ops classified + evidence + persistence; Interaction Scenarios applied (incl. separator-crossing) or explicitly N/A; every issue carries exactly one classified fix with no orphans; traceability maps to the 4 criteria + SURV-06; all completion-check boxes satisfiable. SCHEMA.md unmodified.</done>
</task>

</tasks>

<verification>
- `.planning/compat/SURV-06-lifterlms.md` exists and structurally mirrors SURV-01.
- Every affected LifterLMS item (top-level + submenus + `llms-separator`) has a matrix row; every op cell classified safe/degraded/broken with evidence + persistence.
- Wizard-gated items tagged `[state]`; lms_manager provisioning decision documented (provisioned via survey procedure only if runtime warrants, never via committed .wp-env.json).
- Every surfaced issue carries exactly one classified fix; no orphans.
- `.planning/compat/SCHEMA.md` byte-for-byte unchanged; `tests/compat/.wp-env.json` unchanged.
- No production menu-handling code committed (R1 boundary).
</verification>

<success_criteria>
- SURV-06-lifterlms.md committed, filling the SCHEMA template identically to SURV-01 (criteria 1 & 4).
- Every Maestro op against each LifterLMS menu item classified with evidence (criterion 2).
- Every identified issue carries a classified fix (criterion 3).
- Completion Check satisfiable; traceability maps sections → 4 criteria + SURV-06.
</success_criteria>

<output>
After completion, create `.planning/phases/15-remaining-survey-set/15-05-SUMMARY.md`.
</output>
