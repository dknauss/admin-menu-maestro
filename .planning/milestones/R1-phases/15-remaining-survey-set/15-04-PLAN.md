---
phase: 15-remaining-survey-set
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/compat/SURV-05-wpforms.md
  - .planning/compat/SURV-05-assets/
autonomous: true
requirements: [SURV-05]
must_haves:
  truths:
    - "A committed SURV-05-wpforms.md exists that fills the final SCHEMA.md template structurally identically to SURV-01"
    - "Every affected WPForms top-level and submenu item has a Part 2 matrix row with rename/reorder/hide/re-icon each classified safe/degraded/broken plus observable evidence"
    - "Every surfaced issue carries exactly one R1 classified fix"
    - "First-run / Lite-vs-Pro feature-gated menu items are tagged [state] with both behaviors recorded where they diverge"
    - "The SCHEMA.md Survey Completion Check boxes are all satisfiable and a success-criterion traceability note maps sections to the 4 Phase-15 criteria + SURV-05"
  artifacts:
    - path: ".planning/compat/SURV-05-wpforms.md"
      provides: "Filled WPForms Lite compatibility survey (Parts 1-3 + Method header + Interaction Scenarios + traceability + completion check)"
      contains: "Part 1 — Manipulation-Dimensions Checklist"
    - path: ".planning/compat/SURV-05-assets/"
      provides: "Per-role natural-state baseline dumps + reused dump/probe scripts (planner-discretion: inline if volume is small)"
  key_links:
    - from: ".planning/compat/SURV-05-wpforms.md"
      to: ".planning/compat/SCHEMA.md"
      via: "structural copy of the final template (all 7 Phase-14 conventions applied)"
      pattern: "Survey Completion Check"
    - from: ".planning/compat/SURV-05-wpforms.md"
      to: "includes/class-replay.php"
      via: "classifies Maestro replay ops against WPForms's menu"
      pattern: "PHP_INT_MAX"
---

<objective>
Survey **WPForms Lite** (`wpforms-lite` 1.10.2.1) for Admin Menu Maestro R1 compatibility (requirement SURV-05), producing `.planning/compat/SURV-05-wpforms.md` as a filled copy of the finalized `SCHEMA.md`, structured identically to the SURV-01 exemplar so Phase 16 synthesis is mechanical.

Purpose: Document HOW WPForms registers/manipulates the admin menu (its own top-level **WPForms** menu plus submenus), classify every Maestro operation (rename / reorder / hide / re-icon) against each affected item as safe / degraded / broken with observable runtime evidence, and assign every surfaced issue exactly one classified R1 fix.

Output: `.planning/compat/SURV-05-wpforms.md` (+ optional `.planning/compat/SURV-05-assets/`).

**R1 boundary — research only.** Fixes are CLASSIFIED, never implemented. No production menu-handling code is committed.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# THE methodology source (locked, inherited unchanged):
@.planning/phases/14-woocommerce-survey/14-CONTEXT.md

# THE final template to copy and fill (apply all 7 Phase-14 conventions):
@.planning/compat/SCHEMA.md

# THE worked exemplar — mirror its structure EXACTLY. Do NOT re-survey WooCommerce:
@.planning/compat/SURV-01-woocommerce.md

# Reusable, plugin-agnostic survey assets:
@.planning/compat/SURV-01-assets/dump-menu.php
@.planning/compat/SURV-01-assets/reorder-probe.php

# Pinned-version ledger (cite WPForms Lite's pinned version + slug):
@tests/compat/VERSIONS.md

# The survey's SUBJECT — the replay engine being classified:
@includes/class-replay.php
@includes/class-ordering.php
@includes/class-config.php

<harness>
Committed Phase 13 harness ALREADY loads `wpforms-lite` 1.10.2.1 + the provisioned roles. NO harness
changes needed. `npm run compat:start` → http://localhost:8890 ; `npm run compat:stop`. Docker required;
cold boot ~15 min. Confirm `wpforms-lite` active: `cd tests/compat && npx wp-env run cli wp plugin list`.
</harness>

<method_inheritance>
Method is LOCKED by 14-CONTEXT.md and demonstrated in SURV-01's Method header. Reuse VERBATIM, adapting
only plugin name/slug:
- **Hybrid source + runtime dump.** Read WPForms's `admin_menu` registration to explain HOW, confirm WHAT
  via WP-CLI `$menu`/`$submenu` dumps.
- **`dump-menu.php`** (copy to SURV-05-assets, adapt header): hooks `admin_menu @ PHP_INT_MAX`, prints the
  per-row fields, exits before WP cap filtering. Run per role with `--user=` and
  `--exec="define('WP_ADMIN', true);"`; confirm/document whether WPForms's menu needs WP_ADMIN.
- **Dumps are REPLAY STATE, not the rendered sidebar** (convention 7): per-role Hide evidence MUST come
  from a separate rendered/post-cap-filter check.
- **Top-level Reorder from EFFECTIVE rendered order** via `reorder-probe.php`, not the raw global.
- **Config-driven op application:** write `maestro_config`, re-dump, compare, `wp option delete
  maestro_config` between cases. Spot-check notable items in the editor UI at discretion.
- **Per-role:** admin + compat_editor + compat_shop_manager (all provisioned). WPForms ships its own
  access capabilities (e.g. `wpforms_*` caps) — observe how those intersect the per-role render gate;
  it ships no special menu role of its own to provision; confirm at runtime.

  ```bash
  cd tests/compat
  npx wp-env run cli -- php -d memory_limit=512M /usr/local/bin/wp \
    --exec="define('WP_ADMIN', true);" \
    eval-file wp-content/plugins/maestro-menu-editor/.planning/compat/SURV-05-assets/dump-menu.php \
    --user=admin            # repeat for compat_editor / compat_shop_manager
  npx wp-env run cli wp option delete maestro_config
  npx wp-env run cli -- wp option update maestro_config '<json>' --format=json
  ```
</method_inheritance>

<wpforms_specifics>
- WPForms registers a top-level **WPForms** menu plus submenus (e.g. All Forms, Add New, Entries [Pro],
  Payments, Tools, Settings, Addons, Analytics/SMTP/About upsells, Community). The Lite build gates several
  items behind a Pro upsell. Determine the exact affected set at runtime from the natural-state dump — do
  not assume from memory.
- **Setup is light** (first-run welcome/onboarding, no hard external gate). Note any first-run or
  Lite-vs-Pro feature-gated items with `[state]` (record the Lite behavior; describe Pro-gated items from
  source without buying/activating Pro).
- Watch for: an upgrade/"Upgrade to Pro" badge or count badge baked into a title (convention 3 → degraded
  on rename if present); any entity-encoded (`&amp;`) slug (flag for a slug-resolution tweak); custom menu
  position.
</wpforms_specifics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot harness, copy template, write Part 1 + Method header + per-role natural-state baselines</name>
  <files>.planning/compat/SURV-05-wpforms.md, .planning/compat/SURV-05-assets/dump-menu.php, .planning/compat/SURV-05-assets/reorder-probe.php, .planning/compat/SURV-05-assets/baseline-*.txt</files>
  <action>
1. Boot the compat harness (`npm run compat:start`; Docker required — surface and pause if blocked).
   Confirm `wpforms-lite` 1.10.2.1 active and the three users provisioned.
2. Copy `SCHEMA.md` to `.planning/compat/SURV-05-wpforms.md`; fill FRONT FIELDS (Plugin: WPForms Lite;
   Slug: `wpforms-lite`; Pinned version: `1.10.2.1`; Date; Surveyor).
3. Create `.planning/compat/SURV-05-assets/`; copy `dump-menu.php` + `reorder-probe.php`, adapting only
   header comments to WPForms. Confirm/document whether WPForms's menu requires the `WP_ADMIN` define.
4. Write the **Method / how evidence was gathered** header mirroring SURV-01's (harness boot, per-role
   dump command, replay-state-not-rendered-sidebar warning, top-level-reorder effective-order exception,
   per-role two-gate observation, classification rubric, per-plan traceability table).
5. Capture per-role natural-state (`maestro_config` deleted) baselines → `SURV-05-assets/baseline-<role>.txt`.
   Tag first-run / Lite-vs-Pro feature-gated items `[state]`. Inline small dumps at discretion if volume
   is low.
6. Fill **Part 1 — Manipulation-Dimensions Checklist**: check each of the six locked dimensions WPForms
   exhibits with `Notes:` (source citation under `wp-content/plugins/wpforms-lite/` + runtime confirmation).
   Apply convention 3 + the entity-encoded-slug note where applicable. Add a baseline "revealing slices"
   sub-section and an affected-items inventory that seeds the Task 2 matrix.
  </action>
  <verify>
    <automated>test -f .planning/compat/SURV-05-wpforms.md && grep -q "Part 1 — Manipulation-Dimensions Checklist" .planning/compat/SURV-05-wpforms.md && grep -q "Method" .planning/compat/SURV-05-wpforms.md && grep -qi "wpforms" .planning/compat/SURV-05-wpforms.md && grep -q "1.10.2.1" .planning/compat/SURV-05-wpforms.md && echo OK</automated>
  </verify>
  <done>SURV-05-wpforms.md exists with filled front fields, a Method header reproducing the locked procedure, per-role natural-state baselines (first-run/Lite-vs-Pro items tagged [state]), and Part 1's six-dimension checklist filled with source + runtime evidence and an affected-items inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Write Part 2 matrix + Interaction Scenarios + Part 3 classified fixes + traceability + completion check</name>
  <files>.planning/compat/SURV-05-wpforms.md</files>
  <action>
1. Fill **Part 2 — Classification Matrix**: one row per affected WPForms item (top-level + every submenu
   from the Task 1 inventory). Classify Rename / Reorder / Hide (per role) / Re-icon safe/degraded/broken
   with observable evidence. Apply ALL conventions: per-cell persistence + timing cause; top-level Reorder
   from effective rendered order (reorder-probe); Hide per-role two-gate model ({Maestro cosmetic
   `unset()`} + {WP cap gate}) with loads-vs-403 and "moot no-op" where WP already gates a role away,
   evidence from the separate rendered check; submenu Re-icon = N/A; `[state]` on feature-gated cells;
   recurring evidence stated once as F1…Fn and referenced per cell.
2. Add the **Interaction Scenarios** section — apply S1 (hide-parent-with-visible-children), S2
   (rename+reorder), S3 (re-icon+reorder-across-separator) IF WPForms has a parent/child menu or separator
   (recommended); explicitly mark any N/A probe. Reset config after each; classify with evidence +
   persistence + timing cause.
3. Fill **Part 3 — Classified-Fix List**: every surfaced issue → exactly ONE R1 fix category with a
   one-line rationale; no orphans; cross-reference shared findings as SURV-01 does.
4. Add the **Success-Criterion Traceability** table (sections → 4 Phase-15 criteria + SURV-05) and tick the
   **Survey Completion Check** boxes. Set Status to Complete. Do NOT edit SCHEMA.md.
  </action>
  <verify>
    <automated>grep -q "Part 2 — Classification Matrix" .planning/compat/SURV-05-wpforms.md && grep -q "Part 3 — Classified-Fix List" .planning/compat/SURV-05-wpforms.md && grep -q "Success-Criterion Traceability\|Success-criterion traceability" .planning/compat/SURV-05-wpforms.md && grep -q "Survey Completion Check" .planning/compat/SURV-05-wpforms.md && grep -Eqi "safe|degraded|broken" .planning/compat/SURV-05-wpforms.md && grep -q "SURV-05" .planning/compat/SURV-05-wpforms.md && echo OK</automated>
  </verify>
  <done>SURV-05-wpforms.md is a complete, schema-faithful survey: every affected item has a matrix row with all four ops classified + evidence + persistence; Interaction Scenarios applied or explicitly N/A; every issue carries exactly one classified fix with no orphans; traceability maps to the 4 criteria + SURV-05; all completion-check boxes satisfiable. SCHEMA.md unmodified.</done>
</task>

</tasks>

<verification>
- `.planning/compat/SURV-05-wpforms.md` exists and structurally mirrors SURV-01.
- Every affected WPForms item has a matrix row; every op cell classified safe/degraded/broken with evidence + persistence.
- First-run / Lite-vs-Pro items tagged `[state]`.
- Every surfaced issue carries exactly one classified fix; no orphans.
- `.planning/compat/SCHEMA.md` byte-for-byte unchanged.
- No production menu-handling code committed (R1 boundary).
</verification>

<success_criteria>
- SURV-05-wpforms.md committed, filling the SCHEMA template identically to SURV-01 (criteria 1 & 4).
- Every Maestro op against each WPForms menu item classified with evidence (criterion 2).
- Every identified issue carries a classified fix (criterion 3).
- Completion Check satisfiable; traceability maps sections → 4 criteria + SURV-05.
</success_criteria>

<output>
After completion, create `.planning/phases/15-remaining-survey-set/15-04-SUMMARY.md`.
</output>
