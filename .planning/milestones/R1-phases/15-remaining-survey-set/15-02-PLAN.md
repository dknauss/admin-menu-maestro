---
phase: 15-remaining-survey-set
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/compat/SURV-03-yoast-seo.md
  - .planning/compat/SURV-03-assets/
autonomous: true
requirements: [SURV-03]
must_haves:
  truths:
    - "A committed SURV-03-yoast-seo.md exists that fills the final SCHEMA.md template structurally identically to SURV-01"
    - "Every affected Yoast SEO top-level and submenu item has a Part 2 matrix row with rename/reorder/hide/re-icon each classified safe/degraded/broken plus observable evidence"
    - "Every surfaced issue carries exactly one R1 classified fix"
    - "Rank Math is explicitly noted as out-of-scope/deferred (Yoast is the locked SEO choice; Rank Math not loaded in the harness)"
    - "The SCHEMA.md Survey Completion Check boxes are all satisfiable and a success-criterion traceability note maps sections to the 4 Phase-15 criteria + SURV-03"
  artifacts:
    - path: ".planning/compat/SURV-03-yoast-seo.md"
      provides: "Filled Yoast SEO compatibility survey (Parts 1-3 + Method header + Interaction Scenarios + traceability + completion check)"
      contains: "Part 1 — Manipulation-Dimensions Checklist"
    - path: ".planning/compat/SURV-03-assets/"
      provides: "Per-role natural-state baseline dumps + reused dump/probe scripts (planner-discretion: inline if volume is small)"
  key_links:
    - from: ".planning/compat/SURV-03-yoast-seo.md"
      to: ".planning/compat/SCHEMA.md"
      via: "structural copy of the final template (all 7 Phase-14 conventions applied)"
      pattern: "Survey Completion Check"
    - from: ".planning/compat/SURV-03-yoast-seo.md"
      to: "includes/class-replay.php"
      via: "classifies Maestro replay ops against Yoast's menu"
      pattern: "PHP_INT_MAX"
---

<objective>
Survey **Yoast SEO** (`wordpress-seo` 27.9) for Admin Menu Maestro R1 compatibility (requirement SURV-03), producing `.planning/compat/SURV-03-yoast-seo.md` as a filled copy of the finalized `SCHEMA.md`, structured identically to the SURV-01 exemplar so Phase 16 synthesis is mechanical.

Purpose: Document HOW Yoast registers/manipulates the admin menu, classify every Maestro operation (rename / reorder / hide / re-icon) against each affected Yoast menu item as safe / degraded / broken with observable runtime evidence, and assign every surfaced issue exactly one classified R1 fix.

**Yoast is the LOCKED SEO choice** over Rank Math — `tests/compat/VERSIONS.md` records "Rank Math is not loaded." The survey covers Yoast ONLY; note Rank Math as out-of-scope/deferred in the survey body (do not attempt to survey an absent plugin).

Output: `.planning/compat/SURV-03-yoast-seo.md` (+ optional `.planning/compat/SURV-03-assets/`).

**R1 boundary — research only.** Fixes are CLASSIFIED, never implemented. No production menu-handling code is committed.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# THE methodology source (locked, inherited unchanged):
@.planning/phases/14-woocommerce-survey/14-CONTEXT.md

# THE final template to copy and fill (apply all 7 Phase-14 conventions per its changelog):
@.planning/compat/SCHEMA.md

# THE worked exemplar — mirror its structure EXACTLY. Do NOT re-survey WooCommerce:
@.planning/compat/SURV-01-woocommerce.md

# Reusable, plugin-agnostic survey assets:
@.planning/compat/SURV-01-assets/dump-menu.php
@.planning/compat/SURV-01-assets/reorder-probe.php

# Pinned-version ledger (cite Yoast's pinned version + slug; note Rank Math not loaded):
@tests/compat/VERSIONS.md

# The survey's SUBJECT — the replay engine being classified:
@includes/class-replay.php
@includes/class-ordering.php
@includes/class-config.php

<harness>
Committed Phase 13 harness (`tests/compat/.wp-env.json`) ALREADY loads `wordpress-seo` 27.9 + the
provisioned roles. NO harness changes needed. Boot/observe only:
`npm run compat:start` → http://localhost:8890 ; `npm run compat:stop`. Docker required; cold boot ~15 min.
Confirm `wordpress-seo` active and Rank Math ABSENT: `cd tests/compat && npx wp-env run cli wp plugin list`.
</harness>

<method_inheritance>
Method is LOCKED by 14-CONTEXT.md and demonstrated in SURV-01's Method header. Reuse VERBATIM, adapting
only plugin name/slug:
- **Hybrid source + runtime dump.** Read Yoast's `admin_menu` registration to explain HOW, confirm WHAT
  via WP-CLI `$menu`/`$submenu` dumps.
- **`dump-menu.php`** (copy to SURV-03-assets, adapt header): hooks `admin_menu @ PHP_INT_MAX`, prints
  `pos⇥slug⇥title⇥icon⇥css` / `pos⇥slug⇥title⇥cap`, exits before WP cap filtering. Run per role with
  `--user=` and `--exec="define('WP_ADMIN', true);"`; confirm/document whether Yoast's menu needs WP_ADMIN.
- **Dumps are REPLAY STATE, not the rendered sidebar** (SCHEMA convention 7): per-role Hide evidence MUST
  come from a separate rendered/post-cap-filter check, never the raw dump.
- **Top-level Reorder from EFFECTIVE rendered order** via `reorder-probe.php`, not the raw global.
- **Config-driven op application:** write `maestro_config`, re-dump, compare, `wp option delete
  maestro_config` between cases. Spot-check notable items in the editor UI at discretion.
- **Per-role:** admin + compat_editor + compat_shop_manager (all provisioned). Yoast ships no special menu
  role of its own; confirm at runtime. Note: Yoast exposes capability/role controls of its own (e.g. who
  may manage Yoast settings) — observe how those caps intersect the per-role render gate.

  ```bash
  cd tests/compat
  npx wp-env run cli -- php -d memory_limit=512M /usr/local/bin/wp \
    --exec="define('WP_ADMIN', true);" \
    eval-file wp-content/plugins/maestro-menu-editor/.planning/compat/SURV-03-assets/dump-menu.php \
    --user=admin            # repeat for compat_editor / compat_shop_manager
  npx wp-env run cli wp option delete maestro_config
  npx wp-env run cli -- wp option update maestro_config '<json>' --format=json
  ```
</method_inheritance>

<yoast_specifics>
- **Rank Math is OUT OF SCOPE / deferred** (locked: Yoast is the SEO choice; Rank Math not loaded). State
  this explicitly near the top of the survey body and in the traceability note; do not attempt to observe it.
- Yoast registers a top-level **SEO** menu (`wpseo_dashboard`/`Yoast SEO` with its own icon) plus submenus
  (e.g. General/Dashboard, Search Appearance, Social, Tools, Settings/Premium upsell). Determine the exact
  affected set at runtime from the natural-state dump — do not assume from memory.
- Setup state is LIGHT (Yoast has a first-time configuration but no hard external gate). Note any
  first-run/feature-gated items with `[state]`.
- Watch for: any notification/count badge baked into a title (convention 3 → degraded on rename if present);
  any entity-encoded (`&amp;`) slug (flag for a slug-resolution tweak); premium-upsell submenu items that
  may be present only in the free build.
</yoast_specifics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot harness, copy template, write Part 1 + Method header + per-role natural-state baselines</name>
  <files>.planning/compat/SURV-03-yoast-seo.md, .planning/compat/SURV-03-assets/dump-menu.php, .planning/compat/SURV-03-assets/reorder-probe.php, .planning/compat/SURV-03-assets/baseline-*.txt</files>
  <action>
1. Boot the compat harness (`npm run compat:start`; Docker required — surface and pause if blocked).
   Confirm `wordpress-seo` 27.9 active, Rank Math ABSENT, and the three users provisioned.
2. Copy `SCHEMA.md` to `.planning/compat/SURV-03-yoast-seo.md`; fill FRONT FIELDS (Plugin: Yoast SEO;
   Slug: `wordpress-seo`; Pinned version: `27.9`; Date; Surveyor). Add a short note that Rank Math is
   out-of-scope/deferred (Yoast is the locked SEO choice; Rank Math not loaded).
3. Create `.planning/compat/SURV-03-assets/`; copy `dump-menu.php` + `reorder-probe.php`, adapting only
   header comments to Yoast. Confirm/document whether Yoast's menu requires the `WP_ADMIN` define.
4. Write the **Method / how evidence was gathered** header mirroring SURV-01's (harness boot, per-role
   dump command, replay-state-not-rendered-sidebar warning, top-level-reorder effective-order exception,
   per-role two-gate observation, classification rubric, per-plan traceability table).
5. Capture per-role natural-state (`maestro_config` deleted) baselines for the three roles →
   `SURV-03-assets/baseline-<role>.txt`. Tag any first-run/feature-gated item `[state]`. Inline small
   dumps at discretion if volume is low.
6. Fill **Part 1 — Manipulation-Dimensions Checklist**: check each of the six locked dimensions Yoast
   exhibits with `Notes:` (source citation under `wp-content/plugins/wordpress-seo/` + runtime confirmation).
   Apply convention 3 + the entity-encoded-slug note where applicable. Add a baseline "revealing slices"
   sub-section and an affected-items inventory that seeds the Task 2 matrix.
  </action>
  <verify>
    <automated>test -f .planning/compat/SURV-03-yoast-seo.md && grep -q "Part 1 — Manipulation-Dimensions Checklist" .planning/compat/SURV-03-yoast-seo.md && grep -q "Method" .planning/compat/SURV-03-yoast-seo.md && grep -q "wordpress-seo" .planning/compat/SURV-03-yoast-seo.md && grep -q "27.9" .planning/compat/SURV-03-yoast-seo.md && grep -qi "rank math" .planning/compat/SURV-03-yoast-seo.md && echo OK</automated>
  </verify>
  <done>SURV-03-yoast-seo.md exists with filled front fields, a Rank-Math-out-of-scope note, a Method header reproducing the locked procedure, per-role natural-state baselines captured, and Part 1's six-dimension checklist filled with source + runtime evidence and an affected-items inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Write Part 2 matrix + Interaction Scenarios + Part 3 classified fixes + traceability + completion check</name>
  <files>.planning/compat/SURV-03-yoast-seo.md</files>
  <action>
1. Fill **Part 2 — Classification Matrix**: one row per affected Yoast item (every top-level + every
   submenu from the Task 1 inventory). Classify Rename / Reorder / Hide (per role) / Re-icon
   safe/degraded/broken with observable evidence. Apply ALL conventions: per-cell persistence + timing
   cause; top-level Reorder from effective rendered order (reorder-probe); Hide per-role two-gate model
   ({Maestro cosmetic `unset()`} + {WP cap gate}) with loads-vs-403 and "moot no-op" where WP already
   gates a role away, evidence from the separate rendered check; submenu Re-icon = N/A; `[state]` on
   feature-gated cells; recurring evidence stated once as F1…Fn and referenced per cell.
2. Add the **Interaction Scenarios** section — apply the three canonical probes (S1 hide-parent-with-
   visible-children, S2 rename+reorder, S3 re-icon+reorder-across-separator) IF Yoast has a parent/child
   menu or separator (recommended); explicitly mark any N/A probe rather than omitting. Reset config after
   each; classify with evidence + persistence + timing cause.
3. Fill **Part 3 — Classified-Fix List**: every surfaced issue → exactly ONE R1 fix category with a
   one-line rationale; no orphans; cross-reference shared findings as SURV-01 does.
4. Add the **Success-Criterion Traceability** table (sections → 4 Phase-15 criteria + SURV-03) and tick the
   **Survey Completion Check** boxes. Set Status to Complete. Do NOT edit SCHEMA.md.
  </action>
  <verify>
    <automated>grep -q "Part 2 — Classification Matrix" .planning/compat/SURV-03-yoast-seo.md && grep -q "Part 3 — Classified-Fix List" .planning/compat/SURV-03-yoast-seo.md && grep -q "Success-Criterion Traceability\|Success-criterion traceability" .planning/compat/SURV-03-yoast-seo.md && grep -q "Survey Completion Check" .planning/compat/SURV-03-yoast-seo.md && grep -Eqi "safe|degraded|broken" .planning/compat/SURV-03-yoast-seo.md && grep -q "SURV-03" .planning/compat/SURV-03-yoast-seo.md && echo OK</automated>
  </verify>
  <done>SURV-03-yoast-seo.md is a complete, schema-faithful survey: every affected item has a matrix row with all four ops classified + evidence + persistence; Interaction Scenarios applied or explicitly N/A; every issue carries exactly one classified fix with no orphans; traceability maps to the 4 criteria + SURV-03; all completion-check boxes satisfiable. SCHEMA.md unmodified.</done>
</task>

</tasks>

<verification>
- `.planning/compat/SURV-03-yoast-seo.md` exists and structurally mirrors SURV-01.
- Every affected Yoast item has a matrix row; every op cell classified safe/degraded/broken with evidence + persistence.
- Rank Math explicitly noted out-of-scope/deferred.
- Every surfaced issue carries exactly one classified fix; no orphans.
- `.planning/compat/SCHEMA.md` byte-for-byte unchanged.
- No production menu-handling code committed (R1 boundary).
</verification>

<success_criteria>
- SURV-03-yoast-seo.md committed, filling the SCHEMA template identically to SURV-01 (criteria 1 & 4).
- Every Maestro op against each Yoast menu item classified with evidence (criterion 2).
- Every identified issue carries a classified fix (criterion 3).
- Completion Check satisfiable; traceability maps sections → 4 criteria + SURV-03.
</success_criteria>

<output>
After completion, create `.planning/phases/15-remaining-survey-set/15-02-SUMMARY.md`.
</output>
