---
phase: 16-synthesis
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - .planning/compat/BACKLOG.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DELV-02]
must_haves:
  truths:
    - "A committed .planning/compat/BACKLOG.md lists every actionable issue surfaced across SURV-01 through SURV-06, ranked by severity/frequency"
    - "Each backlog item carries a stable forward ID (COMPAT-01, COMPAT-02, ...) assigned in rank order and NOT renumbered, ready to be cited by a later versioned milestone"
    - "Each backlog item is classified as exactly one of the four R1 fix categories: slug-resolution tweak / later admin_menu re-hook / special-casing / documented limitation"
    - "No orphaned findings — every Part 3 issue (I1..In) from all six surveys maps to exactly one COMPAT-xx item (deduplicated across plugins) OR is explicitly marked a documented limitation, and a traceability table proves the SURV-NN-Ix -> COMPAT-xx mapping with no gaps"
    - "REQUIREMENTS.md FIX-xx forward placeholder is linked to the COMPAT-xx scheme so the IDs are stated as the seed for later versioned-milestone fixes without renaming"
  artifacts:
    - path: ".planning/compat/BACKLOG.md"
      provides: "DELV-02 ranked, classified fix/limitation backlog with stable COMPAT-xx forward IDs + full SURV-NN-Ix -> COMPAT-xx traceability"
      contains: "COMPAT-01"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      provides: "FIX-xx forward requirement linked to the COMPAT-xx backlog as its seed"
      contains: "COMPAT-"
  key_links:
    - from: ".planning/compat/BACKLOG.md"
      to: ".planning/compat/COMPATIBILITY-NOTE.md"
      via: "ranks the cross-plugin patterns the note surfaced into COMPAT-xx items"
      pattern: "COMPATIBILITY-NOTE"
    - from: ".planning/compat/BACKLOG.md"
      to: ".planning/compat/SURV-01-woocommerce.md"
      via: "traceability table maps each survey Part 3 issue (SURV-NN Ix) to a COMPAT-xx item"
      pattern: "SURV-01"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/compat/BACKLOG.md"
      via: "FIX-xx forward requirement names COMPAT-xx as its seed"
      pattern: "COMPAT-"
---

<objective>
Produce the committed DELV-02 deliverable: `.planning/compat/BACKLOG.md`, a prioritized fix/limitation backlog that ranks every issue surfaced across SURV-01..06, classifies each as one of the four R1 fix categories, and assigns each a stable forward `COMPAT-xx` ID ready to be cited (un-renumbered) by a later versioned milestone. Link the existing `FIX-xx` placeholder in REQUIREMENTS.md to the COMPAT-xx scheme so the seed relationship is explicit.

Purpose: The six surveys each ended with a Part 3 classified-fix list (`I1..In`). Many of those issues are the same root cause appearing across plugins (badge-in-title loss, absolute-URL / entity-encoded slugs, cosmetic non-cascading hide, submenu re-icon no-op, render-time reorder filter override). DELV-02 deduplicates these into a single ranked backlog with forward IDs, with a complete traceability table proving no survey finding is orphaned.

Output: `.planning/compat/BACKLOG.md` (DELV-02) + a FIX-xx linkage edit in `.planning/REQUIREMENTS.md`.

**R1 boundary — research/documentation only.** This backlog CLASSIFIES and RANKS fixes; it does NOT implement any. No production menu-handling code is committed. The COMPAT-xx IDs are forward references for a future versioned milestone.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# The classification vocabulary + the four R1 fix categories the backlog must use:
@.planning/compat/SCHEMA.md

# Plan 16-01's output — the consolidated note whose cross-plugin patterns this backlog ranks (read its operation-affected-per-plugin cross-cut):
@.planning/compat/COMPATIBILITY-NOTE.md

# The six surveys' Part 3 classified-fix lists are the authoritative issue inventory to deduplicate and rank:
@.planning/compat/SURV-01-woocommerce.md
@.planning/compat/SURV-02-jetpack.md
@.planning/compat/SURV-03-yoast-seo.md
@.planning/compat/SURV-04-elementor.md
@.planning/compat/SURV-05-wpforms.md
@.planning/compat/SURV-06-lifterlms.md
</context>

<issue_inventory>
<!-- Pre-extracted complete Part 3 inventory across all six surveys, for deduplication. The
     executor MUST confirm each against the source survey's Part 3 table; the survey is authority. -->

**SURV-01 WooCommerce:** I1 badge-in-title lost on rename (documented limitation); I2
`separator-woocommerce` re-cluster on reorder via WC `menu_order` (documented limitation); I3
entity-encoded Products-taxonomy slugs (slug-resolution tweak); I7 shared-slug collision
(Products top-level vs All Products submenu) (documented limitation, possible slug-resolution);
I4 submenu re-icon no-op (documented limitation); I5 cosmetic per-role hide / page LOADS
(documented limitation); I6 parent-hide non-cascading (documented limitation).

**SURV-02 Jetpack:** I1 submenu re-icon no-op (documented limitation; = SURV-01 I4); I2 Settings
absolute-URL slug, env-specific hostname (slug-resolution tweak — flagged highest priority in
SURV-02); I3 cosmetic hide (documented limitation; = SURV-01 I5); I4 parent-hide non-cascading
(documented limitation; = SURV-01 I6); I5 hide moot for editor/shop_manager (cap-gated)
(documented limitation).

**SURV-03 Yoast:** I1 dual-slug role-conditional registration (documented limitation, Yoast-specific);
I2 shared-slug `wpseo_dashboard` parent/General submenu (documented limitation); I3 shared-slug
`wpseo_page_academy` (documented limitation); I4 notification-badge-in-title lost on rename
(documented limitation; = SURV-01 I1 family); I5 upsell-HTML-in-submenu-title lost on rename
(documented limitation; = badge/HTML family); I6 submenu re-icon no-op (documented limitation);
I7 cosmetic hide (documented limitation); I8 parent-hide non-cascading (documented limitation).

**SURV-04 Elementor:** I1 Website Templates absolute-URL slug + `ver=` version param (slug-resolution
tweak; = SURV-02 I2 family, compounded by version); I2 Categories entity-encoded slug
(slug-resolution tweak; = SURV-01 I3); I3 Upgrade dynamic upsell-HTML lost on rename (documented
limitation; = badge/HTML family); I4 submenu re-icon no-op (documented limitation); I5 cosmetic
hide (documented limitation); I6 parent-hide non-cascading (documented limitation); I7 elementor +
elementor_library top-levels CSS-hidden at admin_head — ops land but sidebar masked (documented
limitation, Elementor-specific observational); I8 hide moot for capped non-admin roles (documented
limitation; = SURV-02 I5 family).

**SURV-05 WPForms:** I1 submenu re-icon no-op (documented limitation); I2 NEW! badge lost on rename
(documented limitation; badge family); I3 Addons color-span lost on rename (documented limitation;
badge/HTML family); I4 Upgrade-to-Pro absolute external URL + UTM params (slug-resolution tweak;
slug family — note: stable across installs, lower priority than Jetpack I2); I5 cosmetic hide
(documented limitation); I6 hide moot for non-admin (documented limitation); I7 parent-hide
non-cascading (documented limitation).

**SURV-06 LifterLMS:** I1 `llms-separator` de-grouping on reorder — no menu_order re-cluster
(documented limitation; separator family with SURV-01 I2); I2 submenu reorder overridden by
`submenu_order()` via custom_menu_order at render time (documented limitation, render-time-filter
family, LifterLMS-specific); I3 entity-encoded taxonomy slugs (slug-resolution tweak; = SURV-01 I3);
I4 shared-slug CPT collision (documented limitation; = SURV-01 I7); I5 submenu re-icon no-op
(documented limitation); I6 cosmetic hide (documented limitation); I7 parent-hide non-cascading
(documented limitation).

**Suggested deduplicated COMPAT-xx groupings (executor confirms + finalizes rank order):**
The recurring cross-plugin root-cause families are the natural COMPAT-xx items:
- Slug-resolution: (a) absolute-URL slugs [Jetpack I2, Elementor I1, WPForms I4]; (b) entity-encoded
  `&amp;` slugs [WooCommerce I3, Elementor I2, LifterLMS I3]; (c) shared-slug parent/first-submenu
  collisions [WooCommerce I7, Yoast I2/I3, LifterLMS I4]. These are the only `slug-resolution tweak`
  category items — the highest-frequency ACTIONABLE class, so they rank at the top.
- Render-time reorder override [WooCommerce I2/separator, LifterLMS I1/separator + I2/submenu_order]:
  documented limitation (a later admin_menu re-hook does NOT help — collision is at render-time
  menu_order/custom_menu_order).
- Badge/HTML-in-title loss on rename [WooCommerce I1, Yoast I4/I5, Elementor I3, WPForms I2/I3]:
  documented limitation (candidate special-casing in a later milestone if badge-preservation is
  prioritized — note this forward).
- Submenu re-icon no-op [all six]: documented limitation.
- Cosmetic non-cascading per-role hide / moot-when-cap-gated [all six]: documented limitation
  (core "zero risk to access" semantic).
- Dual-slug role-conditional registration [Yoast I1]: documented limitation, plugin-specific.
- CSS-hidden top-levels observational mask [Elementor I7]: documented limitation, plugin-specific.

**Ranking guidance (severity/frequency):** 0 broken anywhere, so severity is bounded at degraded.
Rank by (1) is it ACTIONABLE in a later milestone (slug-resolution tweaks and the special-casing
candidate outrank pure documented limitations), then (2) cross-plugin frequency, then (3)
user-visible impact. The executor decides the final order and locks the COMPAT-xx numbering to it.
</issue_inventory>

<tasks>

<task type="auto">
  <name>Task 1: Build the ranked, classified COMPAT-xx backlog with full survey traceability</name>
  <files>.planning/compat/BACKLOG.md</files>
  <action>
Create `.planning/compat/BACKLOG.md` (DELV-02). Deduplicate the six surveys' Part 3 issue lists into a single ranked backlog. Structure:

1. **Header (prose):** Title; one-paragraph purpose (DELV-02: ranked, classified fix/limitation backlog seeding a later versioned milestone; R1 research-only — no fixes implemented); link to `COMPATIBILITY-NOTE.md` (DELV-01) and `SCHEMA.md`. State the headline: 0 broken cells across all six surveys, so the backlog is degraded-or-limitation only; severity is bounded at degraded and ranking is driven primarily by actionability + cross-plugin frequency.

2. **ID-stability contract (call-out):** State explicitly that COMPAT-xx IDs are assigned once, in rank order, and MUST NOT be renumbered — they are forward references a later versioned milestone (FIX-xx) will cite. Adding a future item appends a new highest number; it never reorders existing IDs.

3. **Ranked backlog table** — the core DELV-02 artifact. One row per deduplicated issue, in rank order, each with:
   - `COMPAT-xx` ID (assigned sequentially in rank order — COMPAT-01 = highest priority).
   - Issue summary (the cross-plugin root cause, not a single plugin's phrasing).
   - Affected plugins (list every survey that surfaced it).
   - Affected Maestro operation(s) (rename / reorder / hide / re-icon).
   - R1 classification — exactly one of: `slug-resolution tweak` / `later admin_menu re-hook` / `special-casing` / `documented limitation` (use the exact SCHEMA.md category wording).
   - Frequency (how many of the six plugins exhibit it).
   - Rank rationale (one line: why this severity/frequency position).
   Use the deduplicated groupings in `<issue_inventory>` as the starting set, but CONFIRM each grouping against the source surveys' Part 3 tables and finalize the rank order yourself. Rank actionable `slug-resolution tweak` items at the top (highest-frequency actionable class), then the render-time-override and badge-loss families, then the universal documented-limitation semantics (re-icon no-op, cosmetic hide, parent-hide non-cascading), then plugin-specific limitations (Yoast dual-slug, Elementor CSS-hidden). For any item whose category is `documented limitation` but which the surveys flagged as a *candidate* for special-casing in a later milestone (badge preservation, optional subtree-hide, level-qualified slug match), note that forward candidacy in the rank rationale WITHOUT changing the R1 classification.

4. **Per-item detail (optional, short):** For the actionable (non-pure-limitation) items, 2–4 lines on the proposed fix direction (e.g. for slug-resolution: normalize absolute-URL/entity-encoded/shared-slug matching in Maestro's resolve path), so a future milestone can scope it. Keep limitations to their one-line table row.

5. **Traceability table (no-orphans proof)** — a second table mapping **every** survey Part 3 issue to its COMPAT-xx item: columns `Survey`, `Issue (SURV-NN Ix)`, `COMPAT-xx`, `Category`. Include a row for each of: SURV-01 I1–I7, SURV-02 I1–I5, SURV-03 I1–I8, SURV-04 I1–I8, SURV-05 I1–I7, SURV-06 I1–I7. Every one of these MUST map to exactly one COMPAT-xx (deduplicated issues map multiple SURV rows to the same COMPAT id). No survey issue may be unmapped. End with a one-line coverage assertion (total survey issues N → all mapped, 0 orphans).

This is a documentation deliverable — prose and tables only. Do NOT modify any SURV-NN file, SCHEMA.md, or COMPATIBILITY-NOTE.md.
  </action>
  <verify>
    <automated>test -f .planning/compat/BACKLOG.md && grep -qE "COMPAT-01" .planning/compat/BACKLOG.md && grep -qE "COMPAT-0[2-9]" .planning/compat/BACKLOG.md && for s in SURV-01 SURV-02 SURV-03 SURV-04 SURV-05 SURV-06; do grep -q "$s" .planning/compat/BACKLOG.md || { echo "MISSING traceability for $s"; exit 1; }; done && grep -qiE "slug-resolution" .planning/compat/BACKLOG.md && grep -qiE "documented limitation" .planning/compat/BACKLOG.md && grep -qiE "not be renumbered|never reorder|do not renumber|stable" .planning/compat/BACKLOG.md && echo OK</automated>
  </verify>
  <done>
BACKLOG.md exists with: a ranked table of deduplicated issues each carrying a sequential COMPAT-xx ID, affected plugins, affected operation(s), exactly one of the four R1 fix categories, and a rank rationale; an ID-stability contract; and a traceability table mapping every SURV-01..06 Part 3 issue (I1..In for all six) to exactly one COMPAT-xx with no orphans.
  </done>
</task>

<task type="auto">
  <name>Task 2: Link FIX-xx in REQUIREMENTS.md to the COMPAT-xx seed + DELV-02 no-orphan verification</name>
  <files>.planning/REQUIREMENTS.md, .planning/compat/BACKLOG.md</files>
  <action>
1. **REQUIREMENTS.md FIX-xx linkage:** In `.planning/REQUIREMENTS.md`, locate the "Future Requirements → Compatibility Fixes → FIX-xx" bullet (currently: "The actual production menu-handling fixes surfaced and ranked by DELV-02 ..."). Edit it to explicitly name the COMPAT-xx scheme as its seed — e.g. state that FIX-xx items will be scoped from the ranked `COMPAT-xx` backlog in `.planning/compat/BACKLOG.md`, citing COMPAT IDs without renumbering them, and that the slug-resolution-tweak COMPAT items are the highest-priority FIX candidates. Use a markdown link to `BACKLOG.md` (DOC-01 link-hygiene convention — no bare path in prose). Do NOT change any requirement's checkbox state and do NOT mark DELV-02 complete here (execute-phase / verification flips DELV-01/DELV-02). Make ONLY the FIX-xx linkage edit — this is a surgical addition, not a rewrite.

2. **No-orphan verification pass (in BACKLOG.md):** Re-read each of the six surveys' Part 3 tables and confirm the BACKLOG.md traceability table accounts for every `Ix` row — none missing, none mapped to two COMPAT ids. Confirm every COMPAT-xx row carries exactly one of the four SCHEMA.md fix-category strings (no item left unclassified, none with two categories). Confirm the COMPAT-xx IDs are sequential from COMPAT-01 with no gaps and appear in rank order. If any survey issue is unmapped, add it to the traceability table and (if it is a genuinely new root cause) append a new highest-numbered COMPAT item rather than reordering existing IDs. Update the coverage assertion line with the final issue count.
  </action>
  <verify>
    <automated>grep -qiE "COMPAT-" .planning/REQUIREMENTS.md && grep -qiE "FIX-" .planning/REQUIREMENTS.md && grep -qiE "BACKLOG" .planning/REQUIREMENTS.md && grep -qiE "0 orphan|no orphan|all mapped|orphans" .planning/compat/BACKLOG.md && echo OK</automated>
  </verify>
  <done>
REQUIREMENTS.md FIX-xx forward requirement now names the COMPAT-xx backlog (linked to BACKLOG.md) as its seed without altering any checkbox state; and BACKLOG.md's traceability table is verified to map every SURV-01..06 Part 3 issue to exactly one sequential COMPAT-xx with a single fix-category each and an explicit 0-orphans coverage assertion.
  </done>
</task>

</tasks>

<verification>
Goal-backward checks for DELV-02 (ROADMAP Phase 16 success criteria 2, 3, 4):
- BACKLOG.md ranks every surfaced issue by severity/frequency, each classified as exactly one of the four R1 fix categories.
- Every backlog item carries a stable forward COMPAT-xx ID, assigned in rank order, with an explicit no-renumbering contract.
- No orphaned findings: the traceability table maps every SURV-01..06 Part 3 issue (I1..In) to exactly one COMPAT-xx item; deduplicated cross-plugin issues collapse multiple survey rows onto one COMPAT id; the 0-orphans coverage assertion is present and true.
- REQUIREMENTS.md FIX-xx links the COMPAT-xx scheme as its forward seed, no requirement checkbox state changed.
</verification>

<success_criteria>
- `.planning/compat/BACKLOG.md` is committed: a ranked, classified backlog with stable COMPAT-xx forward IDs and complete SURV-NN-Ix -> COMPAT-xx traceability with no orphans.
- REQUIREMENTS.md FIX-xx is linked to the COMPAT-xx seed without renaming/renumbering.
- The backlog is ready to be cited and scoped by a later versioned milestone exactly as written.
</success_criteria>

<output>
After completion, create `.planning/phases/16-synthesis/16-02-SUMMARY.md` per the summary template, recording the final COMPAT-xx list with rank order + category, the total survey-issue count proven orphan-free, and the FIX-xx linkage made in REQUIREMENTS.md.
</output>
