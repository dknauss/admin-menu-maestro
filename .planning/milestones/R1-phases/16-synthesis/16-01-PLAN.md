---
phase: 16-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/compat/COMPATIBILITY-NOTE.md
autonomous: true
requirements: [DELV-01]
must_haves:
  truths:
    - "A committed .planning/compat/COMPATIBILITY-NOTE.md presents all six per-plugin findings (WooCommerce, Jetpack, Yoast SEO, Elementor, WPForms, LifterLMS) under the single SCHM-01 schema vocabulary (safe/degraded/broken; the four R1 fix categories)"
    - "A summary matrix shows, per plugin, the worst-case classification (safe/degraded/broken) for each of the four Maestro operations rename / reorder / hide / re-icon — all six plugins appear, every operation column is populated for every plugin (no empty cells)"
    - "The note records the headline cross-plugin finding that 0 broken cells were observed across all six surveys, and that the entire surfaced backlog is degraded-or-limitation only"
    - "Each per-plugin section links to its source SURV-NN file and names that plugin's distinguishing manipulation behavior (e.g. WooCommerce's menu_order separator re-cluster, Jetpack/Elementor/WPForms absolute-URL slugs, Yoast dual-slug role-conditional registration, LifterLMS submenu_order override)"
    - "The note is internally consistent with the six SURV-NN Part 2 matrices it summarizes — no classification in the summary contradicts the source survey it derives from"
  artifacts:
    - path: ".planning/compat/COMPATIBILITY-NOTE.md"
      provides: "Consolidated DELV-01 compatibility note: six per-plugin findings under one schema + summary safe/degraded/broken matrix (plugin x operation)"
      contains: "rename"
      min_lines: 80
  key_links:
    - from: ".planning/compat/COMPATIBILITY-NOTE.md"
      to: ".planning/compat/SURV-01-woocommerce.md"
      via: "summarizes SURV-01 Part 2 classifications + links to source"
      pattern: "SURV-01"
    - from: ".planning/compat/COMPATIBILITY-NOTE.md"
      to: ".planning/compat/SCHEMA.md"
      via: "uses the SCHM-01 classification vocabulary (safe/degraded/broken + four fix categories) for the summary matrix"
      pattern: "degraded"
---

<objective>
Synthesize the six completed per-plugin compatibility surveys (SURV-01 through SURV-06) into a single committed authoritative compatibility note, `.planning/compat/COMPATIBILITY-NOTE.md`, satisfying requirement DELV-01.

Purpose: Phases 14–15 produced six independently-authored surveys that all fill the same finalized SCHM-01 template. This plan merges them into one document a future versioned milestone can read at a glance: a per-plugin findings section plus a single summary safe/degraded/broken matrix (rows = the six plugins, columns = the four Maestro operations rename / reorder / hide / re-icon), with the headline cross-plugin finding (0 broken cells observed anywhere).

Output: `.planning/compat/COMPATIBILITY-NOTE.md` (DELV-01).

**R1 boundary — research/documentation only.** This is a mechanical synthesis of existing committed artifacts. No production menu-handling code is committed. No fixes are implemented. The note characterizes and summarizes; the ranked backlog with forward IDs is a separate deliverable (DELV-02, Plan 16-02).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# The canonical schema vocabulary the note must use (safe/degraded/broken + the four R1 fix categories + the manipulation dimensions):
@.planning/compat/SCHEMA.md

# The six source surveys this note consolidates. Read each survey's Part 2 (Classification Matrix, including the F-numbered cross-cutting findings) and the "Net for Part 3" / matrix-summary lines to derive the per-operation worst-case classification per plugin:
@.planning/compat/SURV-01-woocommerce.md
@.planning/compat/SURV-02-jetpack.md
@.planning/compat/SURV-03-yoast-seo.md
@.planning/compat/SURV-04-elementor.md
@.planning/compat/SURV-05-wpforms.md
@.planning/compat/SURV-06-lifterlms.md
</context>

<synthesis_inputs>
<!-- Pre-extracted so the executor does not re-derive. Each survey's own Part 2/Part 3 text is the authority; verify against it. -->

**Universal cross-survey finding:** All six surveys report **0 broken cells**. Every surfaced
issue is `degraded` (cosmetic / recoverable) or a `documented limitation` / `slug-resolution tweak`.
This is the note's headline.

**Per-plugin, the worst-case classification per operation (derived from each survey's Part 2 matrix
+ cross-cutting findings; the executor MUST confirm each against the source survey):**

- **WooCommerce (SURV-01):** rename = degraded (badge-in-title loss F1); reorder = degraded
  (separator re-cluster via WC's own `menu_order` filter F4); hide = degraded (cosmetic per-role
  `unset()`, page LOADS F3); re-icon = safe top-level / N/A submenu (F2). Distinguishing trait:
  heaviest manipulator; its `menu_order` filter collides with Maestro's reorder.
- **Jetpack (SURV-02):** rename = safe top-level / safe submenu (no badges); reorder = safe (no
  filter conflict); hide = degraded (cosmetic, admin-only in disconnected state); re-icon = safe
  top-level / N/A submenu. Distinguishing trait: Settings submenu slug is an absolute URL
  (environment-specific hostname) → slug-resolution.
- **Yoast SEO (SURV-03):** rename = degraded (badge + upsell-HTML loss); reorder = (per Part 2);
  hide = degraded (cosmetic); re-icon = safe top-level / N/A submenu. Distinguishing trait:
  dual-slug role-conditional registration (`wpseo_dashboard` admin / `wpseo_page_academy`
  editor+shop_manager).
- **Elementor (SURV-04):** rename = degraded (dynamic upsell HTML loss); reorder = (per Part 2);
  hide = degraded (cosmetic); re-icon = safe top-level / N/A submenu. Distinguishing trait:
  registers three top-levels, two CSS-hidden at `admin_head`; Website Templates slug = absolute
  URL with `ver=` version param; Categories slug entity-encoded.
- **WPForms (SURV-05):** rename = degraded (NEW! badge + color-span loss); reorder = (per Part 2);
  hide = degraded (cosmetic, moot for non-admin — all `manage_options`); re-icon = safe top-level /
  N/A submenu. Distinguishing trait: Upgrade-to-Pro slug = absolute external URL with UTM params.
- **LifterLMS (SURV-06):** rename = degraded (entity-encoded taxonomy slugs); reorder = degraded
  (separator de-grouping F4 + submenu reorder overridden by `submenu_order()` F6); hide = degraded
  (cosmetic); re-icon = safe top-level / N/A submenu. Distinguishing trait: `submenu_order()` via
  `custom_menu_order` overrides Maestro sub_order; `llms-separator` does NOT re-cluster.

**Convention for the summary matrix:** each plugin x operation cell carries the **worst-case
classification across that plugin's affected items** for that operation, with re-icon noted as
"safe (top-level) / N/A (submenu)" so the matrix stays full-coverage and mechanical. State the 0-broken
finding explicitly so no cell is left ambiguous.
</synthesis_inputs>

<tasks>

<task type="auto">
  <name>Task 1: Build the per-plugin findings + summary matrix in COMPATIBILITY-NOTE.md</name>
  <files>.planning/compat/COMPATIBILITY-NOTE.md</files>
  <action>
Create `.planning/compat/COMPATIBILITY-NOTE.md` (DELV-01) — a single consolidated note presenting all six findings under the SCHM-01 schema. Structure:

1. **Header / front matter (prose):** Title; one-paragraph purpose ("consolidates SURV-01..06 into one DELV-01 note under the SCHM-01 schema; R1 research-only, no production code"); the date; a link to `SCHEMA.md` for the classification vocabulary and the four R1 fix categories. Link each plugin name to its source survey file (relative markdown links, e.g. `[WooCommerce](SURV-01-woocommerce.md)`), matching the project's DOC-01 link-hygiene convention (no bare paths in prose).

2. **Headline finding (call-out):** State plainly that across all six surveys **0 broken cells** were observed — Maestro's sparse-delta replay never broke any plugin's menu or access. Every surfaced issue is `degraded` (cosmetic/recoverable) or a documented limitation / slug-resolution tweak. Note Maestro's hide is a cosmetic per-role `unset()` that never strips a capability (page still LOADS by direct URL) — so "hide" is never an access-control break.

3. **Summary safe/degraded/broken matrix** — the core DELV-01 artifact. A markdown table:
   - Rows: the six plugins (WooCommerce, Jetpack, Yoast SEO, Elementor, WPForms, LifterLMS), each with its pinned version (copy from each survey's Front Fields) and a link to its SURV-NN file.
   - Columns: `Rename`, `Reorder`, `Hide`, `Re-icon`.
   - Each cell: the worst-case classification (`safe` / `degraded` / `broken`) for that plugin x operation, derived from that survey's Part 2 matrix + cross-cutting findings, with a 3–6 word evidence tag (e.g. "degraded — badge-in-title lost", "safe — no filter conflict", "degraded — separator re-cluster"). For Re-icon use "safe (top-level) / N/A (submenu)". **Every cell MUST be populated; no plugin row may be missing.** Use the pre-extracted worst-case classifications in `<synthesis_inputs>` but CONFIRM each against the cited survey's Part 2 before writing it — the source survey is the authority; if the survey text disagrees with the pre-extraction, follow the survey and note the correction.
   - Add a one-line legend below the table: safe/degraded/broken definitions (copy from SCHEMA.md) + the "0 broken observed" note + the re-icon N/A-on-submenu convention.

4. **Per-plugin findings sections** — one short subsection per plugin (six total), each containing: the plugin + pinned version; which of the six SCHM-01 manipulation dimensions it exhibits (one line, from that survey's Part 1); its distinguishing manipulation behavior (the "Distinguishing trait" in `<synthesis_inputs>`, confirmed against Part 1/Part 2); and a one-line summary of its operation classifications referencing the matrix row. Keep each section tight (≈5–10 lines) — this note summarizes; the SURV-NN files hold the full evidence and the executor should link to them rather than re-paste matrices.

Write prose and tables only — this is a documentation deliverable, not code. Do NOT modify any SURV-NN file or SCHEMA.md. Do NOT assign COMPAT-xx backlog IDs here — that is DELV-02 (Plan 16-02); this note may forward-reference that a ranked backlog follows.
  </action>
  <verify>
    <automated>test -f .planning/compat/COMPATIBILITY-NOTE.md && for p in woocommerce jetpack yoast elementor wpforms lifterlms; do grep -qi "$p" .planning/compat/COMPATIBILITY-NOTE.md || { echo "MISSING plugin: $p"; exit 1; }; done && for op in -i rename reorder hide re-icon; do :; done; grep -qi "rename" .planning/compat/COMPATIBILITY-NOTE.md && grep -qi "reorder" .planning/compat/COMPATIBILITY-NOTE.md && grep -qi "hide" .planning/compat/COMPATIBILITY-NOTE.md && grep -qi "re-icon" .planning/compat/COMPATIBILITY-NOTE.md && grep -qiE "0 broken|no broken|zero broken" .planning/compat/COMPATIBILITY-NOTE.md && echo OK</automated>
  </verify>
  <done>
COMPATIBILITY-NOTE.md exists with: all six plugins named and linked to their SURV-NN files; a summary matrix whose four operation columns (rename/reorder/hide/re-icon) are each populated for all six plugin rows with safe/degraded/broken classifications consistent with the source surveys; the 0-broken headline finding stated; one per-plugin findings subsection each naming its distinguishing manipulation behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add operation-affected-per-plugin cross-cut + DELV-01 traceability and self-consistency check</name>
  <files>.planning/compat/COMPATIBILITY-NOTE.md</files>
  <action>
Extend `COMPATIBILITY-NOTE.md` with the synthesis cross-cuts that make DELV-01 a usable seed for the later versioned milestone, then verify internal consistency:

1. **"Which operations are affected, across plugins" cross-cut:** A short section (table or list) inverting the matrix — for each Maestro operation, which plugins show degraded behavior and why the degradation recurs. Group the recurring root causes that span plugins:
   - **Rename → badge/HTML-in-title loss** (WooCommerce, Yoast, Elementor, WPForms) — wholesale title overwrite drops embedded count-badge / upsell HTML.
   - **Rename/Reorder/Hide → slug-resolution** (Jetpack absolute URL, Elementor absolute URL + `ver=`, WPForms absolute UTM URL, WooCommerce/Elementor/LifterLMS entity-encoded `&amp;` slugs, shared CPT-slug collisions in WooCommerce/LifterLMS, Yoast dual-slug/shared-slug).
   - **Reorder → render-time filter override** (WooCommerce `menu_order` separator re-cluster; LifterLMS `submenu_order()` override).
   - **Hide → cosmetic-only / non-cascading / moot-for-capped-roles** (all six).
   - **Re-icon → submenu no-op** (all six).
   This section is the conceptual bridge to DELV-02's ranked backlog — it surfaces the cross-plugin patterns that will become COMPAT-xx items, but does NOT yet assign IDs.

2. **DELV-01 success-criterion traceability:** A short table mapping ROADMAP Phase 16 success criterion 1 ("compatibility note presents all six findings under one schema + summary matrix per operation per plugin") to the sections that satisfy it, plus a row for requirement DELV-01 → this file.

3. **Self-consistency pass (verification, not new content):** Re-read each of the six per-plugin matrix cells written in Task 1 against the corresponding SURV-NN Part 2 matrix / "Net for Part 3" summary. Confirm no summary classification contradicts its source survey. Fix any mismatch in favor of the source survey. Confirm the 0-broken claim holds for all six (grep each SURV-NN for any `broken` classification in a matrix cell; all six surveys' Part 3 coverage notes state "no broken cells" — confirm none slipped through).
  </action>
  <verify>
    <automated>grep -qiE "DELV-01" .planning/compat/COMPATIBILITY-NOTE.md && grep -qiE "slug-resolution|slug resolution" .planning/compat/COMPATIBILITY-NOTE.md && grep -qiE "badge|title" .planning/compat/COMPATIBILITY-NOTE.md && grep -qiE "menu_order|submenu_order|render-time" .planning/compat/COMPATIBILITY-NOTE.md && ! grep -riE "\| *broken *\||— broken|: broken" .planning/compat/SURV-0*.md .planning/compat/SURV-06*.md | grep -viE "no broken|zero broken|0 broken|never broken|not broken|broken\.|broken cells|breaks?\b|broken/|/broken|access break" >/dev/null && echo OK</automated>
  </verify>
  <done>
The note contains an operation-affected-per-plugin cross-cut grouping the recurring root causes (badge-in-title loss, slug-resolution, render-time filter override, cosmetic hide, submenu re-icon no-op) across the plugins that exhibit them; a DELV-01 traceability table; and every summary-matrix cell has been confirmed against its source survey with no contradictions and the 0-broken finding verified across all six SURV-NN files.
  </done>
</task>

</tasks>

<verification>
Goal-backward checks for DELV-01 (ROADMAP Phase 16 success criterion 1):
- All six plugins appear in COMPATIBILITY-NOTE.md and each links to its SURV-NN source.
- The summary matrix has a row per plugin and a column per Maestro operation (rename / reorder / hide / re-icon); every cell is populated with a safe/degraded/broken classification consistent with the source survey.
- The single SCHM-01 vocabulary (safe/degraded/broken + the four fix categories) is used throughout — no ad-hoc classification terms.
- The 0-broken-cells headline finding is stated and is true (no SURV-NN matrix cell classifies any op as broken).
- No SURV-NN file or SCHEMA.md was modified by this plan.
</verification>

<success_criteria>
- `.planning/compat/COMPATIBILITY-NOTE.md` is committed and presents all six per-plugin findings under one schema.
- A summary safe/degraded/broken matrix shows, per plugin, which Maestro operations are affected — fully populated, no empty cells, consistent with the six source surveys.
- The note is a faithful, non-contradictory synthesis of SURV-01..06 and is ready to be summarized into the ranked backlog (DELV-02, Plan 16-02).
</success_criteria>

<output>
After completion, create `.planning/phases/16-synthesis/16-01-SUMMARY.md` per the summary template, recording the per-plugin worst-case classifications written into the matrix and any corrections made against a source survey during the self-consistency pass.
</output>
