---
phase: 08-docs-brand-assets
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/08-docs-brand-assets/08-VALIDATION.md
autonomous: false
requirements: [DOC-01, REL-06]
executor_model: sonnet
must_haves:
  truths:
    - "Doc/asset-only changes held at zero regressions: PHP unit 44/44, integration 29/29, e2e green, Plugin Check 0 errors, composer lint clean"
    - "DOC-01 checker is GREEN (0 offenders) on the final tree"
    - "DOC-01 is marked Complete in REQUIREMENTS traceability and ROADMAP Phase 8; REL-06 already Complete"
    - "STATE.md reflects Phase 8 complete and the v1.1 milestone ready to audit/close"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOC-01 flipped to Complete in the v1.1 traceability table"
      contains: "DOC-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 marked complete in the progress table"
      contains: "Phase 8"
    - path: ".planning/STATE.md"
      provides: "Current position updated to Phase 8 complete"
      contains: "Phase 8"
  key_links:
    - from: "the full regression suite"
      to: "the zero-regression bar"
      via: "unit/integration/e2e/Plugin Check/composer lint all green"
      pattern: "44|29"
---

<objective>
Prove the doc/asset-only changes from this phase did not move any test, VERIFY DOC-01 is legitimately complete (checker GREEN), and record Phase 8 done. This is the phase capstone.

Purpose: The zero-regression bar (unit 44/44, integration 29/29, e2e green, Plugin Check 0 errors, composer lint clean) must hold after doc edits. Then update tracking so the v1.1 milestone can be audited/closed.
Output: Green regression run recorded; DOC-01 verified Complete; Phase 8 marked done; 08-VALIDATION.md signed off; STATE.md reconciled.
</objective>

> **Refreshed 2026-06-17 — current state:** DOC-01 was *already* flipped to Complete in REQUIREMENTS during the 1.1.0 release (commit `284f9d6`), but that was **premature** — the doc-link checker was still RED (21 offenders) at that point. So do NOT just leave the checkbox; **verify `npm run check:doc-links` returns 0 offenders** (08-02 must have run) before treating DOC-01 as legitimately Complete. This capstone must also **reconcile pre-existing stale STATE.md fields**: frontmatter `last_activity` (still `2026-06-14` → `2026-06-17`), `total_plans: 10` (inconsistent with the real v1.1 plan count + `completed_plans`), `completed_phases: 1` (phases 6 & 7 are both done), the narrative "Phase 8 next" (Phase 8 is now complete), and the Performance "By Phase" table (it lists only the v1.0 phases — add phases 6/7/8). Also note: v1.0.0 **and 1.1.0** are now live on wordpress.org.

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-docs-brand-assets/08-VALIDATION.md
@TESTING.md

<interfaces>
<!-- Test commands (from package.json + composer + TESTING.md). Docker (wp-env) required for integration + e2e. -->
  Unit (no Docker):        composer test:unit
  Integration (Docker):    npm run env:start && npm run test:php
  E2E (Docker):            npm run test:e2e
  Lint:                    composer lint
  Doc-link checker:        npm run check:doc-links   (must exit 0)
  Plugin Check:            run against the extracted build/ zip (bin/build.sh) per prior phases.

If Docker (Colima/wp-env) is unavailable in the executor environment, this becomes
a checkpoint:human-verify: the human runs the Docker-dependent layers and reports
44/44, 29/29, e2e green, Plugin Check 0 errors. Unit + lint + checker are runnable
without Docker and MUST pass automatically regardless.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the Docker-free gates (unit, lint, doc-link checker)</name>
  <files>(no source changes — verification only)</files>
  <action>
    Run the gates that need no Docker and MUST be green after the doc edits: composer test:unit (expect 44/44), composer lint (expect clean), and npm run check:doc-links (expect 0 offenders) plus npm run test:js. Record results. If any fails, stop and report — a doc-only change must not break these.
  </action>
  <verify>
    <automated>composer test:unit && composer lint && npm run check:doc-links && npm run test:js</automated>
  </verify>
  <done>Unit 44/44, composer lint clean, doc-link checker 0 offenders, node:test green — all recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify Docker-dependent regression layers</name>
  <what-built>Doc/asset-only Phase 8 changes complete. Need confirmation the Docker-dependent layers and Plugin Check still hold at zero regressions.</what-built>
  <how-to-verify>
    1. Start Docker env: npm run env:start
    2. Integration: npm run test:php — expect 29/29 (81 assertions).
    3. E2E: npm run test:e2e — expect green (9/9).
    4. Build + Plugin Check: bin/build.sh then run Plugin Check on the extracted zip — expect 0 errors (readme/docs still parse).
    Report the numbers. (Skip only if you accept the Docker-free gates as sufficient evidence for a docs-only change — but the bar asks for the full suite.)
  </how-to-verify>
  <resume-signal>Type "approved 29/29 e2e-green pc-clean" or report any regression.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip DOC-01 to Complete, mark Phase 8 done, sign off 08-VALIDATION.md, update STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-docs-brand-assets/08-VALIDATION.md</files>
  <action>
    In REQUIREMENTS.md: check the DOC-01 box ([ ] -> [x]) and set its traceability row to Complete. In ROADMAP.md: mark Phase 8 box [x] and set the progress-table row to Complete with today's date; note plans complete count. In 08-VALIDATION.md: fill the Per-Task Verification Map with the real task IDs/commands from this phase, tick the Validation Sign-Off checkboxes, and set nyquist_compliant: true in frontmatter (every logic task — the DOC-01 checker — has an automated verify). In STATE.md: update Current Position to "Phase 8 complete; v1.1 milestone ready to audit/close" and append a Decisions entry summarizing the DOC-01 link checker + REL-06 reconciliation outcome.

    Do NOT touch the REL-06 mechanism wording (owned by 08-03). This plan only flips statuses and signs off validation.
  </action>
  <verify>
    <automated>grep -q "\[x\] \*\*DOC-01" .planning/REQUIREMENTS.md && grep -iq "nyquist_compliant: true" .planning/phases/08-docs-brand-assets/08-VALIDATION.md && echo TRACKING-UPDATED</automated>
  </verify>
  <done>DOC-01 marked Complete; Phase 8 marked done in ROADMAP; STATE.md reflects phase/milestone status; 08-VALIDATION.md signed off with nyquist_compliant: true.</done>
</task>

</tasks>

<verification>
- composer test:unit 44/44, composer lint clean, npm run check:doc-links 0 offenders, node:test green (automated).
- Integration 29/29, e2e green, Plugin Check 0 errors (human-verified via Docker).
- DOC-01 Complete; Phase 8 done; 08-VALIDATION.md nyquist_compliant: true.
</verification>

<success_criteria>
- Zero-regression bar held on a doc/asset-only phase.
- DOC-01 and REL-06 both Complete; Phase 8 closed; v1.1 ready to audit.
</success_criteria>

<output>
After completion, create .planning/phases/08-docs-brand-assets/08-04-SUMMARY.md with the full regression numbers.
</output>
