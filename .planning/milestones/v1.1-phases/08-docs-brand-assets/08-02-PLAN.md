---
phase: 08-docs-brand-assets
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - README.md
  - SPEC.md
  - TESTING.md
autonomous: false
requirements: [DOC-01]
executor_model: sonnet
must_haves:
  truths:
    - "Every in-prose inline-code file-path ref that resolves to a real repo file is a markdown link, in README, SPEC, and TESTING"
    - "Stale path refs are corrected then linked: maestro.php -> maestro-menu-editor.php, bare icon.svg -> .wordpress-org/icon.svg, global-setup.ts -> tests/e2e/global-setup.ts"
    - "WP-core refs (common.js, menu-header.php, wp-admin/menu-header.php) and all readme.txt refs remain inline code (NOT linked)"
    - "Refs inside fenced code blocks remain untouched"
    - "The checker reports 0 offenders (GREEN); node --test tests/js/ passes"
  artifacts:
    - path: "README.md"
      provides: "Prose file-path refs linkified; stale maestro.php/icon.svg corrected and linked"
      contains: "[`SPEC.md`](SPEC.md)"
    - path: "SPEC.md"
      provides: "includes/class-*.php refs and TESTING.md linkified; core refs left as code"
      contains: "class-config.php"
    - path: "TESTING.md"
      provides: "phpunit configs / bootstrap / global-setup refs linkified (global-setup path corrected)"
      contains: "global-setup.ts"
  key_links:
    - from: "README.md"
      to: "the referenced repo files"
      via: "markdown links with code-formatted link text"
      pattern: "\\[`[^`]+`\\]\\([^)]+\\)"
    - from: "tests/js/doc-links.test.mjs"
      to: "the converted docs"
      via: "findOffenders() now returns []"
      pattern: "findOffenders"
---

<objective>
Drive the DOC-01 checker from RED to GREEN: convert every flagged inline-code file-path reference into a markdown link in README.md, SPEC.md, and TESTING.md, correcting any stale paths along the way, while leaving WP-core refs, readme.txt, and fenced code untouched.

Purpose: Closes DOC-01's success criterion (the checker returns 0 offenders). Pure, mechanical link conversion driven by the LIVE checker offender list — no prose rewrites.
Output: Converted README.md, SPEC.md, TESTING.md; checker GREEN; node:test passes.
</objective>

> **Refreshed 2026-06-17 — current state:** the README doc cross-refs (`SPEC.md`, `docs/archive/FIXES.md`, `docs/user-guide.md`) and the `maestro.php`→`maestro-menu-editor.php` stale path were already converted/fixed inline during the 1.1.0 release (commit `284f9d6`). Do **not** assume the plan's original offender examples — run `npm run check:doc-links` for the LIVE list and convert exactly those. As of this refresh that is **21 offenders**: README ×2 (`playground/blueprint.json`, `playground/blueprint-hosted.json`), SPEC ×8 (the `includes/class-*.php` set + `TESTING.md`), TESTING ×4 (phpunit configs, `tests/bootstrap-unit.php`, and the one remaining **stale path** `global-setup.ts` → `tests/e2e/global-setup.ts`). Leave WP-core refs (`common.js`, `menu-header.php`) and all `readme.txt` refs as inline code.

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-docs-brand-assets/08-CONTEXT.md
@.planning/phases/08-docs-brand-assets/08-01-SUMMARY.md

# The checker is the source of truth for what to convert:
@bin/check-doc-links.mjs

<interfaces>
<!-- Link style convention (LOCKED, from CLAUDE.md + CONTEXT): code-formatted filename INSIDE the link text, -->
<!-- with a correct RELATIVE href from the linking doc's directory. -->

Pattern:  `SPEC.md`           ->  [`SPEC.md`](SPEC.md)
          `docs/user-guide.md` (from README at root) -> [`docs/user-guide.md`](docs/user-guide.md)
          `TESTING.md` (from SPEC.md at root)        -> [`TESTING.md`](TESTING.md)

Stale-path corrections (fix the path, THEN link it):
          `maestro.php`     -> [`maestro-menu-editor.php`](maestro-menu-editor.php)
          bare `icon.svg`   -> [`icon.svg`](.wordpress-org/icon.svg)   (keep "icon.svg" as the visible text; href points to the real path)
          `global-setup.ts` -> [`global-setup.ts`](tests/e2e/global-setup.ts)

LEAVE AS INLINE CODE (do NOT link):
  - WP core: `common.js`, `menu-header.php`, `wp-admin/menu-header.php`
  - Anything inside ``` fenced code blocks (e.g. `build/maestro.zip`, command snippets)
  - readme.txt is OUT OF SCOPE entirely (not edited in this plan)
  - Refs already wrapped in a link (e.g. README L52/L56 already-linked docs/user-guide.md and .planning/ROADMAP.md)

Concrete offender locations (verify against the 08-01 SUMMARY, which is authoritative):
  README.md:  L63 `SPEC.md` + `docs/archive/FIXES.md`; L71 `maestro.php`(stale); L72 PNG/svg asset refs + `icon.svg`(stale); L73 `composer.json` `package.json` `.wp-env.json` `bin/build.sh` `playwright.config.ts`; L74 `SPEC.md` `TESTING.md` `docs/user-guide.md` `docs/archive/FIXES.md`; L95 `TESTING.md`; L99 `playground/blueprint.json`; L116 `playground/blueprint-hosted.json`
  SPEC.md:    L37-42 `includes/class-*.php` (6 refs); L170 `TESTING.md`; LEAVE L105/L131 `common.js`, L52/L225 `menu-header.php`/`wp-admin/menu-header.php`
  TESTING.md: L31 `phpunit-unit.xml.dist` `tests/bootstrap-unit.php`; L48 `phpunit-integration.xml.dist`; L70 `global-setup.ts`(stale)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert flagged refs to markdown links in README.md, SPEC.md, TESTING.md (GREEN)</name>
  <files>README.md, SPEC.md, TESTING.md</files>
  <action>
    Work strictly from the plan 08-01 SUMMARY offender list (the authoritative RED baseline) and the &lt;interfaces&gt; block. For each offender, replace the bare inline-code span with a markdown link using the code-formatted-text-inside-link convention and the correct relative href from that doc's directory. For the three stale refs, point the href at the corrected real path (keep the original token as visible link text where that reads naturally — for `maestro.php` update the visible text to `maestro-menu-editor.php` since the old name is simply wrong).

    Do NOT touch: WP-core refs, any token inside a fenced code block, readme.txt, or refs already inside a markdown link. Do NOT rewrite surrounding prose — change only the ref spans. After editing, run the checker; if any offender remains, fix that specific span. Iterate until 0.
  </action>
  <verify>
    <automated>npm run check:doc-links && npm run test:js</automated>
  </verify>
  <done>`npm run check:doc-links` exits 0 (no offenders); `npm run test:js` passes (findOffenders()===[]). Core refs, readme.txt, and fenced-code refs are unchanged (confirm via git diff). The three stale paths now resolve.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify converted links</name>
  <what-built>All in-scope project-file references in README.md, SPEC.md, and TESTING.md converted to markdown links (with three stale paths corrected); WP-core refs, readme.txt, and fenced code left as-is. Checker is GREEN.</what-built>
  <how-to-verify>
    1. `git diff README.md SPEC.md TESTING.md` — confirm only ref spans changed, no prose rewrites, no fenced-code edits.
    2. Spot-check rendered links resolve: click (or `ls`) the hrefs for `maestro-menu-editor.php`, `.wordpress-org/icon.svg`, `tests/e2e/global-setup.ts`, a couple of `includes/class-*.php`.
    3. Confirm `common.js`, `menu-header.php`, `wp-admin/menu-header.php` are STILL inline code (not links).
    4. `npm run check:doc-links` prints 0 offenders.
  </how-to-verify>
  <resume-signal>Type "approved" or list any ref that should/should not have been linked.</resume-signal>
</task>

</tasks>

<verification>
- `npm run check:doc-links` exits 0; `npm run test:js` passes.
- git diff shows only ref-span edits in the three docs; no prose or fenced-code changes.
- The three stale hrefs resolve to existing files; WP-core refs remain code.
</verification>

<success_criteria>
- DOC-01 checker GREEN (0 offenders) and node:test green.
- All eligible refs linked with the locked code-in-link-text style; ineligible refs untouched.
- Human verification of link correctness recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docs-brand-assets/08-02-SUMMARY.md`.
</output>
