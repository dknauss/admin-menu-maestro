---
phase: 08-docs-brand-assets
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .wordpress-org/source/HOW-TO-REGENERATE.md
autonomous: false
requirements: [REL-06]
executor_model: opus
must_haves:
  truths:
    - "npm run assets:banners regenerates banner-772x250.png and banner-1544x500.png from the committed source in one command, no manual steps"
    - "Both regenerated banners are valid PNGs at the correct dimensions (772x250 and 1544x500)"
    - "REQUIREMENTS.md and ROADMAP.md accurately record the actual shipped mechanism (in-code SVG master in build_final.py + Inkscape render + Pillow LANCZOS downscale), so REL-06 Done status is auditable"
    - "No banner redesign occurs; public assets are not changed beyond a deterministic regeneration"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REL-06 line reconciled to the actual mechanism + verified-regeneration note"
      contains: "build_final.py"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 REL-06 criteria 2-3 reconciled to the shipped in-code-SVG/Inkscape/Pillow pipeline"
      contains: "build_final.py"
    - path: ".wordpress-org/source/HOW-TO-REGENERATE.md"
      provides: "Note that the SVG master is generated in build_final.py (not a standalone .svg file)"
      contains: "build_final.py"
  key_links:
    - from: "package.json assets:banners"
      to: ".wordpress-org/source/build_final.py"
      via: "OUT_DIR=.wordpress-org python3 .wordpress-org/source/build_final.py"
      pattern: "build_final.py"
    - from: ".planning/REQUIREMENTS.md REL-06"
      to: "the shipped pipeline"
      via: "wording records Inkscape+Pillow + in-code SVG master"
      pattern: "Inkscape|Pillow"
---

<objective>
Verify the already-shipped REL-06 banner pipeline regenerates deterministically from committed source, then reconcile the roadmap/requirements wording so REL-06 Done status accurately records the actual mechanism. This is verify + reconcile, NOT a rebuild — no banner redesign.

Purpose: REL-06 is marked Complete but the roadmap literal criteria describe a slightly different mechanism than what shipped. Audit the gap precisely and record the substitution so the milestone can be closed honestly.

Key finding to encode (verified during planning): build_final.py builds the banner as an SVG string IN CODE, rasterizes it via Inkscape (subprocess.run(["inkscape", ...])), then downscales 2x to 1x with Pillow LANCZOS. So Inkscape and Pillow ARE both used (matching the roadmap), and the leader/overline before "ADMIN MENU" was removed (brand wordmark leads). The ONLY real divergence from the literal criteria: the SVG master is generated programmatically inside build_final.py rather than stored as a standalone editable .svg file under source/. That is the precise thing to reconcile. (CONTEXT.md framed this as "Python/Pillow, NOT Inkscape" — that framing is INCORRECT; use the accurate framing here.)

Output: Verified regeneration; reconciled REL-06 wording in REQUIREMENTS.md + ROADMAP.md; a one-line clarification in HOW-TO-REGENERATE.md.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-docs-brand-assets/08-CONTEXT.md
@.wordpress-org/source/HOW-TO-REGENERATE.md
@.wordpress-org/source/build_final.py
@package.json

<interfaces>
<!-- Verified during planning: -->
<!--   python3 3.9.6 + Pillow 11.3.0 present; inkscape on PATH at /opt/homebrew/bin/inkscape. -->
<!--   build_final.py uses subprocess.run(["inkscape", ...]) then PIL LANCZOS downscale. -->
<!--   Both banner PNGs exist and are committed (last touched by commit 8ff32a2 "banner subtitle"). -->

Regeneration command (from package.json): npm run assets:banners
  -> OUT_DIR=.wordpress-org python3 .wordpress-org/source/build_final.py

DETERMINISM CAVEAT (important): Inkscape font rasterization + LANCZOS downscale are
NOT guaranteed byte-identical across machines/versions. A strict git-diff-quiet on
the PNGs is the BEST-CASE signal, not a hard requirement. The binding criteria are:
(a) the command runs end-to-end with no manual steps, and (b) outputs are valid PNGs
at exact dimensions. Treat a byte-diff as "expected — visual review gate applies"
(roadmap criterion #4), NOT a failure. If a diff appears, restore the committed PNGs
with: git checkout -- .wordpress-org/banner-772x250.png .wordpress-org/banner-1544x500.png
so no unreviewed asset churn lands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify npm run assets:banners regenerates both banners from committed source</name>
  <files>.wordpress-org/banner-772x250.png, .wordpress-org/banner-1544x500.png</files>
  <action>
    Run npm run assets:banners and confirm it completes with no manual intervention. Verify both output files are valid PNGs at exact dimensions (772x250 and 1544x500) using file (or sips -g pixelWidth -g pixelHeight). Then run git diff --stat on the two banner PNGs and record whether regeneration was byte-identical. Per the determinism caveat, a byte-diff is acceptable (visual-review gate) and NOT a failure — but to avoid landing unreviewed asset churn, restore the committed PNGs afterward with git checkout. Record the outcome (identical vs. visually-equivalent-but-non-byte-identical) in the SUMMARY.
  </action>
  <verify>
    <automated>npm run assets:banners && file .wordpress-org/banner-772x250.png | grep -q "772 x 250" && file .wordpress-org/banner-1544x500.png | grep -q "1544 x 500" && git checkout -- .wordpress-org/banner-772x250.png .wordpress-org/banner-1544x500.png && echo "REGEN OK plus dimensions verified plus committed PNGs restored"</automated>
  </verify>
  <done>npm run assets:banners runs end-to-end with no manual steps; both PNGs validate at exact dimensions; the committed public banners are unchanged on disk after verification (restored). Determinism outcome recorded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Decide REL-06 reconciliation approach</name>
  <decision>How to reconcile REL-06 wording with the actual shipped mechanism</decision>
  <context>
    REL-06 is marked Done. The roadmap literally says "editable SVG master under .wordpress-org/source/" plus "Inkscape render -> Pillow downscale/crop." Reality: Inkscape + Pillow ARE used and the leader/overline was removed, BUT the SVG master is generated in build_final.py rather than stored as a standalone .svg file. We must record this substitution accurately.
  </context>
  <options>
    <option id="option-a">
      <name>Record mechanism substitution as a parenthetical note on the REL-06 line (recommended)</name>
      <pros>Minimal, auditable, keeps Done status; one accurate sentence in REQUIREMENTS + matching note in ROADMAP Phase 8 criteria; no scope creep.</pros>
      <cons>Leaves the standalone-.svg-file expectation formally unmet (but intent — editable source + one-command regen + leader removed — is met).</cons>
    </option>
    <option id="option-b">
      <name>Also extract a standalone editable .svg master file under source/ to satisfy the literal wording</name>
      <pros>Matches roadmap text verbatim.</pros>
      <cons>Net-new asset work; risks banner churn; CONTEXT explicitly defers "no new SVG master if the Python pipeline already satisfies the intent." Out of scope.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (recommended) or option-b.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconcile REL-06 wording in REQUIREMENTS.md, ROADMAP.md, and HOW-TO-REGENERATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .wordpress-org/source/HOW-TO-REGENERATE.md</files>
  <action>
    Per the checkpoint decision (default option-a): update the REL-06 line in REQUIREMENTS.md to accurately state the shipped mechanism — regenerated by npm run assets:banners -> build_final.py, which builds the banner as an in-code SVG master, renders it with Inkscape, and downscales with Pillow (LANCZOS); the standalone-.svg-file form was replaced by the in-code SVG master, intent met (editable source + one-command regen + leader line removed). Mark it verified this phase.

    In ROADMAP.md Phase 8 REL-06 success criteria, annotate criteria #2 and #3 to record the same substitution (SVG master is generated in build_final.py, not a standalone file) so the Done status is auditable. Do not alter criterion intent.

    In HOW-TO-REGENERATE.md, add one clarifying line that the SVG master is generated inside build_final.py (there is no standalone .svg file to edit; edit the banner_svg()/icon_svg() builders + the P=dict(...) palette instead). Keep edits surgical — no banner redesign, no command changes.

    NOTE: REQUIREMENTS.md and ROADMAP.md are also touched by plan 08-04 (final status flip) — this plan edits the REL-06 mechanism wording only; 08-04 owns the phase-complete status flip. Sequencing (this plan wave 1; 08-04 wave 3) prevents conflict.
  </action>
  <verify>
    <automated>grep -iq "build_final.py" .planning/REQUIREMENTS.md && grep -iqE "Inkscape|Pillow" .planning/REQUIREMENTS.md && grep -iq "build_final.py" .planning/ROADMAP.md && grep -iq "build_final.py" .wordpress-org/source/HOW-TO-REGENERATE.md && echo RECONCILED</automated>
  </verify>
  <done>REQUIREMENTS.md and ROADMAP.md accurately record the in-code-SVG + Inkscape + Pillow mechanism and the standalone-svg substitution; HOW-TO-REGENERATE.md notes the in-code SVG master. REL-06 Done status is now auditable. No banner pixels changed.</done>
</task>

</tasks>

<verification>
- npm run assets:banners regenerates both banners at exact dimensions with no manual steps; committed PNGs restored (no unreviewed churn).
- REQUIREMENTS.md, ROADMAP.md, HOW-TO-REGENERATE.md record the actual mechanism (build_final.py in-code SVG master, Inkscape render, Pillow LANCZOS downscale, leader line removed).
- No banner redesign; REL-06 remains Done with accurate, auditable wording.
</verification>

<success_criteria>
- REL-06 regeneration verified from committed source.
- Mechanism substitution reconciled in requirements + roadmap + how-to.
- Zero banner asset churn committed.
</success_criteria>

<output>
After completion, create .planning/phases/08-docs-brand-assets/08-03-SUMMARY.md including the determinism outcome (byte-identical vs visually-equivalent).
</output>
