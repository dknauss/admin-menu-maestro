---
phase: 07-visual-polish-icons
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/e2e/editor.spec.ts
  - .planning/phases/07-visual-polish-icons/UX-02-WALKTHROUGH.md
autonomous: false
requirements: [UX-02, ICON-01]
user_setup: []

must_haves:
  truths:
    - "Playwright e2e regression proves the polished icon-picker grid renders the bundled solid icons and the dashicons tab side by side, both scannable, with no [hidden]/display regressions in tab switching or search"
    - "Playwright e2e proves no text-overlap and no awkward control-resize in the toolbar at both >782px and <=782px viewports after the UX-02 polish"
    - "Playwright e2e proves the first-run cue appears for a fresh session and does not reappear after dismissal (localStorage-gated)"
    - "Screenshot deliverables exist: a side-by-side of the bundled (solid) tab vs the dashicons tab (ICON-01 weight check) and before/after-style captures of the polished toolbar/grid (UX-02)"
    - "The whole suite holds at zero regression: PHP unit 44/44, integration 29/29, full Playwright e2e green (Phase 6 baseline + the new Phase 7 specs), Plugin Check 0 errors, composer lint clean, npm run test:js green"
  artifacts:
    - path: "tests/e2e/editor.spec.ts"
      provides: "New Phase 7 specs: icon-grid scanability/side-by-side, no-overlap/no-resize at both breakpoints, first-run cue once-only, plus screenshot capture"
      contains: "first-run"
    - path: ".planning/phases/07-visual-polish-icons/UX-02-WALKTHROUGH.md"
      provides: "Keyboard/mouse walkthrough notes + links to the captured before/after and side-by-side screenshots"
      contains: "Walkthrough"
  key_links:
    - from: "tests/e2e/editor.spec.ts"
      to: ".planning/phases/07-visual-polish-icons/screenshots/"
      via: "page.screenshot({ path }) writes the deliverable images"
      pattern: "screenshot"
    - from: "tests/e2e/editor.spec.ts"
      to: "the polished UI (Plans 01 + 02)"
      via: "selectors on .maestro-icon-grid, .maestro-status, .maestro-firstrun"
      pattern: "maestro-firstrun"
---

<objective>
Lock the Phase 7 polish in with Playwright e2e regression and produce the UX-02 /
ICON-01 visual deliverables. The new specs prove the solid icon grid is scannable
beside dashicons, the toolbar polish introduces no text-overlap or control-resize
at either breakpoint, and the first-run cue shows exactly once. The plan also
captures the side-by-side (icon weight) and before/after (toolbar/grid) screenshots
and writes the keyboard/mouse walkthrough notes the requirement calls for.

Purpose: UX-02 visual polish is UI styling — not unit-TDD'd — so its safety net is
e2e regression plus human-verified screenshots. This plan is the regression guard
AND the deliverable producer, and it runs after Plans 01 (solid bundle) and 02
(CSS/markup polish) are in.

Output: New specs in tests/e2e/editor.spec.ts; screenshot images under
.planning/phases/07-visual-polish-icons/screenshots/; and UX-02-WALKTHROUGH.md with
notes + a human-verify checkpoint confirming the icons "mix" and the polish reads
native (the perceptual judgments automation can't make).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-visual-polish-icons/07-CONTEXT.md
@.planning/phases/07-visual-polish-icons/07-VALIDATION.md

# The existing e2e patterns to extend (tab switching, filter, viewport, data-URI):
@tests/e2e/editor.spec.ts

<interfaces>
<!-- Existing e2e idioms to reuse (already in editor.spec.ts) — do not reinvent. -->

Enter edit mode: `await page.goto('/wp-admin/index.php?maestro_edit=1')`
Open picker: select `#menu-posts > a.menu-top`, then `panel.locator('.maestro-icon-btn').click()`
Picker: `.maestro-icon-popover`; tabs via `picker.getByRole('tab', { name: 'Bootstrap' })`
Bootstrap panel `#maestro-panel-bootstrap`, Dashicons panel `#maestro-panel-dashicons`
Search filter narrows `:visible` cells (existing regression guard at line ~177)
Viewport: `await page.setViewportSize({ width, height })` (folded test uses 900px)
Reset cleanup idiom: `page.once('dialog', d=>d.accept()); .maestro-reset-all` click.

NEW selectors introduced by Plan 02 (depend on it):
- `.maestro-firstrun` (cue) + its dismiss button
- `.maestro-status` with per-state non-color signal
- `.maestro-icon-grid` cells tuned to ~20px

Screenshots: write to `.planning/phases/07-visual-polish-icons/screenshots/` via
`await page.screenshot({ path: '.planning/phases/07-visual-polish-icons/screenshots/<name>.png' })`.
Create the dir in the spec (fs.mkdirSync recursive) or via a beforeAll. These are
deliverable PNGs for human review, NOT toHaveScreenshot pixel-diff baselines (avoid
flaky pixel baselines — use plain captures).

localStorage for the first-run test: clear it before the fresh-session assertion
(`await page.addInitScript(() => localStorage.removeItem('maestroFirstRunDone'))`),
then assert the cue is visible; dismiss; reload; assert it's gone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e regression specs — grid side-by-side, no-overlap/no-resize, first-run once [model: sonnet]</name>
  <files>tests/e2e/editor.spec.ts</files>
  <action>
    model: sonnet — write Playwright specs from the explicit assertions below, reusing
    the existing idioms in the file.

    Add a new describe block (or specs within the existing one) covering:

    1. ICON-01 side-by-side capture + scanability: open the picker, screenshot the
       Bootstrap (now solid) tab panel and the Dashicons tab panel into
       .planning/phases/07-visual-polish-icons/screenshots/ (e.g. icons-bootstrap-tab.png,
       icons-dashicons-tab.png, and one combined icons-side-by-side.png of the open
       picker). Assert the Bootstrap panel renders > 50 `.maestro-icon-cell:visible`
       (the 87-icon bundle), each with a 20px img, and that search 'gear' still
       narrows visible cells (reuse the existing filter regression guard).

    2. UX-02 no-overlap / no-resize at BOTH breakpoints: at width 1200 and again at
       width 700 (the <=782px branch), enter edit mode, select Posts so the panel
       shows. Assert:
       - the toolbar and panel are visible and the status element is visible;
       - no horizontal overflow of the toolbar (boundingBox width <= viewport width);
       - the rename input's width does not exceed its container (no awkward resize):
         compare `.maestro-rename-input` box to `.maestro-panel` box;
         capture toolbar-1200.png and toolbar-700.png as the before/after deliverables.
       (Use bounding-box math, not pixel diff.)

    3. First-run cue once-only: `addInitScript` to clear maestroFirstRunDone, goto edit
       mode, assert `.maestro-firstrun` visible; click its dismiss; assert it's gone;
       reload; assert `.maestro-firstrun` has count 0 (suppressed by localStorage).

    Keep every state-mutating spec self-cleaning (reset-all idiom) so the suite stays
    order-independent. Run `npm run test:e2e` — all specs (Phase 6 baseline + these)
    MUST pass. Commit: `test(07-03): e2e regression for solid grid, toolbar polish, first-run cue`.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>New e2e specs pass alongside the Phase 6 baseline; screenshots written under .planning/phases/07-visual-polish-icons/screenshots/; no-overlap/no-resize asserted at 1200px and 700px; first-run cue proven once-only; suite is order-independent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: confirm icons mix with dashicons and the polish reads native [model: opus]</name>
  <action>
    Review the captured screenshots and do a live keyboard/mouse pass to clear the
    ICON-01 weight gate and the UX-02 polish gate per the how-to-verify steps. This is
    the one perceptual judgment automation can't make; it decides whether the Heroicons
    fallback (Claude's Discretion #2) is needed.
  </action>
  <what-built>
    Solid `*-fill` icon bundle (Plan 01) + polished toolbar/status/grid + first-run
    cue (Plan 02), with e2e regression and captured screenshots (Task 1 above):
    - icons-side-by-side.png / icons-bootstrap-tab.png / icons-dashicons-tab.png
    - toolbar-1200.png / toolbar-700.png
    all under .planning/phases/07-visual-polish-icons/screenshots/.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/07-visual-polish-icons/screenshots/icons-side-by-side.png
       (and the two per-tab PNGs). CONFIRM the bundled icons now read at a solid
       weight comparable to the dashicons tab — they should "mix," not look
       noticeably lighter. This is the ICON-01 perceptual gate.
       - If they STILL read too light: that triggers Claude's Discretion #2
         (Heroicons Mini fallback). Say so in your resume note; a follow-up gap plan
         installs Heroicons. Default expectation: Bootstrap fill is enough.
    2. Open toolbar-1200.png and toolbar-700.png. CONFIRM the toolbar reads as
       grouped zones (status | label | controls | right actions), status state is
       legible, and nothing overlaps or is awkwardly squished at either width.
    3. In a live editor (`?maestro_edit=1`), do a quick keyboard + mouse pass:
       Tab through the toolbar controls, trigger a save (rename + Enter) and watch the
       status go saving→saved (distinguishable without relying on color), open the
       icon picker and arrow around the grid. CONFIRM it reads as native WP admin.
  </how-to-verify>
  <resume-signal>Type "approved" if the icons mix and the polish reads native, or describe what still reads too light / overlaps / feels off (and whether Heroicons fallback is warranted).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write UX-02 walkthrough notes + run the full zero-regression suite [model: sonnet]</name>
  <files>.planning/phases/07-visual-polish-icons/UX-02-WALKTHROUGH.md</files>
  <action>
    model: sonnet — capture the deliverable notes and run the full gate.

    Create .planning/phases/07-visual-polish-icons/UX-02-WALKTHROUGH.md containing:
    - A "Walkthrough" section with keyboard and mouse notes from the checkpoint pass
      (what was tested: toolbar tab order, save status transition, icon-grid arrow
      nav, first-run cue once-only, both breakpoints).
    - Links to the captured screenshots (side-by-side + toolbar before/after) using
      markdown links to the screenshots/ files.
    - A one-line ICON-01 verdict (Bootstrap fill sufficient, or Heroicons follow-up
      flagged) reflecting the checkpoint outcome.
    - A zero-regression results table (the commands below and their pass counts).

    Then run the FULL phase gate and record results:
    `npm run test:js` (green), `composer test:unit` (44/44),
    `composer test:integration` / `npm run test:php` (29/29),
    `npm run test:e2e` (green), `composer lint` (clean). Note Plugin Check 0 errors is
    verified on the build zip (`bash bin/build.sh` then run Plugin Check) — if the
    Plugin Check CLI isn't available locally, state that it must be confirmed on the
    build zip before phase close and leave the other gates green.
    Commit: `docs(07-03): UX-02 walkthrough notes and zero-regression results`.
  </action>
  <verify>
    <automated>npm run test:js && composer test:unit && npm run test:php && npm run test:e2e && composer lint && test -f .planning/phases/07-visual-polish-icons/UX-02-WALKTHROUGH.md</automated>
  </verify>
  <done>UX-02-WALKTHROUGH.md exists with keyboard/mouse notes, screenshot links, the ICON-01 verdict, and the results table; full suite green (JS unit, PHP unit 44/44, integration 29/29, e2e, lint); Plugin Check status recorded.</done>
</task>

</tasks>

<verification>
- `npm run test:e2e` — Phase 6 baseline + the new Phase 7 specs all green
- `npm run test:js` — icons-bundle suite green
- `composer test:unit` — 44/44 · `npm run test:php` — 29/29 · `composer lint` — clean
- Plugin Check — 0 errors on `build/maestro-menu-editor.zip`
- Deliverables present: side-by-side + toolbar before/after PNGs under screenshots/, UX-02-WALKTHROUGH.md with notes and the ICON-01 verdict
</verification>

<success_criteria>
- UX-02 proven by e2e regression (no text-overlap, no control-resize at both breakpoints; grid scannable; first-run cue once-only) and documented with before/after + side-by-side screenshots and walkthrough notes
- ICON-01 perceptual gate cleared at the human-verify checkpoint (icons mix with dashicons) — or Heroicons fallback explicitly flagged for a follow-up
- Zero-regression bar met: PHP unit 44/44, integration 29/29, e2e green, lint clean, JS unit green, Plugin Check 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-polish-icons/07-03-SUMMARY.md`
</output>
