---
phase: 09-editor-ux-polish
plan: 05
type: execute
wave: 4
depends_on: [02, 03, 04]
autonomous: false
files_modified:
  - assets/maestro.css
  - tests/e2e/editor.spec.ts
  - .planning/phases/09-editor-ux-polish/screenshots/
requirements: [UX-07]
user_setup: []

must_haves:
  truths:
    - "At <=782px (the WP admin mobile breakpoint) the toolbar controls -- every .button and the .maestro-rename-input -- use denser padding and smaller font so the toolbar fits without overflow"
    - "Every real tap target (every .button: Icon, Visibility, Reset Item, Reset All, Exit; and the .maestro-rename-input) keeps a >=44px min-height even as padding/font shrink"
    - "Density-first: the existing flex-wrap toolbar (BUG-03) handles overflow; a structural restructure happens ONLY if a 700px screenshot shows density alone still overflows -- gated by a human screenshot-review checkpoint BEFORE any restructure"
    - "A new e2e assertion proves every button + the rename input is >=44px tall at <=782px (bounding-box check); the existing 700px no-overflow and no-overlap guards still pass"
  artifacts:
    - path: "assets/maestro.css"
      provides: "density rules + 44px min-height floors inside the @media (max-width:782px) block"
      contains: "min-height: 44px"
    - path: "tests/e2e/editor.spec.ts"
      provides: ">=44px tap-target bounding-box assertion at <=782px for every button + rename input"
      contains: "44"
    - path: ".planning/phases/09-editor-ux-polish/screenshots/"
      provides: "700px before/after density screenshots for the restructure decision"
      contains: ""
  key_links:
    - from: "assets/maestro.css @media (max-width:782px)"
      to: ".maestro-toolbar .button, .maestro-rename-input"
      via: "density padding/font + min-height:44px floor"
      pattern: "min-height: 44px"
    - from: "tests/e2e/editor.spec.ts"
      to: ".maestro-toolbar .button"
      via: "boundingBox().height >= 44 assertion at a <=782px viewport"
      pattern: "boundingBox"
---

<objective>
UX-07 mobile/small-screen pass: at <=782px reduce control and input padding/font-size
so the toolbar is denser and fits without overflow, while holding a >=44px touch-target
floor on every real tap target. Density-first -- restructure layout ONLY if a 700px
screenshot shows density alone still overflows.

Purpose: BUG-03 added toolbar flex-wrap, but on small/mobile the buttons and rename
field are still desktop-sized, so the toolbar is bulky and wraps into too many rows.
Denser sizing makes it read well on mobile; the 44px floor keeps every tap target
comfortably operable per the locked decision.

Output: density + 44px-floor rules in the existing @media (max-width:782px) block, a
new >=44px bounding-box e2e assertion, 700px before/after screenshots, and a human
checkpoint to decide whether any structural restructure is warranted. Depends on Plans
02/03/04 (all toolbar/CSS changes must be in before the final density + screenshot pass).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-editor-ux-polish/09-CONTEXT.md
@.planning/phases/09-editor-ux-polish/09-RESEARCH.md
@.planning/phases/09-editor-ux-polish/09-VALIDATION.md

# The existing @media (max-width:782px) block (~418-437) to extend:
@assets/maestro.css
# The existing 700px no-overflow (~508) + no-overlap (~557) guards to preserve, and
# where the new 44px assertion goes:
@tests/e2e/editor.spec.ts

<interfaces>
<!-- Exact anchors from 09-RESEARCH.md, verified in-tree. -->

Existing @media (max-width:782px) block (assets/maestro.css:418-437) only narrows the
rename input width and panel-label max-width and sets toolbar left:0; it does NOT reduce
button padding/font or enforce >=44px. ADD to that block:
```css
.maestro-toolbar {
    gap: 6px 10px;
    padding: 8px 10px;
}
.maestro-toolbar .button {
    padding: 4px 8px;
    font-size: 12px;
    min-height: 44px;   /* tap-target floor */
}
.maestro-rename-input {
    padding: 0 6px;
    font-size: 12px;
    min-height: 44px;   /* tap-target floor */
}
```
Keep .maestro-toolbar { left: 0; } and .maestro-firstrun { left: 0; } as-is (Pitfall 6).
The toolbar already has flex-wrap (BUG-03) -- density makes each row shorter; wrapping is
the existing overflow fallback.

The 44px floor exceeds WCAG 2.5.8 (24px AA) and matches 2.5.5 (44px AAA) -- conservative,
locked. min-height (not height) lets content set the real height but never below 44px.

e2e guards to PRESERVE (tests/e2e/editor.spec.ts):
- ~line 508: no toolbar overflow at 700px -- must still pass.
- ~line 557: no control overlap at 700px -- must still pass.
NEW assertion to ADD: at a <=782px viewport (e.g. 700px) with the panel open, every
`.maestro-toolbar .button` and the `.maestro-rename-input` has boundingBox().height >= 44.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Density + 44px tap-target floor at <=782px (CSS) [model: sonnet]</name>
  <files>assets/maestro.css</files>
  <action>
    model: sonnet -- CSS density work to the explicit checklist in the interfaces block.

    Extend the existing @media (max-width:782px) block (~418-437) with the density rules:
    `.maestro-toolbar` gap/padding reduction; `.maestro-toolbar .button` padding 4px 8px,
    font-size 12px, min-height 44px; `.maestro-rename-input` padding 0 6px, font-size 12px,
    min-height 44px. Use min-height (not height) so the 44px is a floor, not a fixed size.
    Preserve the existing `left: 0` rules and the icon-picker touch rules already in the
    block. Do NOT restructure the toolbar layout in this task -- density only (the restructure
    decision is gated on the screenshot in Task 3).

    DISCRETION recorded: padding/font values (4px 8px / 12px) chosen to shrink the controls
    while the 44px min-height preserves the tap target; these are the research-recommended
    starting values and may be nudged if the screenshot shows the controls cramped -- but the
    44px floor is fixed.

    Commit: `style(09-05): denser toolbar controls + 44px tap-target floor at <=782px (UX-07)`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('assets/maestro.css','utf8');const m=c.match(/@media[^{]*max-width:\s*782px[\s\S]*/);if(!m)throw new Error('782px media block not found');const blk=m[0];const floors=(blk.match(/min-height:\s*44px/g)||[]).length;if(floors<2)throw new Error('need >=2 min-height:44px floors (button + rename input), found '+floors);if(!/\.maestro-toolbar\s+\.button/.test(blk))throw new Error('no .maestro-toolbar .button density rule');if(!/font-size/.test(blk))throw new Error('no font-size density reduction');process.exit(0)"</automated>
  </verify>
  <done>The <=782px block reduces button + input padding/font and enforces min-height:44px on both .maestro-toolbar .button and .maestro-rename-input; left:0 and icon-picker rules preserved; no layout restructure yet.</done>
</task>

<task type="auto">
  <name>Task 2: e2e >=44px tap-target assertion at <=782px [model: sonnet]</name>
  <files>tests/e2e/editor.spec.ts</files>
  <action>
    model: sonnet -- add the Wave-0 missing e2e assertion from explicit spec.

    Add a Playwright assertion (extend the existing narrow-viewport test, or a sibling in the
    same describe) that, at a <=782px viewport (use the existing 700px viewport the spec already
    sets) with the panel open (an item selected), iterates every `.maestro-toolbar .button`
    and the `.maestro-rename-input` and asserts each element's `boundingBox().height >= 44`.

    PRESERVE the existing 700px no-overflow (~line 508) and no-overlap (~line 557) guards --
    do not weaken them. Run `npm run test:e2e` if runnable. The static verify below guards the
    assertion exists; the full run is part of Plan 06's regression gate.

    Commit: `test(09-05): e2e >=44px tap-target floor at narrow widths (UX-07)`.
  </action>
  <verify>
    <automated>grep -q "boundingBox" tests/e2e/editor.spec.ts && grep -Eq "44" tests/e2e/editor.spec.ts && grep -q "maestro-rename-input" tests/e2e/editor.spec.ts</automated>
  </verify>
  <done>editor.spec.ts asserts every toolbar button + the rename input is >=44px tall at <=782px via boundingBox; the existing 700px overflow/overlap guards remain intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): 700px density screenshot review -- confirm fit and decide on restructure [model: opus]</name>
  <files>.planning/phases/09-editor-ux-polish/screenshots/</files>
  <action>
    BEFORE the checkpoint, the executor (model: sonnet) captures before/after screenshots at a
    700px viewport with the panel open, saved to .planning/phases/09-editor-ux-polish/screenshots/
    (e.g. toolbar-700-before.png, toolbar-700-after.png) using the existing Playwright screenshot
    path the repo uses (see 07/08 screenshots/ for the convention). This automates everything up
    to the human visual judgment.

    what-built: Density CSS at <=782px (denser button/input padding + font, 44px tap-target floor)
    plus a >=44px e2e assertion. The toolbar already wraps (BUG-03); this checkpoint confirms
    density alone makes the toolbar read well and decides whether ANY structural restructure is
    warranted -- a planner's-discretion call deliberately gated on this screenshot per
    09-RESEARCH.md Open Question #3.
  </action>
  <verify>
    <automated>test -d .planning/phases/09-editor-ux-polish/screenshots/ && ls .planning/phases/09-editor-ux-polish/screenshots/ | grep -qi "700"</automated>
  </verify>
  <done>
    700px before/after screenshots exist in the screenshots/ dir; a human has reviewed them and
    EITHER approved density-only (no restructure) OR described the specific 700px overflow/clipping
    that warrants a follow-up density nudge or minimal restructure. The decision is recorded in the
    SUMMARY.
  </done>
  <how-to-verify>
    1. Open both 700px screenshots.
    2. Confirm: the toolbar fits without ugly overflow; controls are denser but every tap target
       still looks comfortably >=44px; text does not overlap or clip.
    3. DECIDE (model: opus for the judgment if a call is needed): does density alone read well, or
       is a structural restructure (stacked rows / overflow grouping) warranted? If density is
       sufficient (expected per research's ~700px fit estimate), approve as-is. If not, describe
       the specific overflow so a follow-up density nudge or a minimal restructure can be scoped.
  </how-to-verify>
  <resume-signal>Type "approved" if density alone reads well (no restructure), or describe the specific 700px overflow/clipping that needs a restructure or further density nudge.</resume-signal>
</task>

</tasks>

<verification>
- `npm run test:e2e` -- new >=44px assertion green; existing 700px no-overflow + no-overlap guards still green
- `composer test:unit` -- 44/44 unchanged; `composer lint` clean (no PHP touched, but run for hygiene)
- Manual (09-VALIDATION.md): 700px + real phone width screenshots read well; tap targets comfortably >=44px
- Restructure decision recorded in the SUMMARY (approved density-only, or the scoped restructure)
</verification>

<success_criteria>
- <=782px controls are denser (reduced padding/font) and the toolbar fits without overflow
- Every real tap target (all buttons + rename input) holds a >=44px min-height floor, proven by e2e
- Density-first honored: restructure only if the 700px screenshot review demands it (human-gated)
- 700px before/after screenshots captured; the restructure call is recorded
</success_criteria>

<output>
After completion, create `.planning/phases/09-editor-ux-polish/09-05-SUMMARY.md`
(record the screenshot-review restructure decision explicitly).
</output>
