---
phase: 12-release-assets-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/specs/capture-directory-screenshots.spec.ts
  - package.json
  - .wordpress-org/screenshot-1.png
  - .wordpress-org/screenshot-2.png
  - .wordpress-org/screenshot-3.png
  - .wordpress-org/screenshot-4.png
  - .planning/phases/12-release-assets-refresh/screenshots/
autonomous: false
requirements: [REL-08]
must_haves:
  truths:
    - "A MAESTRO_CAPTURE-gated capture-directory-screenshots.spec.ts exists and is skipped on a normal test:e2e run"
    - "Screenshots are captured against the final v1.2 editor UI including the Phase 11.2 icon-only unified toolbar"
    - "Each screenshot-N.png is written to both the planning review dir and .wordpress-org/"
    - "Each screenshot PNG is under the 10 MB wp.org limit"
    - "The live .wordpress-org/screenshot-*.png are overwritten only after explicit human visual review"
  artifacts:
    - path: "tests/e2e/specs/capture-directory-screenshots.spec.ts"
      provides: "MAESTRO_CAPTURE-gated Playwright capture of the v1.2 directory screenshots"
      contains: "test.skip"
      min_lines: 30
    - path: "package.json"
      provides: "screenshots script extended to run the new capture spec"
      contains: "capture-directory-screenshots"
    - path: ".wordpress-org/screenshot-1.png"
      provides: "Directory screenshot 1 against post-11.2 UI"
  key_links:
    - from: "tests/e2e/specs/capture-directory-screenshots.spec.ts"
      to: ".wordpress-org/screenshot-N.png"
      via: "page.screenshot({ path }) gated by MAESTRO_CAPTURE"
      pattern: "page\\.screenshot"
    - from: "package.json"
      to: "tests/e2e/specs/capture-directory-screenshots.spec.ts"
      via: "screenshots npm script"
      pattern: "screenshots"
---

<objective>
Recapture the WordPress.org directory screenshots against the FINAL v1.2 editor UI — which
now includes the Phase 9 "Edit Mode" label / rename placeholder, the Phase 11 fixed
reorder/badge, and the Phase 11.2 icon-only unified toolbar (gray square buttons, semantic
colour, flat indicator glyphs, palette icon-picker glyph, back-arrow Exit). The current four
`.wordpress-org/screenshot-*.png` predate this UI and must be replaced.

Purpose: The live 1.2.0 listing must show what 1.2.0 actually ships; the current captures
show a pre-Phase-11.2 toolbar.
Output: A new MAESTRO_CAPTURE-gated `capture-directory-screenshots.spec.ts`, an extended
`screenshots` npm script, and regenerated `.wordpress-org/screenshot-N.png` — overwritten
live only after human visual review.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-release-assets-refresh/12-RESEARCH.md
@.planning/phases/12-release-assets-refresh/12-VALIDATION.md

# Model the new spec on this existing MAESTRO_CAPTURE-gated capture spec (auth, gating, committed PNGs):
@tests/e2e/specs/capture-screenshots.spec.ts

<interfaces>
<!-- The established capture pattern (from capture-screenshots.spec.ts) the executor must reuse. -->

- Gate at describe level (NOT just around screenshot calls — Pitfall 5):
    const CAPTURE = Boolean( process.env.MAESTRO_CAPTURE );
    test.describe( '...', () => {
      test.skip( !CAPTURE, 'Set MAESTRO_CAPTURE=1 (npm run screenshots) to regenerate.' );
      ...
    });
- Auth: inherits the shared storageState admin session from playwright.config.ts
  (global-setup logs in once). No bespoke login.
- Enter editor: page.goto('/wp-admin/index.php?maestro_edit=1'); wait on a Maestro-specific
  anchor (e.g. '.maestro-toolbar') before capturing — never a fixed sleep.
- Write each capture to BOTH destinations:
    .planning/phases/12-release-assets-refresh/screenshots/screenshot-N.png  (review artifact)
    .wordpress-org/screenshot-N.png                                          (wp.org publish target)

Current package.json "screenshots" script:
  "screenshots": "MAESTRO_CAPTURE=1 playwright test tests/e2e/specs/capture-screenshots.spec.ts tests/e2e/editor.spec.ts"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the MAESTRO_CAPTURE-gated directory screenshot capture spec and wire the npm script</name>
  <files>tests/e2e/specs/capture-directory-screenshots.spec.ts, package.json</files>
  <action>
Create `tests/e2e/specs/capture-directory-screenshots.spec.ts`, modeled on the existing
`capture-screenshots.spec.ts`. Requirements:

- Gate at the describe level with `test.skip( !CAPTURE, ... )` where
  `CAPTURE = Boolean(process.env.MAESTRO_CAPTURE)` — so a normal `npm run test:e2e` NEVER
  regenerates the PNGs (Pitfall 5). This is the critical hygiene fix that the Phase 7 specs
  lack.
- `beforeAll`: `fs.mkdirSync(SCREENSHOTS_DIR, { recursive: true })` for
  `.planning/phases/12-release-assets-refresh/screenshots`.
- Inherit the shared storageState admin auth (no bespoke login).
- Define a set of 4 screenshots (Claude's discretion on exact order/states; map to the
  existing caption intents so caption sync in 12-03 is clean), at 1440x980 viewport
  (current convention):
    1. Editor active, a top-level item selected, the icon-only unified toolbar + controls
       panel visible (shows the Phase 11.2 toolbar + Phase 9 "Edit Mode" indicator).
    2. The icon picker open (palette glyph) — Dashicons + bundled Bootstrap Icons tabs.
    3. Per-role visibility control open — hiding an item from selected roles.
    4. A renamed item showing the saved/auto-clearing "Saved" state (Phase 11.2).
  For each, wait on a Maestro-specific anchor (e.g. `.maestro-toolbar`, the panel, the picker)
  before `page.screenshot`.
- Write each screenshot to BOTH `.planning/phases/12-release-assets-refresh/screenshots/screenshot-N.png`
  AND `.wordpress-org/screenshot-N.png`.

Then extend the `screenshots` script in `package.json` to include the new spec, e.g.:
  "screenshots": "MAESTRO_CAPTURE=1 playwright test tests/e2e/specs/capture-screenshots.spec.ts tests/e2e/specs/capture-directory-screenshots.spec.ts tests/e2e/editor.spec.ts"

Do NOT run the capture here (Docker/wp-env required — see Task 2). Validate only that the spec
is correctly gated and parses, and that `package.json` is valid JSON.
  </action>
  <verify>
    <automated>node --check tests/e2e/specs/capture-directory-screenshots.spec.ts && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" && grep -q "test.skip" tests/e2e/specs/capture-directory-screenshots.spec.ts && grep -q "capture-directory-screenshots" package.json && echo "spec gated + script wired OK"</automated>
  </verify>
  <done>`capture-directory-screenshots.spec.ts` exists, is `test.skip`-gated at describe level, writes to both dirs; `package.json` `screenshots` script includes it; both files parse.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bring up wp-env, capture the screenshots, and visually review against the post-11.2 UI</name>
  <files>.wordpress-org/screenshot-1.png, .wordpress-org/screenshot-2.png, .wordpress-org/screenshot-3.png, .wordpress-org/screenshot-4.png</files>
  <action>Bring up wp-env (Docker/Colima, sandbox-disabled), run `npm run screenshots` to capture, then human reviews the planning-dir PNGs against the post-11.2 UI before the live `.wordpress-org/screenshot-*.png` are accepted. See &lt;how-to-verify&gt; for the exact steps (incl. the Phase 11.2 merge pre-flight and wp-env port handling).</action>
  <what-built>
A MAESTRO_CAPTURE-gated capture spec that drives the live editor and writes the four
directory screenshots to both the planning review dir and `.wordpress-org/`. Capture requires
a running wp-env (Docker/Colima) — the known Maestro test-execution sandbox gap — so this step
runs sandbox-disabled.
  </what-built>
  <how-to-verify>
PRE-FLIGHT (per 12-VALIDATION.md): confirm Phase 11.2 is merged to main (PR #50, confirmed
2026-06-22) so the editor shows the icon-only unified toolbar — otherwise the captures show
the old toolbar (Pitfall 4).

1. Start wp-env (Colima must be running). If port 8889 is taken by another wp-env project,
   start on an alternate tests port and pass `WP_ENV_TESTS_PORT` (per the Phase 11 gate note):
     `npx wp-env start`   (or on an alternate port if 8889 is busy)
2. Capture (runs the gated spec; sandbox-disabled, Docker required):
     `npm run screenshots`   (sets MAESTRO_CAPTURE=1)
3. Review the four captures:
     `open .planning/phases/12-release-assets-refresh/screenshots/screenshot-*.png`
   Confirm each shows the FINAL v1.2 UI: the Phase 11.2 icon-only unified toolbar (gray
   square buttons, semantic colour, flat indicator glyphs, palette icon-picker glyph,
   back-arrow Exit), the Phase 9 "Edit Mode" indicator, and that the captures are clear and
   well-framed. If a state is wrong, adjust the spec and re-run `npm run screenshots`.
4. When approved, confirm the same images now sit at `.wordpress-org/screenshot-1..4.png`
   (the spec already wrote both destinations).
5. Tear down when done: `npx wp-env stop`.
  </how-to-verify>
  <done>>=4 `.wordpress-org/screenshot-*.png` captured against the post-11.2 v1.2 UI, each <=10 MB, human-approved; final count recorded for 12-03 caption sync.</done>
  <verify>
    <automated>python3 -c "import os,glob; from PIL import Image;
fs=sorted(glob.glob('.wordpress-org/screenshot-*.png'));
assert len(fs)>=4, f'expected >=4 screenshots, found {len(fs)}';
[ (print(p, Image.open(p).size, os.path.getsize(p),'bytes'), os.path.getsize(p)<=10*1024*1024 or (_ for _ in ()).throw(AssertionError(p+' >10MB'))) for p in fs ];
print('screenshots present and within size limit')"</automated>
  </verify>
  <resume-signal>Type "approved" once the live `.wordpress-org/screenshot-*.png` are captured against the post-11.2 UI and reviewed, or describe what to adjust.</resume-signal>
</task>

</tasks>

<verification>
- `capture-directory-screenshots.spec.ts` is `test.skip`-gated at the describe level (a normal `npm run test:e2e` does NOT regenerate PNGs).
- `package.json` `screenshots` script runs the new spec.
- N (>=4) `.wordpress-org/screenshot-*.png` exist, each <=10 MB.
- Human visual review confirms the captures show the Phase 11.2 icon-only toolbar and the final v1.2 UI before live overwrite.
- Final screenshot COUNT is recorded for 12-03 caption sync.
</verification>

<success_criteria>
- New gated capture spec authored and wired into `npm run screenshots`.
- Directory screenshots recaptured against the final v1.2 editor UI (post Phase 9 + 11 + 11.2).
- `.wordpress-org/screenshot-N.png` replaced only after the visual-review checkpoint.
- The number of captured screenshots is known so 12-03 can sync the readme captions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-release-assets-refresh/12-02-SUMMARY.md`
(record the final screenshot count, the state each shows, and the wp-env port used).
</output>
