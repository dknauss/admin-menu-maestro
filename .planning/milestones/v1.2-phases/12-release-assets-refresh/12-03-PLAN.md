---
phase: 12-release-assets-refresh
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - readme.txt
files_uses:
  - .wordpress-org/banner-772x250.png
  - .wordpress-org/banner-1544x500.png
  - .wordpress-org/screenshot-1.png
  - .wordpress-org/screenshot-2.png
  - .wordpress-org/screenshot-3.png
  - .wordpress-org/screenshot-4.png
autonomous: true
requirements: [REL-07, REL-08]
must_haves:
  truths:
    - "The readme.txt == Screenshots == caption count exactly equals the number of .wordpress-org/screenshot-*.png files"
    - "Each caption describes the v1.2 UI shown in its corresponding screenshot (including the Phase 11.2 toolbar where relevant)"
    - "Both banners are exactly 772x250 / 1544x500 and <=4 MB"
    - "Each screenshot is <=10 MB"
    - "The full e2e suite is green (zero asset-caused regression); the capture spec is skipped on a normal run"
  artifacts:
    - path: "readme.txt"
      provides: "== Screenshots == captions synced to the recaptured set"
      contains: "== Screenshots =="
  key_links:
    - from: "readme.txt"
      to: ".wordpress-org/screenshot-*.png"
      via: "one numbered caption per screenshot file (count must match)"
      pattern: "== Screenshots =="
---

<objective>
Close out Phase 12: sync the `== Screenshots ==` captions in `readme.txt` to the recaptured
v1.2 screenshot set (caption count == screenshot count, copy that explains the post-11.2
interface/workflow), then run the final deterministic consistency + zero-regression gate over
both REL-07 and REL-08 assets before the 1.2.0 cut.

Purpose: The directory page caption list must match the new screenshot set in number and
content; and the assets-only phase must prove it introduced no code regression.
Output: Updated `readme.txt` captions and a green final gate.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-release-assets-refresh/12-VALIDATION.md

# The recaptured set + count come from 12-02; the captions live here:
@readme.txt
@.planning/phases/12-release-assets-refresh/12-02-SUMMARY.md

<interfaces>
<!-- Current readme.txt == Screenshots == block (4 captions, pre-v1.2 UI) — the starting point. -->
1. Editing the admin menu in place — the Posts item selected, with the shared controls panel (rename, icon, visibility, reset) open.
2. The icon picker: searchable Dashicons and bundled Bootstrap Icons tabs for swapping a top-level admin menu icon.
3. Per-role visibility — hiding an admin menu item from selected roles (a cosmetic declutter, not access control).
4. A renamed admin menu item, saved automatically by debounced autosave.

The number of captions MUST equal the number of .wordpress-org/screenshot-*.png files
produced by 12-02. If 12-02 captured a different count, add/remove captions to match.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync the readme.txt == Screenshots == captions to the recaptured set</name>
  <files>readme.txt</files>
  <action>
Read `12-02-SUMMARY.md` for the final screenshot count and what each capture shows. Update the
`== Screenshots ==` block in `readme.txt` so:

- The number of numbered captions exactly equals the number of
  `.wordpress-org/screenshot-*.png` files (validation: caption count == file count).
- Each caption matches the state its screenshot now shows, reflecting the v1.2 UI. In
  particular, where a capture shows the Phase 11.2 icon-only unified toolbar or the
  auto-clearing "Saved" state, the caption should read accordingly (e.g. caption 4 maps the
  existing "saved automatically by debounced autosave" intent onto the new auto-clearing
  "Saved -> idle" behavior).
- Keep captions as workflow explanations ("explain the interface or workflow"), consistent in
  tone with the current copy.

Follow the project markdown authoring rules (no backslash escapes inside code/fences). Touch
ONLY the `== Screenshots ==` section; do not alter unrelated readme copy.
  </action>
  <verify>
    <automated>python3 -c "import re,glob;
txt=open('readme.txt',encoding='utf-8').read();
m=re.search(r'== Screenshots ==\\n(.*?)\\n==',txt,re.S);
assert m, 'Screenshots section not found';
caps=[l for l in m.group(1).splitlines() if re.match(r'^\\s*\\d+\\.',l)];
files=glob.glob('.wordpress-org/screenshot-*.png');
assert len(caps)==len(files), f'caption count {len(caps)} != screenshot count {len(files)}';
print(f'caption/screenshot count match: {len(caps)}')"</automated>
  </verify>
  <done>`== Screenshots ==` has exactly one caption per `.wordpress-org/screenshot-*.png`, each describing the v1.2 UI shown.</done>
</task>

<task type="auto">
  <name>Task 2: Final deterministic asset gate + zero-regression e2e suite</name>
  <files>readme.txt</files>
  <action>
Run the full deterministic asset-consistency gate and the zero-regression suite to confirm
this assets-only phase introduced no code regression and all REL-07/REL-08 acceptance gates
hold:

1. Banner gate: both banners exactly 772x250 / 1544x500 and <=4 MB.
2. Screenshot gate: each `.wordpress-org/screenshot-*.png` <=10 MB; caption count == file count.
3. Zero-regression e2e: run the full suite (sandbox-disabled, Docker — same gate path as prior
   phases). Confirm the new `capture-directory-screenshots.spec.ts` is SKIPPED on this run
   (not regenerating PNGs) and that the suite is green. If wp-env is not available in this
   step, defer the e2e run to the phase wave boundary and note it in the SUMMARY — the
   deterministic asset checks (1 + 2) still gate here.

This task makes no code change; it is the consolidation gate. If any check fails, fix the
offending caption/asset (loop back to the owning plan if an asset must be regenerated).
  </action>
  <verify>
    <automated>python3 -c "import os,glob,re;
from PIL import Image;
b=[('.wordpress-org/banner-772x250.png',(772,250)),('.wordpress-org/banner-1544x500.png',(1544,500))];
[ (Image.open(p).size==e or (_ for _ in ()).throw(AssertionError(p)), os.path.getsize(p)<=4*1024*1024 or (_ for _ in ()).throw(AssertionError(p+' >4MB'))) for p,e in b ];
sc=glob.glob('.wordpress-org/screenshot-*.png');
[ os.path.getsize(p)<=10*1024*1024 or (_ for _ in ()).throw(AssertionError(p+' >10MB')) for p in sc ];
txt=open('readme.txt',encoding='utf-8').read();
caps=[l for l in re.search(r'== Screenshots ==\\n(.*?)\\n==',txt,re.S).group(1).splitlines() if re.match(r'^\\s*\\d+\\.',l)];
assert len(caps)==len(sc), f'caption {len(caps)} != screenshot {len(sc)}';
print(f'ALL deterministic gates green: 2 banners OK, {len(sc)} screenshots, {len(caps)} captions')"</automated>
  </verify>
  <done>All deterministic asset gates green (banner dimensions/size, screenshot size, caption==count); full e2e suite green (or e2e deferred to wave boundary with the capture spec confirmed skipped).</done>
</task>

</tasks>

<verification>
- `readme.txt` `== Screenshots ==` caption count == `.wordpress-org/screenshot-*.png` count.
- Both banners 772x250 / 1544x500, <=4 MB; each screenshot <=10 MB.
- Full e2e suite green; `capture-directory-screenshots.spec.ts` skipped on a normal run.
- No plugin code changed — assets + readme captions only.
</verification>

<success_criteria>
- REL-07 and REL-08 assets are consistent and within wp.org constraints.
- readme captions match the recaptured screenshot set in number and content.
- Zero regressions; the listing reflects what 1.2.0 ships, ready for the release cut.
</success_criteria>

<output>
After completion, create `.planning/phases/12-release-assets-refresh/12-03-SUMMARY.md`
and confirm REL-07 + REL-08 can be flipped to Complete in the v1.2 traceability table.
</output>
