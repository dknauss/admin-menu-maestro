---
phase: 12-release-assets-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .wordpress-org/source/build_final.py
  - .wordpress-org/banner-772x250.png
  - .wordpress-org/banner-1544x500.png
autonomous: false
requirements: [REL-07]
must_haves:
  truths:
    - "The banner is regenerated from build_final.py via the REL-06 pipeline (not hand-edited)"
    - "The MAESTRO wordmark, the subtitle, the tagline, and the gold underline rule occupy approximately the same horizontal measure"
    - "banner-772x250.png is exactly 772x250 and banner-1544x500.png is exactly 1544x500"
    - "Each banner PNG is under the 4 MB wp.org limit"
    - "The live .wordpress-org/banner-*.png are overwritten only after explicit human visual review"
  artifacts:
    - path: ".wordpress-org/source/build_final.py"
      provides: "Tagline auto-fit constrained to the wordmark width (ww), not the full column (maxw)"
      contains: "ww"
    - path: ".wordpress-org/banner-772x250.png"
      provides: "Standard wp.org banner at 772x250"
    - path: ".wordpress-org/banner-1544x500.png"
      provides: "Retina wp.org banner at 1544x500"
  key_links:
    - from: ".wordpress-org/source/build_final.py"
      to: ".wordpress-org/banner-*.png"
      via: "npm run assets:banners (Inkscape render + Pillow LANCZOS downscale)"
      pattern: "assets:banners"
---

<objective>
Refresh the WordPress.org/GitHub banner to the REL-07 design goal: the MAESTRO wordmark,
the "THE INLINE ADMIN MENU EDITOR" subtitle, the tagline, and the gold underline rule all
occupy approximately the same horizontal measure (balanced to a common width), rather than
today's mismatched line widths.

Purpose: The live directory banner is the first impression of the 1.2.0 listing; the current
tagline runs wider than the wordmark/subtitle/rule, reading as unbalanced.
Output: An edited `build_final.py` (tagline fit constrained to the wordmark width) and
regenerated `.wordpress-org/banner-772x250.png` + `banner-1544x500.png`, replaced live only
after human visual review.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-release-assets-refresh/12-RESEARCH.md
@.planning/phases/12-release-assets-refresh/12-VALIDATION.md

# The single source of truth for the banner layout (in-code SVG master + Pillow compositor):
@.wordpress-org/source/build_final.py

<interfaces>
<!-- Key build_final.py layout facts the executor needs. Extracted from the script. -->
<!-- All coordinates are in 2x pixel space (S=2; master is 3088x1000, downscaled to 1x). -->

Line 125:  tx=512*S; maxw=(1486-512)*S      # text column left edge; full column width (~974px at 1x)
Line 132:  if word.size[0]<=maxw or size<=110*S: break   # MAESTRO wordmark auto-fit
Line 134:  ww,wh=word.size                   # ww = rendered MAESTRO wordmark pixel width (the common measure)
Line 137:  tag="Orchestrate your menu in place, inside the dashboard."   # 54-char tagline
Line 140:  while dr.textlength(tag,font=ftag)>maxw and ts>20*S:          # <-- TAGLINE auto-fit loop (CHANGE POINT)
Line 141:      ts-=1; ftag=fnt(f"{FD}/Poppins-Light.ttf",ts)
Line 151:  fit=ww if desc=="below" else maxw # subtitle ALREADY constrained to ww
Line 176:  word_right=tx+ww                  # gold rule right edge ALREADY anchored to ww
Line 182:  dr.line([(tx+2*S,ry),(word_right-2*S,ry)],...)               # gold underline rule
Line 187:  dr.text((tx,y-tb[1]),tag,...)     # tagline draw; x-start already tx (correct, left-aligned)
Line 189:  suf=os.environ.get("VARIANT_SUFFIX","")                      # staging suffix wiring

Subtitle (fit=ww) and gold rule (word_right=tx+ww) are ALREADY keyed to ww.
ONLY the tagline loop (line 140) fits to maxw — that is the entire source of the mismatch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constrain the tagline auto-fit to the wordmark width and stage a comparison variant</name>
  <files>.wordpress-org/source/build_final.py</files>
  <action>
In `build_final.py`, change the tagline auto-fit loop condition (line ~140) so the tagline is
fit to the MAESTRO wordmark width `ww` instead of the full column width `maxw`:

    while dr.textlength(tag,font=ftag)>maxw and ts>20*S:
    # becomes:
    while dr.textlength(tag,font=ftag)>ww and ts>20*S:

This is the ONLY substitution needed — the subtitle (line 151, `fit=ww`) and the gold rule
(line 176, `word_right=tx+ww`) are already keyed to `ww`. Do NOT hardcode `ww`; it must stay
derived from the live `word.size[0]` render so the gold rule and tagline track together
(Pitfall 2 in 12-RESEARCH.md). Leave the tagline draw x-start (line 187, `tx`) unchanged —
it is already left-aligned to the text column, so all four elements share the same left edge.

Then stage a non-destructive comparison variant WITHOUT touching the live assets:

    VARIANT_SUFFIX=-v2 npm run assets:banners

This writes `.wordpress-org/banner-772x250-v2.png` and `banner-1544x500-v2.png`.

FALLBACK (if the tagline auto-fits to a noticeably small font at the `ww` constraint —
Pitfall 1): shorten the tagline string on line 137 to a shorter phrase such as
"Edit your admin menu, in place." and re-stage with `VARIANT_SUFFIX=-v2`. Note which tagline
you used in the SUMMARY so the visual-review checkpoint knows what to judge.

Do NOT run `npm run assets:banners` without a suffix yet — that would overwrite the live
assets before review (Pitfall 3).
  </action>
  <verify>
    <automated>VARIANT_SUFFIX=-v2 npm run assets:banners && python3 -c "from PIL import Image; assert Image.open('.wordpress-org/banner-772x250-v2.png').size==(772,250); assert Image.open('.wordpress-org/banner-1544x500-v2.png').size==(1544,500); print('staged variant dimensions OK')"</automated>
  </verify>
  <done>`build_final.py` tagline loop fits to `ww`; staged `banner-*-v2.png` exist at exactly 772x250 and 1544x500; live banners untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual review of the staged banner, then overwrite the live assets</name>
  <files>.wordpress-org/banner-772x250.png, .wordpress-org/banner-1544x500.png</files>
  <action>Human reviews the staged `banner-*-v2.png` variants, then (on approval) the executor runs `npm run assets:banners` (no suffix) to overwrite the live banners and removes the `-v2` staging files. See &lt;how-to-verify&gt; for the exact review steps.</action>
  <what-built>
A regenerated banner where the tagline is now constrained to the MAESTRO wordmark width, so
the wordmark, subtitle, tagline, and gold underline rule share approximately one horizontal
measure. Staged non-destructively as `banner-772x250-v2.png` / `banner-1544x500-v2.png`.
  </what-built>
  <how-to-verify>
1. Open both staged variants:
   `open .wordpress-org/banner-772x250-v2.png .wordpress-org/banner-1544x500-v2.png`
2. Confirm the four elements (MAESTRO wordmark, "THE INLINE ADMIN MENU EDITOR" subtitle, the
   tagline, the gold underline rule) all end at roughly the same right edge — balanced to a
   common measure, not the previous mismatched widths.
3. Confirm the tagline font is not uncomfortably small. If it is, ask the executor to apply
   the shortened-tagline fallback from Task 1 and re-stage.
4. Compare against the current live `banner-772x250.png` to confirm it is an improvement.
5. When approved, the executor overwrites the live assets:
   `npm run assets:banners`  (no suffix -> writes banner-772x250.png + banner-1544x500.png)
   then removes the staging variants:
   `rm .wordpress-org/banner-772x250-v2.png .wordpress-org/banner-1544x500-v2.png`
  </how-to-verify>
  <done>Live `.wordpress-org/banner-772x250.png` + `banner-1544x500.png` overwritten at exact dimensions, <=4 MB, human-approved as balanced; `-v2` staging files removed.</done>
  <verify>
    <automated>python3 -c "import os; from PIL import Image;
checks=[('.wordpress-org/banner-772x250.png',(772,250)),('.wordpress-org/banner-1544x500.png',(1544,500))];
[ (lambda p,e: (print(p, Image.open(p).size, os.path.getsize(p),'bytes'), Image.open(p).size==e or (_ for _ in ()).throw(AssertionError(p)), os.path.getsize(p)<=4*1024*1024 or (_ for _ in ()).throw(AssertionError(p+' >4MB'))) )(p,e) for p,e in checks ];
import glob; assert not glob.glob('.wordpress-org/banner-*-v2.png'), 'staging variants must be removed'; print('live banners OK and staging cleaned')"</automated>
  </verify>
  <resume-signal>Type "approved" once the live banners are overwritten and the -v2 staging files removed, or describe what to adjust.</resume-signal>
</task>

</tasks>

<verification>
- Both live banners are exactly 772x250 and 1544x500 (Pillow dimension check).
- Each live banner PNG is under 4 MB.
- The change is in `build_final.py` (regenerated from source), not a hand-edited PNG.
- No `banner-*-v2.png` staging files remain.
- Human visual review confirms the balanced common-measure design before live overwrite.
</verification>

<success_criteria>
- `build_final.py` tagline auto-fit is constrained to `ww` (the wordmark width).
- `.wordpress-org/banner-772x250.png` and `banner-1544x500.png` regenerated, exact dimensions, <=4 MB.
- The four banner elements share approximately one horizontal measure (human-approved).
- REL-07 design goal met; live assets replaced only after the visual-review checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/12-release-assets-refresh/12-01-SUMMARY.md`
(note the final tagline string used, and the visual-review outcome).
</output>
