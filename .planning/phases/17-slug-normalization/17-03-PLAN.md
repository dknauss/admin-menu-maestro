---
phase: 17-slug-normalization
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - readme.txt
  - README.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "The full PHP unit + integration + Playwright e2e suites are green at the Phase 17 head commit"
    - "WPCS (phpcs) is clean and PHPStan reports 0 errors"
    - "Plugin Check reports 0 errors on the shippable source"
    - "The new ReplayTest integration cases (host/ver/UTM/&amp;/collision) pass under Docker"
    - "A short user-facing changelog note records that saved overrides now survive host moves, ver= bumps, UTM drift, and &amp; taxonomy slugs"
  artifacts:
    - path: "readme.txt"
      provides: "Draft v1.3.0 changelog note (finalized in Phase 18)"
      contains: "1.3.0"
  key_links:
    - from: "Phase 17 head commit"
      to: "green full suite + Plugin Check 0 errors"
      via: "wave-boundary gate run (Docker, sandbox-disabled)"
      pattern: "test:e2e"
---

<objective>
The Phase 17 zero-regression gate (locked success criterion #5). Run the full
suite end-to-end at the Wave-3 boundary — PHP unit + integration + Playwright
e2e + WPCS + PHPStan + Plugin Check — proving the normalization change holds at
0 regressions across the whole plugin. Then draft the short user-facing v1.3.0
changelog note (final release copy lands in Phase 18).

Purpose: The normalization seam changes the matching basis for ALL items at
once (CONTEXT: "wider blast radius"); the full suite + collision-guard is the
safety net. Integration + E2E run only at the wave boundary because Docker /
sandbox blocks per-task E2E (established Phase 9 / 11.1 pattern, STATE.md).

Output: a green-suite gate record (in the SUMMARY) and a drafted changelog note.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-slug-normalization/17-CONTEXT.md
@.planning/phases/17-slug-normalization/17-VALIDATION.md
@.planning/phases/17-slug-normalization/17-02-SUMMARY.md

# Changelog targets.
@readme.txt
@README.md

<gate_notes>
<!-- Docker / wp-env boot notes carried from STATE.md so the gate run does not
     stall on environment quirks. -->
- This gate needs Docker (wp-env) for integration + e2e. Run sandbox-DISABLED.
- Port contention: if 8889 is held by another wp-env project, start this stack
  on an alternate port and pass `WP_ENV_TESTS_PORT=<port>` (the 11-08 config
  change supports this); the playwright config + global-setup login honor it.
- Full suite command (from 17-VALIDATION.md):
  `composer test:unit && composer lint && composer analyse:phpstan && npm run test:js && npm run env:start && npm run test:php && npm run test:e2e`
- Plugin Check runs against the shippable source (same invocation as the v1.2
  release pipeline / Phase 9 sign-off). 0 errors required.
- Phase-07 e2e screenshot specs overwrite committed PNGs on a full e2e run
  (known hygiene issue, STATE.md). Revert any incidental PNG churn before the
  gate sign-off so the gate commit carries only intended changes.
</gate_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite zero-regression gate (unit + integration + e2e + WPCS + PHPStan + Plugin Check)</name>
  <files></files>
  <action>
    Run sandbox-disabled (Docker required). Bring up wp-env (honor an alternate
    `WP_ENV_TESTS_PORT` if 8889 is taken), then run the full suite:
      1. `composer test:unit` — PHP unit (incl. SlugTest, OrderingTest).
      2. `composer lint` — WPCS clean.
      3. `composer analyse:phpstan` — 0 errors.
      4. `npm run test:js` — JS logic suite.
      5. `npm run env:start && npm run test:php` — PHP integration, INCLUDING the
         new ReplayTest normalization cases (host/ver/UTM/&amp;/sub_order/collision).
      6. `npm run test:e2e` — Playwright e2e.
      7. Plugin Check against shippable source — 0 errors.

    If any step fails, fix the regression in the owning file and re-run that step
    (the normalization change is the prime suspect for any integration/e2e break;
    treat a real product break as the fix target, not the assertion). Revert any
    incidental Phase-07 screenshot PNG churn before sign-off. Record the final
    pass counts (unit N/N, integration N/N, js N/N, e2e N/N, phpcs clean, PHPStan
    0, Plugin Check 0) for the SUMMARY.

    This task makes no source change unless a regression must be fixed; if a fix
    is needed, commit it as `fix(17-03): <regression>`. Otherwise no commit here.
  </action>
  <verify>
    <automated>composer test:unit && composer lint && composer analyse:phpstan && npm run test:js</automated>
  </verify>
  <done>All seven gate steps pass: PHP unit + integration (incl. new ReplayTest normalization cases) + JS + Playwright e2e green, WPCS clean, PHPStan 0 errors, Plugin Check 0 errors. Pass counts recorded for the SUMMARY. No unintended file churn (Phase-07 PNGs reverted).</done>
</task>

<task type="auto">
  <name>Task 2: Draft the v1.3.0 user-facing changelog note</name>
  <files>readme.txt, README.md</files>
  <action>
    Add a short, user-facing draft note under a `1.3.0` changelog heading in
    readme.txt (and mirror in README.md if it carries a changelog/feature list):
    saved menu overrides now keep applying across site host moves, plugin-version
    (`ver=`) bumps, UTM-link drift, and `&amp;`-encoded taxonomy slugs — no manual
    re-save needed. Keep it to 1-3 plain-language bullet lines (no internal terms
    like "normalize seam" / "denylist"). Mark it draft-for-1.3.0; final release
    copy + version bump land in Phase 18 — do NOT bump the plugin Version header
    or Stable tag here.

    Commit: `docs(17-03): draft v1.3.0 slug-resilience changelog note`.
  </action>
  <verify>
    <automated>grep -q "1.3.0" readme.txt && echo "changelog note present"</automated>
  </verify>
  <done>readme.txt carries a 1.3.0 changelog note in user-facing language covering host moves, ver= bumps, UTM drift, and &amp; slugs; no version/Stable-tag bump (deferred to Phase 18).</done>
</task>

</tasks>

<verification>
- Full suite green at the Phase 17 head: unit + integration + js + e2e, WPCS
  clean, PHPStan 0, Plugin Check 0 errors.
- New ReplayTest normalization cases passed under Docker.
- readme.txt has a user-facing 1.3.0 changelog note; no premature version bump.
- No unintended working-tree churn at sign-off (Phase-07 PNGs reverted).
</verification>

<success_criteria>
- Locked success criterion #5 met: all existing PHP unit + integration +
  Playwright e2e suites green, WPCS clean, Plugin Check 0 errors — with the
  normalization change in place.
- The three FIX behaviors are proven end-to-end (unit data provider + Replay
  integration), and the collision-guard proves conservatism.
- A user-facing changelog note is drafted for the Phase 18 release.
</success_criteria>

<output>
After completion, create `.planning/phases/17-slug-normalization/17-03-SUMMARY.md`
recording the gate pass counts (unit/integration/js/e2e, phpcs, PHPStan, Plugin
Check), the wp-env port used, any regression fixed, and the changelog draft —
and noting Phase 17 is ready for /gsd:verify-work then the Phase 18 release.
</output>
