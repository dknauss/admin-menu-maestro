---
phase: 09-editor-ux-polish
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
autonomous: true
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
requirements: [UX-03, UX-04, UX-07]
user_setup: []

must_haves:
  truths:
    - "The full zero-regression bar holds at phase close: JS logic green (npm run test:js), PHP unit 44/44 (composer test:unit), integration 29/29 + new localization assertions (composer test:integration), Playwright e2e green (npm run test:e2e), composer lint clean, and Plugin Check 0 errors"
    - "All three Phase 9 requirements are demonstrably satisfied end to end: UX-03 (short Edit Mode indicator with dashicon + first-run pulse), UX-04 (rename placeholder + accessible name), UX-07 (mobile density + 44px floor)"
    - "ROADMAP.md and REQUIREMENTS.md traceability for UX-03/UX-04/UX-07 are flipped to Complete with the Edit-Mode reconciliation note recorded"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 marked complete; UX-03/04/07 plan checkboxes ticked"
      contains: "UX-03"
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.2 traceability rows flipped to Complete"
      contains: "UX-07"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "Phase 9 success criteria"
      via: "criteria verified against the full green suite"
      pattern: "UX-03"
---

<objective>
Phase 9 zero-regression gate and sign-off: run the full suite, confirm all three
requirements (UX-03, UX-04, UX-07) hold end to end with no regression, and flip the
ROADMAP/REQUIREMENTS traceability to Complete.

Purpose: Plans 01–05 land the behavior; this plan proves the LOCKED zero-regression
bar still holds across every layer and records completion. New tests added across the
phase are additive — they raise counts, never lower the passing set.

Output: a clean full-suite run, Plugin Check 0 errors, and ROADMAP/REQUIREMENTS updates
marking Phase 9 done. Depends on all prior Phase 9 plans.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-editor-ux-polish/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full zero-regression suite + Plugin Check [model: sonnet]</name>
  <files></files>
  <action>
    model: sonnet — read-only verification, run the full suite to the explicit checklist.

    Run, in order, and confirm each is green (this is the LOCKED zero-regression bar):
    - `npm run test:js` — JS logic suites (Phase 6 + the 3 new Phase 9 seams) green
    - `composer test:unit` — PHP unit 44/44
    - `composer test:integration` — integration 29/29 + the new modeLabel/renamePlaceholder
      localization assertions
    - `npm run test:e2e` — Playwright green (mode label, idle ::before, first-run pulse,
      rename accessible-name/placeholder, 44px tap targets, no overflow/overlap at 700px)
    - `composer lint` — clean
    - Plugin Check — 0 errors (run the project's Plugin Check command/path as in prior phases;
      see Phase 7/8 SUMMARY for the exact invocation)

    If any check is red, STOP and report which layer regressed — do not flip traceability over
    a red suite. New tests are additive; counts may rise (e.g. integration localization
    assertions) but the passing set must never shrink.

    No commit for this task unless the Plugin Check run produces a build artifact to clean up;
    this is verification.
  </action>
  <verify>
    <automated>npm run test:js && composer test:unit && composer test:integration && composer lint</automated>
  </verify>
  <done>JS logic, PHP unit 44/44, integration (29/29 + new localization assertions), e2e, and lint are all green; Plugin Check reports 0 errors; any regression is reported instead of signed off.</done>
</task>

<task type="auto">
  <name>Task 2: Flip ROADMAP + REQUIREMENTS traceability to Complete [model: sonnet]</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    model: sonnet — mechanical doc update once the suite is green.

    .planning/ROADMAP.md (Phase 9 section): mark Phase 9 complete in the Progress table
    (Status Complete, today's date), tick the plan checkboxes under "Plans:" for 09-01..09-06,
    and confirm the success-criteria are met. Record the reconciliation explicitly: the idle
    copy is "Edit Mode" (not the literal "Menu Edit Mode" in criterion #1) by the user's LOCKED
    refinement, satisfying the intent (short, glanceable, non-colour-signalled) — same pattern
    as Phase 8 / REL-06.

    .planning/REQUIREMENTS.md (Traceability (v1.2) table): flip UX-03, UX-04, UX-07 from Pending
    to Complete. Update the v1.2 coverage note if needed. Leave V2-16/Phase 10 untouched.

    Commit: `docs(09): mark Phase 9 (UX-03/UX-04/UX-07) complete; record Edit Mode reconciliation`.
  </action>
  <verify>
    <automated>grep -q "UX-03" .planning/REQUIREMENTS.md && grep -q "UX-07" .planning/ROADMAP.md && node -e "const fs=require('fs');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const sec=r.split('Traceability (v1.2)')[1]||'';for(const id of ['UX-03','UX-04','UX-07']){const line=sec.split('\n').find(l=>l.includes(id));if(line&&/Pending/.test(line))throw new Error(id+' still Pending in v1.2 traceability');}process.exit(0)"</automated>
  </verify>
  <done>ROADMAP Phase 9 is Complete with the Edit-Mode reconciliation note; REQUIREMENTS v1.2 traceability shows UX-03/UX-04/UX-07 Complete; no other rows changed.</done>
</task>

</tasks>

<verification>
- Full suite green: `npm run test:js && composer test:unit && composer test:integration && npm run test:e2e`
- `composer lint` clean; Plugin Check 0 errors
- ROADMAP + REQUIREMENTS reflect Phase 9 complete with the reconciliation recorded
</verification>

<success_criteria>
- Zero-regression bar holds across every layer; new tests additive only
- UX-03, UX-04, UX-07 satisfied end to end and marked Complete in traceability
- Edit Mode vs Menu Edit Mode reconciliation recorded at sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/09-editor-ux-polish/09-06-SUMMARY.md`
</output>
