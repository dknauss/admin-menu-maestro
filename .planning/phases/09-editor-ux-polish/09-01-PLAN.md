---
phase: 09-editor-ux-polish
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/js/mode-status.test.mjs
  - tests/js/first-run-gate.test.mjs
  - tests/js/placeholder.test.mjs
  - assets/maestro-logic.js
autonomous: true
requirements: [UX-03, UX-04]
user_setup: []

must_haves:
  truths:
    - "A pure modeStatusLabel(state, strings) maps a save-state ('idle'|'saving'|'saved'|'error') to the transient save-status text, returning '' for idle so the save-status element is empty (the persistent 'Edit Mode' mode label is NEVER produced by this function)"
    - "A pure firstRunSeen(storage) returns true/false from a localStorage-like stub, and returns true (treat as seen, suppress cue) when storage.getItem throws"
    - "A pure placeholderVisible(value) returns true only when the rename value is empty or whitespace-only (mirrors commitRename's raw.trim() rule)"
    - "All three helpers are exported from assets/maestro-logic.js via the existing dual-export guard (window.maestroLogic in the browser, module.exports under node:test) with zero new runtime dependencies"
    - "Each helper's test was committed failing (RED in the working tree) before its implementation, and npm run test:js is green after implementation"
  artifacts:
    - path: "tests/js/mode-status.test.mjs"
      provides: "node:test coverage for modeStatusLabel across idle/saving/saved/error"
      contains: "modeStatusLabel"
    - path: "tests/js/first-run-gate.test.mjs"
      provides: "node:test coverage for firstRunSeen incl. absent/'1'/other/throws"
      contains: "firstRunSeen"
    - path: "tests/js/placeholder.test.mjs"
      provides: "node:test coverage for placeholderVisible empty/whitespace/non-empty"
      contains: "placeholderVisible"
    - path: "assets/maestro-logic.js"
      provides: "modeStatusLabel, firstRunSeen, placeholderVisible added to the api object"
      contains: "modeStatusLabel"
  key_links:
    - from: "tests/js/mode-status.test.mjs"
      to: "assets/maestro-logic.js"
      via: "createRequire import of the dual-export CJS api"
      pattern: "maestro-logic"
    - from: "assets/maestro-logic.js"
      to: "window.maestroLogic"
      via: "browser dual-export guard consumed by maestro.js in later plans"
      pattern: "maestroLogic"
---

<objective>
Establish the Phase 9 TDD seam: build the three pure helpers test-first that the
UX-03 and UX-04 implementation plans stand on — modeStatusLabel (save-state →
text), firstRunSeen (localStorage gate), and placeholderVisible (empty-vs-filled).

Purpose: Per the LOCKED TDD directive (global CLAUDE.md + roadmap criterion #4),
every behavioral JS change with a pure/observable contract is red-first. Writing
these helpers and their failing tests now means the DOM-glue plans (02, 03, 04)
consume already-green, single-source-of-truth logic instead of inlining and
re-deriving it.

Output: Three new node:test specs under tests/js/, and three new functions appended
to assets/maestro-logic.js's existing api object via the established dual-export
guard. Zero new runtime dependencies; tests/js/ is build-excluded; assets/ ships.
</objective>

<tdd_seam_decision>
DECISION (required by CONTEXT.md + 09-RESEARCH.md "Pure/Observable Logic Seams"):
extend the Phase 6 node:test seam with three new pure helpers. Justification:

1. All three are intrinsically frontend and pure: modeStatusLabel is a state→string
   map; firstRunSeen reads an injected storage stub; placeholderVisible is a trim
   check. Each is writable as `expect(fn(input)).toBe(output)` BEFORE implementation,
   so each qualifies for red-first TDD.
2. assets/maestro-logic.js already exists (Phase 6) with the dual-export guard and is
   already enqueued as the `maestro-logic` dependency of `maestro`. We EXTEND its
   `api` object — no new file, no new enqueue, no build step.
3. node:test + node:assert are Node built-ins (zero devDependency); npm run test:js is
   already wired in directory form.

placeholderVisible is INCLUDED as a unit seam (not skipped) because it pins the
"whitespace-only counts as empty" contract to commitRename's trim() rule — a real
decision worth a regression guard, and cheap.

Test-blocking commit gate note (global TDD rule): if a pre-commit hook re-runs the
suite and rejects red commits, keep the RED step in the WORKING TREE (write the
failing test, run it, watch it fail) and commit test+impl together as ONE GREEN
commit per helper. Use separate test:/feat: commits only if no such gate is active.
</tdd_seam_decision>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-editor-ux-polish/09-CONTEXT.md
@.planning/phases/09-editor-ux-polish/09-RESEARCH.md
@.planning/phases/09-editor-ux-polish/09-VALIDATION.md

# The file to EXTEND — read it first; mirror its existing dual-export + JSDoc style:
@assets/maestro-logic.js
# The existing node:test pattern to copy (createRequire interop, assert usage):
@tests/js/modified-diff.test.mjs

<interfaces>
<!-- Exact contracts from 09-RESEARCH.md. Implement these verbatim. -->

modeStatusLabel( state, strings ) -> string
  'saving' -> strings.saving
  'saved'  -> strings.saved
  'error'  -> strings.saveError
  anything else ('idle') -> ''        // save-status element shows nothing at idle
  // The persistent "Edit Mode" mode label is built in the DOM (Plan 02), NOT here.

firstRunSeen( storage ) -> boolean
  storage.getItem('maestroFirstRunDone') === '1'  -> true
  null / any other value                          -> false
  storage.getItem throws                          -> true  (treat as seen; suppress cue)

placeholderVisible( value ) -> boolean
  value.trim() === ''  -> true   (empty or whitespace-only -> placeholder shows)
  otherwise            -> false

Existing api object to extend (assets/maestro-logic.js, end of file):
```js
var api = { reorderMove: reorderMove, diffItem: diffItem, resetItem: resetItem };
```
Add the three new functions to THIS object — do NOT duplicate the export guard.

createRequire interop already used by tests/js/*.test.mjs:
```js
import { createRequire } from 'node:module';
const require = createRequire( import.meta.url );
const { modeStatusLabel } = require( '../../assets/maestro-logic.js' );
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD modeStatusLabel — failing test first, then implement (UX-03) [model: sonnet]</name>
  <files>tests/js/mode-status.test.mjs, assets/maestro-logic.js</files>
  <behavior>
    modeStatusLabel(state, strings) -> string (pure):
    - Test: 'idle'   -> '' (empty: save-status element renders nothing)
    - Test: 'saving' -> strings.saving
    - Test: 'saved'  -> strings.saved
    - Test: 'error'  -> strings.saveError
    - Test: an unknown state (e.g. 'frobnicate') -> '' (defaults to empty, never throws)
    Use a strings stub: { saving: 'Saving…', saved: 'Saved', saveError: 'Save failed. Retrying on next change.' }
  </behavior>
  <action>
    model: sonnet — write the test from the explicit assertions above, then the
    minimal implementation.

    RED first: create tests/js/mode-status.test.mjs using node:test + node:assert,
    importing modeStatusLabel via the createRequire interop (copy the import block from
    tests/js/modified-diff.test.mjs). Run `node --test tests/js/mode-status.test.mjs`
    and watch it FAIL (function missing).

    GREEN: add modeStatusLabel(state, strings) to assets/maestro-logic.js — add the
    function above the `var api = {...}` block AND add `modeStatusLabel: modeStatusLabel`
    to the existing api object (do NOT add a second export guard). Implementation per
    the interface contract: return strings.saving/saved/saveError for those states,
    else return ''. Run the test — MUST pass.

    Commit (one GREEN commit, test+impl together if the pre-commit gate rejects red;
    otherwise test: then feat:): `feat(09-01): pure modeStatusLabel save-state mapping`.
  </action>
  <verify>
    <automated>node --test tests/js/mode-status.test.mjs</automated>
  </verify>
  <done>mode-status.test.mjs covers idle→''/saving/saved/error/unknown and passes; assets/maestro-logic.js exports modeStatusLabel via the single existing dual-export guard; no new devDependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD firstRunSeen + placeholderVisible — failing tests first, then implement (UX-03, UX-04) [model: sonnet]</name>
  <files>tests/js/first-run-gate.test.mjs, tests/js/placeholder.test.mjs, assets/maestro-logic.js</files>
  <behavior>
    firstRunSeen(storage) -> boolean (pure; storage is an injected stub):
    - Test: { getItem: () => null }      -> false  (key absent: not seen)
    - Test: { getItem: () => '1' }       -> true   (seen)
    - Test: { getItem: () => '0' }       -> false  (other value: not seen)
    - Test: { getItem: () => { throw new Error('blocked'); } } -> true  (treat as seen)

    placeholderVisible(value) -> boolean (pure):
    - Test: ''      -> true
    - Test: '   '   -> true  (whitespace-only counts as empty, matches commitRename trim)
    - Test: 'Posts' -> false
  </behavior>
  <action>
    model: sonnet — write both tests from the explicit assertions, then implement.

    RED first: create tests/js/first-run-gate.test.mjs and tests/js/placeholder.test.mjs
    (node:test + node:assert, same createRequire interop), importing firstRunSeen and
    placeholderVisible respectively from ../../assets/maestro-logic.js. Run
    `node --test tests/js/first-run-gate.test.mjs tests/js/placeholder.test.mjs` and
    watch both FAIL.

    GREEN: add both functions to assets/maestro-logic.js (extend the SAME api object).
    - firstRunSeen(storage): try { return storage.getItem('maestroFirstRunDone') === '1'; }
      catch (e) { return true; }
    - placeholderVisible(value): return value.trim() === '';
    Run the tests — both MUST pass. Then run the full `npm run test:js` to confirm the
    Phase 6 suites (reorderMove/diffItem/resetItem) plus all three new suites are green.

    Commit (one GREEN commit each if the pre-commit gate rejects red; else test:/feat:):
    `feat(09-01): pure firstRunSeen localStorage gate and placeholderVisible`.
  </action>
  <verify>
    <automated>node --test tests/js/first-run-gate.test.mjs tests/js/placeholder.test.mjs && npm run test:js</automated>
  </verify>
  <done>Both specs pass incl. the throws→true and whitespace→true cases; firstRunSeen and placeholderVisible are on the shared api object; full npm run test:js is green (Phase 6 suites unregressed).</done>
</task>

</tasks>

<verification>
- `npm run test:js` — all suites green: Phase 6 (reorderMove/diffItem/resetItem) + the 3 new Phase 9 seams
- No new runtime or dev dependency added (node:test/node:assert are built-in)
- Build hygiene unchanged: tests/js/ is not copied by bin/build.sh; assets/maestro-logic.js ships (already enqueued from Phase 6)
- No PHP/CSS/e2e touched in this plan
</verification>

<success_criteria>
- modeStatusLabel, firstRunSeen, placeholderVisible implemented test-first and green
- Each lives on the single existing api object with no duplicated export guard
- Zero new dependency; tests/js/ build-excluded; npm run test:js green end to end
- Seams ready for consumption by Plans 02 (modeStatusLabel), 03 (firstRunSeen), 04 (placeholderVisible)
</success_criteria>

<output>
After completion, create `.planning/phases/09-editor-ux-polish/09-01-SUMMARY.md`
</output>
