---
phase: 11.1-p1-review-hardening
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]
user_setup: []

must_haves:
  truths:
    - "The full zero-regression bar holds at phase close: PHP unit green (composer test:unit, incl. new ConfigSanitizeTest), PHP integration green (npm run test:php, incl. new custom_menu_order gate assertions), JS logic green (npm run test:js), Playwright e2e green (npm run test:e2e, incl. new save-race.spec.ts), composer lint clean, composer analyse:phpstan clean, and Plugin Check 0 errors on the shippable source"
    - "All three HARD requirements are demonstrably satisfied end to end: HARD-01 (custom_menu_order gated on stored top_order), HARD-02 (sanitize() payload caps), HARD-03 (save-race e2e green)"
    - "ROADMAP.md and REQUIREMENTS.md v1.2 traceability for HARD-01/HARD-02/HARD-03 are flipped to Complete and Phase 11.1 is marked done"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11.1 marked complete; HARD-01/02/03 plan checkboxes ticked"
      contains: "HARD-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.2 traceability rows for HARD-01/02/03 flipped to Complete"
      contains: "HARD-03"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "Phase 11.1 success criteria"
      via: "criteria verified against the full green suite + Plugin Check"
      pattern: "HARD-0"
---

<objective>
Phase 11.1 zero-regression gate and sign-off: run the full suite across every layer,
confirm all three requirements (HARD-01, HARD-02, HARD-03) hold end to end with no
regression, confirm Plugin Check reports 0 errors and PHPStan is clean on the shippable
source, and flip ROADMAP/REQUIREMENTS traceability to Complete.

Purpose: Plans 01–03 land the hardening + coverage; this plan proves the LOCKED
zero-regression bar still holds across PHP unit, PHP integration, JS logic, Playwright
e2e, phpcs, PHPStan, and Plugin Check, then records completion. New tests added across
the phase are additive — they raise counts, never lower the passing set. This is also
the Wave 2 boundary full-E2E run (Docker, sandbox-disabled) per the Phase 9 pattern.

Output: a clean full-suite run, Plugin Check 0 errors, PHPStan clean, and
ROADMAP/REQUIREMENTS updates marking Phase 11.1 done. Depends on all prior 11.1 plans.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11.1-p1-review-hardening-scope-custom-menu-order-bound-config-payload-save-race-e2e-coverage/11.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full zero-regression suite + PHPStan + Plugin Check (Wave 2 boundary e2e run) [model: sonnet]</name>
  <files></files>
  <action>
    model: sonnet — read-only verification; run the full suite to the explicit checklist.
    Docker E2E + integration run here sandbox-disabled (established Phase 9 wave-boundary pattern;
    the GSD executor cannot run Docker in the sandbox — request sandbox-disabled for the env:start /
    test:php / test:e2e commands).

    Run, in order, and confirm each is green (this is the LOCKED zero-regression bar):
    - `vendor/bin/phpunit -c phpunit-unit.xml.dist` (or `composer test:unit`) — PHP unit green incl.
      the new ConfigSanitizeTest (HARD-02 caps), OrderingTest/IconValidationTest unregressed
    - `npm run test:js` — JS logic suites green (no JS logic added this phase; must stay green)
    - `npm run env:start` then `npm run test:php` — PHP integration green incl. the new
      custom_menu_order gate assertions in ReplayTest (HARD-01)
    - `npm run test:e2e` — full Playwright suite green incl. the new save-race.spec.ts three races
      (HARD-03), editor.spec.ts unregressed
    - `composer lint` (phpcs) — clean
    - `composer analyse:phpstan` — clean on the shippable source
    - Plugin Check — 0 errors (run the project's Plugin Check command/path as in prior phases; see
      Phase 7/8/9 SUMMARY for the exact invocation)

    If any check is red, STOP and report which layer regressed — do NOT flip traceability over a red
    suite. For HARD-03 specifically: if a save-race assertion fails, that is a maestro.js defect to
    raise and fix (per CONTEXT.md), not a reason to weaken the test. New tests are additive; counts
    may rise but the passing set must never shrink.

    No commit for this task unless Plugin Check produces a build artifact to clean up; this is
    verification.
  </action>
  <verify>
    <automated>vendor/bin/phpunit -c phpunit-unit.xml.dist && npm run test:js && composer lint && composer analyse:phpstan</automated>
  </verify>
  <done>PHP unit (incl. ConfigSanitizeTest), JS logic, PHP integration (incl. custom_menu_order gate), Playwright e2e (incl. save-race.spec.ts), phpcs, and PHPStan are all green; Plugin Check reports 0 errors on the shippable source; any regression is reported instead of signed off.</done>
</task>

<task type="auto">
  <name>Task 2: Flip ROADMAP + REQUIREMENTS traceability to Complete; mark Phase 11.1 done [model: sonnet]</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    model: sonnet — mechanical doc update once the suite is fully green.

    .planning/ROADMAP.md (Phase 11.1 section + Progress table): mark Phase 11.1 complete in the
    Progress table (Status Complete, today's date), tick the plan checkboxes under "Plans:" for
    11.1-01..11.1-04, and confirm the four success criteria are met (HARD-01 gate, HARD-02 caps,
    HARD-03 e2e, zero-regression bar incl. PHPStan + Plugin Check 0 errors).

    .planning/REQUIREMENTS.md (Traceability (v1.2) table): flip HARD-01, HARD-02, HARD-03 from
    Pending to Complete. Update the v1.2 coverage note if needed. Leave all other rows untouched
    (UX-08/BUG-06/BUG-07 Phase 11, REL-07/08 Phase 12, V2-16 Phase 10).

    Commit: `docs(11.1): mark Phase 11.1 (HARD-01/02/03) complete; flip v1.2 traceability`.
  </action>
  <verify>
    <automated>grep -q "HARD-01" .planning/REQUIREMENTS.md && grep -q "HARD-03" .planning/ROADMAP.md && node -e "const fs=require('fs');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const sec=r.split('Traceability (v1.2)')[1]||'';for(const id of ['HARD-01','HARD-02','HARD-03']){const line=sec.split('\n').find(l=>l.includes(id));if(line&&/Pending/.test(line))throw new Error(id+' still Pending in v1.2 traceability');}process.exit(0)"</automated>
  </verify>
  <done>ROADMAP Phase 11.1 is Complete with plan checkboxes ticked; REQUIREMENTS v1.2 traceability shows HARD-01/HARD-02/HARD-03 Complete; no other rows changed.</done>
</task>

</tasks>

<verification>
- Full suite green: PHP unit + integration + JS logic + Playwright e2e
- `composer lint` clean; `composer analyse:phpstan` clean; Plugin Check 0 errors on shippable source
- ROADMAP + REQUIREMENTS reflect Phase 11.1 complete; HARD-01/02/03 flipped to Complete
</verification>

<success_criteria>
- Zero-regression bar holds across every layer; new tests additive only
- HARD-01, HARD-02, HARD-03 satisfied end to end and marked Complete in v1.2 traceability
- Phase 11.1 ready to fold into the 1.2.0 cut (path 9 → 11 → 11.1 → 12)
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-p1-review-hardening-scope-custom-menu-order-bound-config-payload-save-race-e2e-coverage/11.1-04-SUMMARY.md`
</output>
