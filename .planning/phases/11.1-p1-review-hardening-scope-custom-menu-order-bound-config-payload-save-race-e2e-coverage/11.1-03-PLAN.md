---
phase: 11.1-p1-review-hardening
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - tests/e2e/save-race.spec.ts
autonomous: true
requirements: [HARD-03]
user_setup: []

must_haves:
  truths:
    - "Race (a): while a slow POST /maestro/v1/config is in flight, clicking Exit does NOT immediately navigate (page still shows maestro_edit=1); after the delayed POST settles, Exit navigates to the exit URL — proving onExit() awaits waitForSaveIdle() rather than the debounce timer alone"
    - "Race (b): with a pending rename (queued autosave, saveTimer set, no in-flight POST), Reset All cancels the queued autosave (no POST fires) and the DELETE wins — final stored state is reset, not the rename"
    - "Race (c): with an in-flight slow POST, Reset All does NOT issue the DELETE until the POST settles, then fires DELETE and only reloads on a successful (r.ok) DELETE"
    - "All three races are asserted with Playwright web-first assertions (waitForResponse with method+ok checks; URL contains/not-contains maestro_edit=1) — no arbitrary sleeps; the POST delay is injected via page.route() set up BEFORE navigation, with no mu-plugin"
    - "The spec is test-only: no production change to assets/maestro.js. If a race does NOT hold, that is a defect to fix in maestro.js (raised in the SUMMARY), never a weakened assertion"
  artifacts:
    - path: "tests/e2e/save-race.spec.ts"
      provides: "Three Playwright tests covering the (saveTimer,inFlight) race states (a)/(b)/(c)"
      contains: "save-race"
  key_links:
    - from: "tests/e2e/save-race.spec.ts"
      to: "POST /maestro/v1/config + DELETE /maestro/v1/config"
      via: "page.route() POST delay + waitForResponse method/ok checks"
      pattern: "maestro/v1/config"
    - from: "save-race.spec.ts race (a)"
      to: "onExit() waitForSaveIdle() defer"
      via: "URL stays maestro_edit=1 while POST in flight, then navigates"
      pattern: "maestro_edit"
---

<objective>
HARD-03 — Playwright E2E regression coverage for the autosave/reset/exit save races
that PR #36 hardened in `assets/maestro.js` (`waitForSaveIdle()`,
`cancelQueuedAutosave()`, `doResetAll()` awaiting in-flight saves and checking
`response.ok`). There is currently NO automated coverage locking that subtle
single-flight behaviour in. Add a new spec that intercepts/slows
`POST /maestro/v1/config` and asserts all three races resolve correctly.

Purpose: protect already-shipped hardening from silent regression. Test-only — no
production code change is expected. If a race is found NOT to hold, raise it as a
maestro.js defect (do not weaken the assertion).

Output: tests/e2e/save-race.spec.ts with three `test()` blocks mirroring the existing
editor.spec.ts patterns (class-based selectors, `waitForResponse`, the `/maestro/v1/config`
URL predicate), extended with a `page.route()` POST-delay wrapper. Runs under wp-env
Docker via `npm run test:e2e` — executed at the Wave 2 boundary per the Phase 9 pattern.
</objective>

<tdd_note>
HARD-03 is E2E-only and NOT red-first in the unit sense — the behaviour under test is
already shipped in production (PR #36); these are regression LOCKS, not test-first design
of new logic (11.1-VALIDATION.md / RESEARCH § Per-Requirement Behavior Map: "Red-first
eligible: NO"). The discipline that applies: each test must FAIL if the corresponding
hardening is reverted (a meaningful lock, not a vacuous pass) — verify that by reasoning
about the assertion, and ensure the route delay is installed BEFORE navigation so the race
window actually exists.

Docker/sandbox note (STATE.md line 93, established Phase 9 pattern): the GSD executor
cannot run Docker E2E in the sandbox. Author the spec, then run the full Playwright suite
ONCE at the Wave 2 boundary (this plan's verify + the Plan 04 gate) sandbox-disabled. Do
NOT mark HARD-03 done on authored-but-unrun tests — the green E2E run at the boundary is
the gate.
</tdd_note>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11.1-p1-review-hardening-scope-custom-menu-order-bound-config-payload-save-race-e2e-coverage/11.1-CONTEXT.md
@.planning/phases/11.1-p1-review-hardening-scope-custom-menu-order-bound-config-payload-save-race-e2e-coverage/11.1-RESEARCH.md
@.planning/phases/11.1-p1-review-hardening-scope-custom-menu-order-bound-config-payload-save-race-e2e-coverage/11.1-VALIDATION.md

# The existing e2e spec to mirror — copy POST_SAVE, selectors, goto/click idioms:
@tests/e2e/editor.spec.ts
# The production save-race code under test (DO NOT modify) — read so the assertions match the real flow:
@assets/maestro.js

<interfaces>
<!-- Verified production behavior from 11.1-RESEARCH.md § Code Examples. The spec asserts THIS; do not change maestro.js. -->

onExit() (maestro.js:1087–1095): if ( saveTimer || inFlight ) { e.preventDefault();
waitForSaveIdle().then(() => window.location.href = D.exitUrl); }
  -> Race (a): navigation deferred until the in-flight/queued save settles.

doResetAll() (maestro.js:1060–1085): confirm -> disable button -> setStatus('saving')
-> cancelQueuedAutosave() (kills saveTimer + savePending) -> (inFlight || resolve()).then(DELETE)
-> if !r.ok throw; else location.reload(); .catch(re-enable + setStatus('error')).
  -> Race (b): saveTimer set / inFlight null -> queued autosave cancelled, DELETE fires (no POST).
  -> Race (c): inFlight set -> awaits inFlight, THEN DELETE; reloads only on r.ok.

POST-delay route wrapper (RESEARCH § Validation Architecture — set up BEFORE goto):
```typescript
await page.route( '**/maestro/v1/config*', async ( route ) => {
    if ( route.request().method() === 'POST' ) {
        await new Promise( r => setTimeout( r, 2000 ) ); // slow the in-flight save
    }
    await route.continue(); // real WP response -> r.ok is authentic
} );
```

Existing predicate to reuse (editor.spec.ts:13–14):
```typescript
const POST_SAVE = ( url: string ) => url.includes( '/maestro/v1/config' );
// waitForResponse: r => POST_SAVE(r.url()) && r.request().method() === 'POST' && r.ok()
```

Race-(a) exit detection (RESEARCH Open Q#3 — RESOLVED): do NOT hardcode D.exitUrl.
While the delayed POST is in flight, assert the URL STILL contains `maestro_edit=1`
(page did not leave edit mode); after the POST settles, assert the URL NO LONGER
contains `maestro_edit=1`.

Race trigger setup: enter edit mode (`/wp-admin/index.php?maestro_edit=1`), select an
item (e.g. `#menu-posts > a.menu-top`), type into the rename field to queue an autosave
(saveTimer) — mirror how editor.spec.ts drives the rename input. Reset All / Exit are
class-based selectors (`.maestro-reset-all` / the Exit control) — confirm exact selectors
against editor.spec.ts + maestro.js before asserting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author save-race.spec.ts — races (a) slow save + Exit and (c) in-flight save + Reset All [model: sonnet]</name>
  <files>tests/e2e/save-race.spec.ts</files>
  <action>
    model: sonnet — write the spec mirroring editor.spec.ts; do NOT modify assets/maestro.js.

    Create tests/e2e/save-race.spec.ts (import { test, expect } from '@playwright/test'). Add the
    POST-delay route wrapper from the interface block and set it up BEFORE each goto. Reuse the
    POST_SAVE predicate idiom and class-based selectors from editor.spec.ts. End each test with a
    Reset All so the next spec starts from a stable baseline (editor.spec.ts convention).

    Race (a) — slow save + Exit (saveTimer/inFlight set, Exit clicked):
    - install POST delay, goto maestro_edit=1, select an item, type a rename to queue/trigger a save
    - click Exit; assert the page URL STILL contains 'maestro_edit=1' WHILE the POST is in flight
      (navigation deferred by onExit -> waitForSaveIdle)
    - wait for the POST response (method POST, ok), then assert the URL NO LONGER contains
      'maestro_edit=1' (navigation happened after the save settled)

    Race (c) — in-flight save + Reset All (inFlight set, saveTimer null):
    - install POST delay, enter edit mode, select item, make a change that triggers a POST and let
      it be in flight (slow), then click Reset All and accept the confirm
    - assert the DELETE does NOT fire until the POST settles: use waitForResponse to observe the POST
      (method POST) completes BEFORE the DELETE (method DELETE) — e.g. capture response order, or
      assert no DELETE response is seen until after the POST response
    - assert the DELETE response is ok and the page reloads (doResetAll reloads only on r.ok)

    Handle window.confirm for Reset All (page.on('dialog', d => d.accept())) per maestro.js
    `window.confirm( I.confirmAll )`. Use Playwright web-first assertions (waitForResponse /
    expect(page).toHaveURL or URL contains checks) — NO arbitrary page.waitForTimeout sleeps for
    assertions (the 2s route delay is the only sleep, and it lives inside the route handler).

    Verify (full Playwright suite) runs at the Wave 2 boundary, sandbox-disabled (Docker). If the
    sandbox blocks env:start here, author the spec and defer the run to the boundary; note it.

    Commit: `test(11.1): e2e for slow-save+Exit and in-flight+Reset-All races (HARD-03)`.
  </action>
  <verify>
    <automated>npm run test:e2e -- save-race.spec.ts</automated>
  </verify>
  <done>save-race.spec.ts contains races (a) and (c); POST delay via page.route set up before navigation; race (a) asserts URL stays then leaves maestro_edit=1, race (c) asserts DELETE waits for POST then reloads on ok; assertions are web-first (no sleeps); maestro.js unchanged. Green at the Wave 2 boundary run.</done>
</task>

<task type="auto">
  <name>Task 2: Add race (b) pending rename + Reset All to save-race.spec.ts [model: sonnet]</name>
  <files>tests/e2e/save-race.spec.ts</files>
  <action>
    model: sonnet — extend the spec with the third race; do NOT modify assets/maestro.js.

    Race (b) — pending rename + Reset All (saveTimer set, inFlight null):
    - enter edit mode, select an item, type a rename so an autosave is QUEUED (saveTimer set) but
      NOT yet fired — do not wait for the debounce to elapse before clicking Reset All
    - click Reset All, accept the confirm; assert that cancelQueuedAutosave() killed the queued
      autosave: NO POST /maestro/v1/config fires (assert no POST response is observed), and a DELETE
      fires instead
    - assert the DELETE is ok, the page reloads, and the final state is RESET (the rename did not
      persist) — verify the item shows its default title after reload, not the typed rename

    To prove "no POST fired": register a waitForResponse for a POST that should time out / never
    resolve within a bound, or count requests via page.on('request') and assert zero POSTs to the
    config endpoint between the rename and the DELETE. Prefer a request counter (deterministic) over
    a negative waitForResponse timeout.

    This task shares the file with Task 1 but is sequential within the plan (same file, one plan) —
    add the third test() block; keep the shared route wrapper/helpers DRY.

    Run the full new spec (all three races) at the Wave 2 boundary, sandbox-disabled.

    Commit: `test(11.1): e2e for pending-rename+Reset-All cancels queued autosave (HARD-03)`.
  </action>
  <verify>
    <automated>npm run test:e2e -- save-race.spec.ts</automated>
  </verify>
  <done>Race (b) asserts the queued autosave is cancelled (no POST fires), the DELETE wins, the page reloads, and the rename did not persist (default title after reload); all three races green at the Wave 2 boundary; maestro.js unchanged.</done>
</task>

</tasks>

<verification>
- `npm run test:e2e` (run at the Wave 2 boundary, sandbox-disabled) — save-race.spec.ts all three races green, full e2e suite unregressed
- Spec is test-only: `git diff` shows no change to assets/maestro.js
- POST delay installed via page.route before navigation; assertions are web-first (waitForResponse / URL checks), no assertion sleeps
- If any race fails, it is reported as a maestro.js defect in the SUMMARY, not papered over
</verification>

<success_criteria>
- Three races (slow save + Exit; pending rename + Reset All; in-flight save + Reset All) covered and green (HARD-03)
- No production code change to maestro.js
- Assertions meaningfully lock the PR #36 hardening (would fail if reverted)
- Full Playwright suite green at the wave boundary
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-p1-review-hardening-scope-custom-menu-order-bound-config-payload-save-race-e2e-coverage/11.1-03-SUMMARY.md`
</output>
