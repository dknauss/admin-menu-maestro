---
phase: 11-editor-entry-reorder-fixes
plan: 08
type: execute
wave: 2
depends_on: [11-06, 11-07]
gap_closure: true
files_modified:
  - tests/e2e/specs/capture-screenshots.spec.ts   # extend — add enter-state capture
autonomous: true
requirements: [UX-08a, UX-08b, BUG-06, BUG-07]
must_haves:
  truths:
    - "The full suite is green, sandbox-disabled at the wave boundary, including the two corrected 11-05 guards (UX-08a enter-state + control-driven reorder) and the unchanged UX-08a exit / BUG-06 / BUG-07 baselines"
    - "Enter-state mobile screenshots (ux-08a-enter-782.png, ux-08a-enter-600.png) are deterministically captured showing the visible icon-only ENTER toggle, alongside the existing exit-state PNGs"
    - "No production source is modified by this plan (test/artifact + capture-spec only); any red routes back to 11-06 or 11-07"
  artifacts:
    - path: ".planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-enter-782.png and ux-08a-enter-600.png"
      provides: "Regenerable proof the enter toggle is reachable on mobile (npm run screenshots)"
  key_links:
    - from: "npm run test:js + test:php + test:e2e (sandbox-disabled)"
      to: "Phase 11 gap-closure zero-regression bar"
      via: "full suite run including the corrected guards"
      pattern: "passed"
---

<objective>
Wave 2 (gap closure): the zero-regression gate. Run the FULL suite sandbox-disabled (Docker) to confirm
all four gaps are closed and nothing regressed, then extend the guarded capture spec to deterministically
screenshot the UX-08a ENTER toggle at 782px/600px (the UAT explicitly asked for ux-08a-enter-{782,600}.png
in addition to the existing exit-state PNGs).

This plan adds NO production source — it extends ONE guarded test-only capture spec and runs the suite. It
depends on both Wave 1 gap plans (11-06 enqueue, 11-07 panel+css), so it runs only after their fixes land.

Purpose: hold the zero-regression bar before re-UAT and the traceability sign-off.
Output: a green full-suite run + enter-state mobile screenshots for the gap-closure gate.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-editor-entry-reorder-fixes/11-UAT.md
@.planning/phases/11-editor-entry-reorder-fixes/11-04-PLAN.md

<interfaces>
<!-- Baselines + commands. Docker e2e + PHP run sandbox-disabled at this boundary (known sandbox/Docker gap). -->
Phase 11 post-ship baseline: PHP unit 61/61 · JS 53/53 · integration 34/34 (incl. AdminBarTest) · e2e 31/31.
Expected after this gap closure:
  - JS logic: 53/53 (unchanged — DOM glue, no new node:test).
  - PHP unit: 61/61 (unchanged).
  - PHP integration: >= prior, +2 if LocalizationTest gained moveUp/moveDown assertions (11-07).
  - Playwright e2e: prior baseline + the NEW UX-08a enter-state test (11-05 Task 1); the converted
    control-driven reorder test (11-05 Task 2) replaces the old Alt+Arrow one (net e2e count may be +1).
Commands:
  npm run test:js     # fast, no Docker — run first
  npm run test:php    # wp-env integration (Docker) — sandbox-disabled
  npm run test:e2e    # Playwright (Docker) — sandbox-disabled
  npm run screenshots # MAESTRO_CAPTURE-gated capture spec (tests/e2e/specs/capture-screenshots.spec.ts)

Existing capture spec — tests/e2e/specs/capture-screenshots.spec.ts (from 11-04): guarded by
`test.skip( ! process.env.MAESTRO_CAPTURE, … )`, reuses the editor.spec.ts admin-auth fixture, loops
[782,600], visits wp-admin (with maestro_edit=1 — EXIT state), waits on #wp-admin-bar-maestro-toggle, and
writes screenshots/ux-08a-{width}.png. EXTEND it — do NOT create a second spec file.
The `prescreenshots` npm hook already activates the plugin (package.json).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full zero-regression suite (sandbox-disabled for Docker)</name>
  <files></files>
  <action>
Run the full suite and confirm all green, including the two corrected 11-05 guards:
  1. `npm run test:js` — expect 53/53. Fast; first to catch JS regressions cheaply.
  2. `npm run test:php` — wp-env integration. Expect prior count (+2 if LocalizationTest gained
     moveUp/moveDown). Confirms the i18n strings render.
  3. `npm run test:e2e` — Playwright. Expect ALL green, specifically:
       - NEW: UX-08a enter-state (no maestro_edit, 782/600) PASSES — was RED in 11-05 (Gap 1 fix).
       - CONVERTED: the control-driven reorder test (rename input focused -> button.maestro-move-down)
         PASSES — was RED in 11-05 (Gap 3 fix); chaining + persistence + separator preservation hold.
       - UNCHANGED baselines still green: UX-08a exit-state, BUG-06 separator, BUG-07 badge placement,
         UX-02/UX-07 toolbar-overflow @700px, the 44px tap-target floor (editor.spec.ts:1008).

The Docker-backed steps MUST run sandbox-disabled (project's known sandbox/Docker gap).

If any suite is red: STOP, report the failing test(s), and route the gap back to 11-06 (enqueue/CSS) or
11-07 (panel JS/CSS/i18n) — do NOT patch production source from this gate plan (file ownership is held by
the Wave 1 plans). Disk note: on ENOSPC, STOP and report — do not retry in a loop.
  </action>
  <verify>
  <automated>npm run test:js</automated>
  </verify>
  <done>JS 53/53 green locally; the full Docker suite (test:php + test:e2e, sandbox-disabled) reported green by the executor, including the UX-08a enter-state test and the converted control-driven reorder test, with the unchanged exit/BUG-06/BUG-07/overflow/tap-target baselines all passing. Counts recorded in the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the capture spec with enter-state mobile screenshots</name>
  <files>tests/e2e/specs/capture-screenshots.spec.ts</files>
  <action>
EXTEND the existing guarded `tests/e2e/specs/capture-screenshots.spec.ts` (do NOT add a new file). Add an
ENTER-state capture loop mirroring the existing exit-state one:
  - Keep the existing `test.skip( ! process.env.MAESTRO_CAPTURE, … )` guard and the editor.spec.ts
    admin-auth reuse — do not invent a new auth path.
  - For each width in [ 782, 600 ]: `page.setViewportSize`, visit `/wp-admin/index.php` with NO
    `maestro_edit` param (the ENTER state — the thing UAT said was broken), wait on
    `#wp-admin-bar-maestro-toggle` (the SAME selector the UX-08a enter-state e2e asserts, so a wrong/error
    page can't satisfy the wait and capture silently), then `page.screenshot({ path: … })` into
    `.planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-enter-{width}.png`.
  - Cheap re-assert the toggle is visible + the `.ab-icon` glyph visible at capture time (the authoritative
    assertions live in the e2e; this spec is an ARTIFACT generator).
  - Leave the existing exit-state captures (ux-08a-{width}.png) intact — both sets are committed.

`npm run screenshots` already runs this spec MAESTRO_CAPTURE-gated with the prescreenshots plugin-activation
hook; no package.json change needed (confirm the script still resolves the spec path). Commit the new PNGs
under the phase screenshots/ dir as the gap-closure artifact. Disk note: on ENOSPC, STOP and report.
  </action>
  <verify>
  <automated>npm run screenshots && test -f .planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-enter-782.png && test -f .planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-enter-600.png</automated>
  </verify>
  <done>`npm run screenshots` deterministically writes ux-08a-enter-782.png and ux-08a-enter-600.png (visible icon-only ENTER toggle) alongside the existing exit-state PNGs; the MAESTRO_CAPTURE guard keeps the normal e2e suite from overwriting them; the new PNGs are committed under the phase screenshots/ dir.</done>
</task>

</tasks>

<verification>
- Full suite green (sandbox-disabled) incl. the UX-08a enter-state + control-driven reorder corrected guards and all unchanged baselines.
- ux-08a-enter-{782,600}.png deterministically captured (visible icon-only enter toggle) and committed.
- No production source modified by this plan — capture-spec extension + a suite run only.
</verification>

<success_criteria>
The Phase 11 gap-closure zero-regression bar holds: JS >=53/53, PHP unit >=61/61, integration green
(+ any moveUp/moveDown LocalizationTest), e2e green incl. the two corrected guards and unchanged baselines,
plus deterministic enter-state mobile screenshots. The phase is ready for re-UAT and the traceability flip.
</success_criteria>

<output>
After completion, create `.planning/phases/11-editor-entry-reorder-fixes/11-08-SUMMARY.md`.
</output>
