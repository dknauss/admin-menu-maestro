---
phase: 11-editor-entry-reorder-fixes
plan: 07
type: execute
wave: 1
depends_on: [11-05]
gap_closure: true
files_modified:
  - assets/maestro.js
  - assets/maestro.css
  - includes/class-assets.php   # i18n strings only (moveUp/moveDown aria-labels) in wp_localize_script
autonomous: true
requirements: [BUG-06, UX-08b]
must_haves:
  truths:
    - "When an item is selected, the panel exposes explicit ▲/▼ up/down reorder buttons that move the selected top-level item one position via the existing reorderMove/insertBefore path — Tab-reachable from the rename input, OS-independent, working on macOS"
    - "The ▲/▼ buttons and the secondary panel buttons (Icon, Visibility, Reset Item) collapse to icon-only at <=600px with min-width:44px, each keeping an aria-label + a visually-hidden .maestro-btn-label so no control becomes unlabeled (WCAG 4.1.2); the toolbar no longer overflows with all controls visible"
    - "Rename auto-focus-on-select is preserved (the type-immediately flow does not regress); aria-keyshortcuts no longer advertise the macOS-broken Alt+Arrow as the only path"
    - "The modified-state badge bullet is bumped to 14-16px so it reads at a glance — no layout/contrast/a11y regression"
  artifacts:
    - path: "assets/maestro.js"
      provides: "moveUp/moveDown panel buttons calling the existing reorder DOM path; iconBtn/visBtn/resetItem/move buttons gain aria-label + .maestro-btn-label span; aria-keyshortcuts updated"
      contains: "maestro-move-up"
    - path: "assets/maestro.css"
      provides: "<=600px icon-only compression (.maestro-btn-label hidden, min-width:44px) + badge font-size 14-16px"
      contains: "maestro-btn-label"
  key_links:
    - from: "assets/maestro.js (move buttons)"
      to: "window.maestroLogic.reorderMove + parentUl.insertBefore single-node move"
      via: "refactor the keydown reorder body into a shared moveSelected(dir) called by both the buttons and any retained accelerator"
      pattern: "function moveSelected|reorderMove\\("
    - from: "assets/maestro.css (@media max-width:600px)"
      to: ".maestro-panel button .maestro-btn-label"
      via: "display:none on the label span; button min-width:44px keeps the tap target"
      pattern: "max-width: 600px"
---

<objective>
Gaps 2, 3 and 4 — COUPLED because all three touch the selected-item panel layout in assets/maestro.js +
assets/maestro.css and the resolved Gap-3 design folds the new reorder buttons INTO the Gap-2 toolbar
compression. Designed together so the ▲/▼ buttons and the icon-only compression do not conflict.

Gap 3 (major, BUG-06 lineage) RESOLVED DESIGN: add explicit ▲/▼ up/down reorder buttons to the panel,
Tab-reachable from the rename input, reusing the existing reorderMove/insertBefore path (maestro.js:284-337).
OS-independent and accessible — fixes the macOS Option+Arrow-is-caret-nav unreachability. Keep rename
auto-focus-on-select (do NOT regress type-immediately). Optionally add a NON-Alt accelerator. Update
aria-keyshortcuts + i18n.

Gap 2 (minor, UX-08b lineage): at narrow widths the panel's 4 controls (rename + Icon + Visibility +
Reset Item) plus the new ▲/▼ overflow the flex-wrap toolbar. Collapse the secondary buttons to icon-only
at <=600px with a 44px tap-target floor; add aria-label + a .maestro-btn-label text span to each so only
the visible text hides (no 4.1.2 unlabeled-control regression).

Gap 4 (cosmetic, BUG-07 lineage): the modified-badge bullet at maestro.css:84 (font-size:10px) renders
~7px tall and is easily missed. Bump to 14-16px. CSS-only.

Purpose: turn the 11-05 control-driven reorder guard GREEN and close the toolbar-overflow + badge gaps.
Output: an OS-independent, accessible, mobile-fitting selected-item panel + a legible modified badge.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-editor-entry-reorder-fixes/11-UAT.md

<interfaces>
<!-- Real anchors — reuse, do not reinvent. -->

The reorder DOM body to factor out — maestro.js:262-337 (inside the keydown handler at :247). It:
  - reads `model[selectedSlug]`, derives `dir`, builds `currentSlugs` (sub vs top scope),
  - calls `window.maestroLogic.reorderMove( currentSlugs, selectedSlug, dir )`,
  - detects boundary clamp (speak moveAtTop/moveAtBottom),
  - performs the SINGLE-NODE move: `parentUl.insertBefore( selectedNode, … )` keyed off `maestroChildren`
    index (this is the BUG-06 separator-preserving move — separators stay put),
  - restores focus to the moved anchor, speak()s the new position, sets aria-keyshortcuts, scheduleAutosave().
Refactor this body into `function moveSelected( dir )` so BOTH the buttons and any retained key accelerator
call it. The button path should NOT re-focus the row anchor (that broke macOS) — after a button move,
return focus to the SAME move button (or keep it, since the button is not detached) so repeated clicks chain.

selectItem — maestro.js:341-362: sets selectedSlug, populatePanel, then `panel.rename.focus()` when
`opts.focusPanel`. KEEP this (auto-focus-on-select). The :352 line sets
`li.setAttribute('aria-keyshortcuts','Alt+ArrowUp Alt+ArrowDown')` — update the advertised shortcut.

Panel button creation — maestro.js:420-459 (iconBtn :420, visBtn :428, resetItemBtn :436; appended :444-449;
`panel = {...}` :452-459). Current buttons set `.textContent = I.icon` etc. with NO aria-label and NO label
span. The `el(tag, class, text)` helper sets textContent when a 3rd arg is passed.

i18n strings — includes/class-assets.php:96-128 wp_localize_script 'i18n' map (already has icon/visibility/
resetItem/exit/dirUp/dirDown). ADD 'moveUp' => __( 'Move up' ) and 'moveDown' => __( 'Move down' ) for the
button aria-labels (consumed as I.moveUp / I.moveDown in maestro.js). This is the ONLY class-assets.php edit
this plan makes (string additions in the localize array) — it does NOT touch the enqueue logic 11-06 owns.

Badge CSS — maestro.css:82-90 (`.maestro-editing #adminmenu .maestro-modified-badge { font-size:10px; … }`).
vertical-align:middle, pointer-events:none, color:#dba617 stay; only font-size changes.

Narrow-width media block — maestro.css:471 `@media screen and ( max-width: 782px )`. Gap-2 compression goes
in a NEW `@media (max-width:600px)` block (the UAT specifies <=600px for icon-only), scoped under
`.maestro-toolbar .maestro-panel`. The 782px block keeps the existing 44px floor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ▲/▼ panel reorder buttons wired to the existing single-node move (BUG-06 path)</name>
  <files>assets/maestro.js, includes/class-assets.php</files>
  <behavior>
    Driven by the 11-05 e2e (control-driven reorder): after selecting #menu-posts (rename input focused),
    clicking button.maestro-move-down moves the top-level item down one position, persists via the existing
    debounced autosave, and chains (a second click moves it again). Separators stay put (BUG-06 path reused).
    aria-keyshortcuts no longer advertise Alt+Arrow as the path.
  </behavior>
  <action>
1. Refactor the reorder body (maestro.js:262-337) out of the keydown handler into a top-level
   `function moveSelected( dir )` that takes 'up'|'down', performs the EXACT existing logic (reorderMove +
   single-node insertBefore + boundary speak + announce + scheduleAutosave). The keydown handler at :247
   now just computes `dir` and calls `moveSelected(dir)` (preserve the input/popover guard so a real
   key path, if kept, still ignores typing — but the BUTTONS bypass that guard entirely).
   IMPORTANT: in the button path do NOT re-focus the row anchor (that was the macOS trap). The move
   button itself is not detached by insertBefore (it lives in the fixed toolbar panel, not in #adminmenu),
   so focus naturally stays on it — repeated clicks chain. Confirm focus stays on the button after a move.

2. Build the two buttons in buildToolbar() (alongside iconBtn/visBtn at :420-449):
     var moveUp   = el( 'button', 'button maestro-move-up' );   moveUp.type   = 'button';
     var moveDown = el( 'button', 'button maestro-move-down' ); moveDown.type = 'button';
   Each gets:
     - an aria-hidden dashicon glyph (▲ = 'dashicons-arrow-up-alt2', ▼ = 'dashicons-arrow-down-alt2'),
     - `aria-label` = I.moveUp / I.moveDown,
     - a `.maestro-btn-label` <span> with the visible text (I.moveUp / I.moveDown) — see Task 2 pattern,
     - click handler: `e.preventDefault(); moveSelected('up'|'down');`.
   Append them in the panel (recommend FIRST, before rename, so reorder is the leftmost panel affordance —
   or immediately after Reset Item; pick one and note it). Add to the `panel = {…}` object (moveUp, moveDown).

3. Add i18n: in includes/class-assets.php wp_localize_script 'i18n' map (:96-128) add
     'moveUp'   => __( 'Move up', 'maestro-menu-editor' ),
     'moveDown' => __( 'Move down', 'maestro-menu-editor' ),
   If an integration LocalizationTest enumerates i18n keys (Phase 09 precedent), add the two keys to it in
   the SAME commit so integration never goes red mid-task (per the project's localize+test single-commit rule).

4. Update the advertised shortcut: at selectItem (maestro.js:352) and at the move announce (:333), STOP
   advertising the macOS-broken `Alt+ArrowUp Alt+ArrowDown`. Either drop aria-keyshortcuts (the buttons +
   their aria-labels are the discoverable affordance) OR, if a NON-Alt accelerator is added, set
   aria-keyshortcuts to that combo. Do NOT advertise Alt+Arrow. Keep rename auto-focus-on-select intact.

This is behavioral JS — but the move LOGIC (reorderMove) is already unit-tested; the new code is DOM glue
(buttons -> existing path), so the red-first guard is the 11-05 e2e, not a new node:test (matches the
Phase 11 decision that single-node insertBefore is "pure DOM glue, not unit-testable as expect(fn).toBe(out)").
  </action>
  <verify>
  <automated>grep -q "maestro-move-down" assets/maestro.js && grep -q "function moveSelected" assets/maestro.js && grep -q "'moveDown'" includes/class-assets.php</automated>
  </verify>
  <done>The panel has ▲/▼ buttons (button.maestro-move-up / .maestro-move-down) with aria-labels, wired to a shared moveSelected(dir) reusing reorderMove + single-node insertBefore; Alt+Arrow is no longer advertised; rename auto-focus is preserved; the 11-05 control-driven reorder e2e passes at the Wave 2 boundary (incl. chaining + persistence + separator preservation).</done>
</task>

<task type="auto">
  <name>Task 2: Icon-only compression of the panel buttons at <=600px (aria-label + label span)</name>
  <files>assets/maestro.js, assets/maestro.css</files>
  <action>
JS (maestro.js) — give EACH secondary panel button (iconBtn, visBtn, resetItemBtn, and the new moveUp/
moveDown from Task 1) the icon-only-ready structure so the visible text can hide without losing the
accessible name (WCAG 4.1.2):
  - Set `btn.setAttribute( 'aria-label', <full label> )` (Icon / Visibility / Reset Item / Move up / Move down).
  - Replace bare `.textContent = I.x` with: an aria-hidden dashicon span (Icon -> 'dashicons-format-image'
    or existing choice; Visibility -> 'dashicons-visibility'; Reset Item -> 'dashicons-image-rotate' or
    'dashicons-undo'; move buttons use the arrow glyphs from Task 1) + a `<span class="maestro-btn-label">`
    holding the visible text. Build a small helper, e.g.
    `function iconButton(cls, glyph, label){ … appends span.dashicons[aria-hidden] + span.maestro-btn-label }`,
    and route all five buttons through it to avoid drift.
  - The aria-label is the authoritative accessible name at ALL widths; the .maestro-btn-label span is the
    visible text that CSS hides at <=600px. Keep rename input + its <label> untouched.

CSS (assets/maestro.css) — add a NEW `@media screen and ( max-width: 600px )` block (UAT specifies <=600px),
scoped under `.maestro-toolbar`:
```css
@media screen and ( max-width: 600px ) {
	/* Icon-only: hide the visible button text, keep the dashicon; aria-label carries the name. */
	.maestro-toolbar .maestro-panel button .maestro-btn-label { display: none; }
	/* Preserve the WCAG 2.5.5 tap target when text is gone. */
	.maestro-toolbar .maestro-panel button {
		min-width: 44px;
		justify-content: center;
	}
}
```
Keep the existing 782px block's min-height:44px floor (maestro.css:485-496) — the 600px block adds
min-WIDTH for the now-square icon buttons. Confirm the dashicon glyph is centered (the button already has
the WP `.button` padding; `display:inline-flex; align-items:center` may be needed on
`.maestro-panel button` at base width — add a base rule if the glyph+span don't align).

CSS is TDD-exempt; the JS aria-label/label-span change is verified by the existing a11y-shaped e2e plus the
44px tap-target e2e (editor.spec.ts:1008). No new behavioral seam.
  </action>
  <verify>
  <automated>grep -q "maestro-btn-label" assets/maestro.js && grep -q "max-width: 600px" assets/maestro.css && grep -q "maestro-btn-label" assets/maestro.css</automated>
  </verify>
  <done>All five secondary panel buttons carry an aria-label + an aria-hidden dashicon + a .maestro-btn-label span; at <=600px the label span is hidden and buttons keep min-width:44px (tap-target floor); the toolbar fits without overflow with an item selected; no button is unlabeled (4.1.2 preserved).</done>
</task>

<task type="auto">
  <name>Task 3: Bump the modified-badge bullet to a readable size (BUG-07 cosmetic)</name>
  <files>assets/maestro.css</files>
  <action>
At assets/maestro.css:84, change the modified-badge glyph size from `font-size: 10px;` to `font-size: 15px;`
(within the 14-16px target — a graphical marker, not body text). Leave color:#dba617, line-height:1,
margin-left:4px, vertical-align:middle, pointer-events:none, user-select:none unchanged. The
`.maestro-modified-sr` screen-reader sibling and badge POSITION (already on the row from Phase 11 BUG-07)
are untouched — this is size-only.

CSS-only, TDD-exempt. The BUG-07 placement e2e (badge inside #menu-posts .wp-menu-name) still passes
(unchanged DOM); this only enlarges the glyph.
  </action>
  <verify>
  <automated>grep -A6 "maestro-modified-badge" assets/maestro.css | grep -q "font-size: 1[4-6]px"</automated>
  </verify>
  <done>The modified-badge font-size is 14-16px (15px); all other badge properties and its on-row placement are unchanged; the BUG-07 placement e2e still passes.</done>
</task>

</tasks>

<verification>
- 11-05 control-driven reorder e2e PASSES: rename input focused after selection, button.maestro-move-down moves the item, chains, persists; separators stay put (BUG-06 path reused).
- aria-keyshortcuts no longer advertise Alt+Arrow; rename auto-focus-on-select preserved.
- At <=600px the panel buttons are icon-only (label span hidden) with min-width:44px and intact aria-labels; toolbar does not overflow with an item selected (UX-02/UX-07 overflow e2e at 700px still green; 44px tap-target e2e still green).
- Modified badge renders at 14-16px; BUG-07 placement e2e still passes.
- phpcs clean on class-assets.php (i18n string additions only); LocalizationTest (if present) green with moveUp/moveDown.
</verification>

<success_criteria>
The selected-item panel offers OS-independent, accessible ▲/▼ reorder (Gap 3 closed), fits at mobile
widths via icon-only compression without unlabeled controls (Gap 2 closed), and the modified badge is
legible at a glance (Gap 4 closed) — all without regressing rename auto-focus, the 44px tap-target floor,
or BUG-06/BUG-07 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-editor-entry-reorder-fixes/11-07-SUMMARY.md`.
</output>
