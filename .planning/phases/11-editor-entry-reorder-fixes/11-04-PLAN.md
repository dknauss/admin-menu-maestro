---
phase: 11-editor-entry-reorder-fixes
plan: 04
type: execute
wave: 2
depends_on: [11-02, 11-03]
files_modified:
  - tests/e2e/specs/capture-screenshots.spec.ts   # new — guarded mobile-capture spec (MAESTRO_CAPTURE-gated)
  - package.json                                    # new `screenshots` script
autonomous: true
requirements: [UX-08a, UX-08b, BUG-06, BUG-07]
must_haves:
  truths:
    - "The full test suite is green including the four new Phase 11 tests"
    - "JS logic >=53/53, PHP unit >=61/61, integration >=34/34 (was 33 + AdminBarTest), e2e >=31/31 (was 28 + 3)"
    - "Mobile toggle screenshots are deterministically captured at <=782px and <=600px (icon-only, long-form label preserved as the accessible name); a human glance at the committed PNGs is optional and non-blocking"
  artifacts:
    - ".planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-782.png and ux-08a-600.png (deterministic, regenerable via `npm run screenshots`)"
  key_links:
    - from: "npm run test:e2e + test:php + test:js"
      to: "zero-regression bar"
      via: "full suite run, sandbox-disabled for Docker"
      pattern: "passed"
---

<objective>
Wave 2: the zero-regression gate. Run the full suite (sandbox-disabled, Docker) to confirm every
Phase 11 requirement is met and nothing regressed, then deterministically capture the UX-08a mobile
toggle screenshots at 782px/600px via a guarded Playwright capture spec (the same pattern as wp-sudo's
`capture-screenshots.spec.ts` — a scripted, regenerable replacement for the old manual browser handoff).

This plan adds ONE new test-only file (a guarded capture spec) plus a `screenshots` npm script; it
modifies NO production source. It depends on both Wave 1 plans (11-02 UX-08, 11-03 BUG-06/07), so it
runs only after their CSS/JS is in place — the capture reflects the fixed, icon-only state.

Purpose: hold the zero-regression bar before `/gsd:verify-work` and the 1.2.0 traceability flip.
Output: a green full-suite run + deterministic mobile screenshots committed for the phase gate (an
optional non-blocking human glance at the PNGs replaces the old blocking browser-handoff checkpoint).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-editor-entry-reorder-fixes/11-VALIDATION.md

<interfaces>
<!-- Commands and baselines. Docker e2e + PHP must run sandbox-disabled. -->
Baseline (Phase 11.1): PHP unit 61/61 · JS 53/53 · integration 33/33 · e2e 28/28.
Expected after Phase 11:
  - JS logic: 53/53 (unchanged — DOM glue, no new node:test)
  - PHP unit: 61/61 (unchanged — AdminBarTest is integration, not unit)
  - PHP integration: 34/34 (33 + AdminBarTest's assertions; exact count may be 33 + N)
  - Playwright e2e: 31/31 (28 + UX-08a + BUG-06 + BUG-07)  [BUG-06 may report skipped if wp-env has no
    separators AND no fixture was added — see note]

Commands:
  npm run test:js                                   # fast, no Docker
  npm run test:php                                  # integration suite, wp-env (Docker) — sandbox-disabled
  npm run test:e2e                                  # Playwright (Docker) — sandbox-disabled
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full zero-regression suite (sandbox-disabled for Docker)</name>
  <files></files>
  <action>
Run the full suite and confirm all green, including the four new Phase 11 tests. The Docker-backed
steps (`test:php`, `test:e2e`) MUST run sandbox-disabled (per the project's known sandbox/Docker gap).

  1. `npm run test:js` — expect 53/53. Fast; run first to catch any JS regression cheaply.
  2. `npm run test:php` — wp-env integration suite. Expect prior count + AdminBarTest green
     (>=34/34). Confirms UX-08b strings render via the admin bar.
  3. `npm run test:e2e` — Playwright. Expect 28 baseline + 3 new (UX-08a, BUG-06, BUG-07) all green,
     AND the existing baselines (L301 'modified indicator', L355 'keyboard-only reorder') still pass.

BUG-06 separator note: at execute time the BUG-06 e2e probes
`#adminmenu > li.wp-menu-separator` count. If wp-env's WP core registers separators (positions
4/9/25/59/99), the test runs and must PASS. If the count is 0 and no fixture was added in Wave 1, the
test reports SKIPPED — in that case this gate task MUST add the minimal mu-plugin separator fixture
(RESEARCH Pitfall 4) via `.wp-env.json` mapping or `global-setup.ts`, re-run, and confirm the test
actually exercises the separator-preservation assertion (a skipped BUG-06 test does NOT satisfy the
phase). Note explicitly in the summary whether a fixture was needed.

If any suite is red: STOP, report the failing test(s), and route the gap back to 11-02 or 11-03 (do
not patch source from this gate plan — file ownership is held by the Wave 1 plans).

Disk note: if any command fails with ENOSPC, STOP and report — do not retry in a loop.
  </action>
  <verify>
  <automated>npm run test:js</automated>
  </verify>
  <done>
JS 53/53 green locally; the full Docker suite (`npm run test:php` + `npm run test:e2e`,
sandbox-disabled) reported green by the executor, including AdminBarTest and the three new e2e tests,
with the BUG-06 test actually exercising a separator-bearing menu (not skipped). Counts recorded in the
summary: JS, PHP unit, integration, e2e — each >= baseline plus the new Phase 11 tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deterministic mobile-toggle capture spec (scripted, replaces the manual handoff)</name>
  <files>tests/e2e/specs/capture-screenshots.spec.ts, package.json</files>
  <action>
Add a guarded Playwright capture spec that deterministically screenshots the UX-08a mobile toggle at
782px and 600px, mirroring wp-sudo's `tests/e2e/specs/capture-screenshots.spec.ts` pattern:

  1. New spec `tests/e2e/specs/capture-screenshots.spec.ts` (a NEW file — does NOT touch
     `tests/e2e/editor.spec.ts`, which 11-01 owns):
       - GUARD: `test.skip( ! process.env.MAESTRO_CAPTURE, 'Set MAESTRO_CAPTURE=1 (npm run screenshots)…' )`
         so the normal `test:e2e` / CI run never regenerates or overwrites the committed PNGs.
       - Reuse the e2e harness's admin auth exactly as `editor.spec.ts` does (storageState / login
         fixture — match whatever editor.spec.ts uses; do not invent a new auth path).
       - For each width in [782, 600]: `page.setViewportSize`, visit wp-admin, wait on a
         Maestro-specific anchor (the admin-bar toggle node — the SAME selector the UX-08a e2e asserts,
         so a wrong/error page cannot satisfy the wait and capture silently), then
         `page.screenshot({ path: … })` into
         `.planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-{width}.png`.
       - Assert the toggle is visible + icon-only at capture time (cheap re-assert; the authoritative
         icon-only/visibility assertions live in the UX-08a e2e from 11-01/11-02 — this spec is an
         ARTIFACT generator, not the primary gate).
  2. `package.json`: add `"screenshots": "MAESTRO_CAPTURE=1 playwright test tests/e2e/specs/capture-screenshots.spec.ts"`
     (match maestro's existing `test:e2e` invocation style — config flags, project, wp-env). Confirm the
     exact command shape against the current `test:e2e` script before writing.
     NOTE: npm only chains `pre`/`post` hooks on exact name match, so the existing `pretest:e2e`
     plugin-activation hook will NOT auto-run for `npm run screenshots`. Either add a matching
     `"prescreenshots": "<same wp plugin activate command as pretest:e2e>"` hook, or ensure wp-env is
     already up with the plugin active before capturing (the normal Wave 2 context, since Task 1 just
     ran the full e2e suite). Prefer the `prescreenshots` hook so `npm run screenshots` is standalone.

This runs in Wave 2 (after 11-02's CSS lands) so the captured PNGs show the FIXED icon-only state.
Commit the generated PNGs under the phase `screenshots/` dir as the phase-gate artifact.

Disk note: if any command fails with ENOSPC, STOP and report — do not retry in a loop.
  </action>
  <verify>
  <automated>npm run screenshots && test -f .planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-782.png && test -f .planning/phases/11-editor-entry-reorder-fixes/screenshots/ux-08a-600.png</automated>
  </verify>
  <done>`npm run screenshots` writes ux-08a-782.png and ux-08a-600.png deterministically (icon-only toggle, long-form label preserved as accessible name), the guard keeps the normal e2e suite from overwriting them, and the PNGs are committed under the phase screenshots/ dir. Optional, non-blocking: a human may glance at the committed PNGs to confirm comfortable tap-target reach — this is no longer a blocking session handoff.</done>
</task>

</tasks>

<verification>
- Full suite green incl. AdminBarTest, UX-08a/BUG-06/BUG-07 e2e, and the L301/L355 baselines.
- BUG-06 e2e exercises a separator-bearing menu (passes, not skipped).
- `npm run screenshots` deterministically writes ux-08a-782.png + ux-08a-600.png; the guard keeps the
  normal e2e suite from overwriting them; PNGs committed under the phase screenshots/ dir.
- No PRODUCTION source modified by this plan — only a new guarded test spec + a `screenshots` npm script.
</verification>

<success_criteria>
The Phase 11 zero-regression bar holds: JS >=53/53, PHP unit >=61/61, integration green incl.
AdminBarTest, e2e green incl. the three new tests and unchanged baselines, plus deterministic mobile
screenshots for UX-08a (no manual browser handoff). The phase is ready for `/gsd:verify-work` and the
v1.2 traceability flip (UX-08 / BUG-06 / BUG-07 -> Complete).
</success_criteria>

<output>
After completion, create `.planning/phases/11-editor-entry-reorder-fixes/11-04-SUMMARY.md`.
</output>
